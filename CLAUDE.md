# কর্জে হাসানা ফাউন্ডেশন — ডেভেলপমেন্ট নির্দেশনা

## 🎨 থীম নিয়ম (বাধ্যতামূলক — প্রতিটি ফিচারে)

যেকোনো নতুন ফিচার, পেজ, বা কম্পোনেন্ট তৈরির সময় **ডার্ক ও লাইট — দুই থীমেই** কাজ করতে হবে।

1. **দুই থীমেই যাচাই করতে হবে।** লাইট থীম চালু হয় `<html data-theme="light">` দিয়ে; ডার্ক থীম ডিফল্ট। নতুন কোনো রঙ/সারফেস যোগ করলে `kh-ui.css`-এর `html[data-theme="light"]` অংশে তার লাইট-ভ্যারিয়েন্টও যোগ করতে হবে।

2. **সাদা লেখা = রঙিন পটভূমি।** কোনো বাটন, চিপ, ব্যাজ বা লেবেলের ফন্ট কালার সাদা (বা হালকা) হলে সেটির পটভূমি অবশ্যই রঙিন/গ্র্যাডিয়েন্ট হতে হবে — থীম যেটাই হোক। সাদা পটভূমিতে সাদা লেখা কখনো নয়।

3. **কনট্রাস্ট।** লেখা ও পটভূমির কনট্রাস্ট অন্তত ৪.৫:১ হতে হবে (WCAG AA)। সন্দেহ হলে মেপে নিতে হবে।

4. **হার্ডকোড রঙ এড়ানো।** সরাসরি `#fff`/`#000` না দিয়ে যথাসম্ভব থীম ভেরিয়েবল (`--kh-*`) বা উভয় থীমের নিয়ম ব্যবহার করতে হবে।

## 🎨 ব্র্যান্ড রঙ (Aurora থীম)

- গোলাপি `#d6336c` · কমলা `#e95420` · অ্যাম্বার `#f08c28` · বেগুনি `#7048e8`
- গাঢ় বেস: প্লাম `#241539`, `#160d26`
- ব্র্যান্ড গ্র্যাডিয়েন্ট বাটন: `linear-gradient(112deg,#d6336c,#a021c9 48%,#e83e8c)` বা কাছাকাছি

## 🏗️ আর্কিটেকচার

- **shared kit:** `kh-ui.css` + `kh-ui.js` — নেভবার, চ্যাট, থীম টগল, কার্সর গ্লো, পাসওয়ার্ড মিটার, প্রিমিয়াম ইমেইল ইনপুট, ফর্ম অটো-ফিল। নতুন সাইট-ব্যাপী কম্পোনেন্ট এখানেই যোগ করতে হবে যাতে সব পেজে একসাথে আসে।
- সব পেজ এই দুটি ফাইল `</body>`-এর আগে লোড করে।
- **ব্যাকএন্ড:** Supabase (`fgczixybyrzkrsoqrgdl`)। ব্যক্তিগত/আর্থিক টেবিলে RLS আবশ্যক; anon-কে শুধু প্রয়োজনীয় SELECT/INSERT দিতে হবে; SECURITY DEFINER ফাংশনে `auth.uid()` যাচাই ও `IS DISTINCT FROM` ব্যবহার (NULL ফাঁক এড়াতে)।

## ⚠️ যে ভুলগুলো আগে হয়েছে — আর করা যাবে না

1. **প্রতিটি পেজে সুপাবেস লোড করতে হবে।** `kh-ui.js`-এর লগইন-চিপ, অটোফিল, সেটিংস — সবই `_db` ছাড়া চলে না। তাই প্রতিটি HTML-এ `kh-ui.js`-এর **আগে**:
   `<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>` ও `<script src="_supabase.js"></script>`
   (index/about/contact/loan-application-এ এগুলো ছিল না — নেভবারে নাম দেখাত না, ফর্ম অটোফিল হত না।)

2. **`const _db` window-এ যায় না।** `_supabase.js`-এ তাই স্পষ্টভাবে `window._db = _db;` আছে — সরানো যাবে না। শেয়ার্ড কোডে সবসময় `sb()` হেল্পার (`KHUI.db()`) ব্যবহার করতে হবে, সরাসরি `window._db` নয়।

3. **`insert().select()` করতে হলে SELECT policy লাগে।** `donations`-এ INSERT policy ছিল কিন্তু SELECT ছিল না — ফলে `insert().select().single()` পুরো লেনদেন রোলব্যাক করত এবং **দান নীরবে হারিয়ে যেত**। তাই লেনদেন লেখার কাজ SECURITY DEFINER RPC দিয়ে করতে হবে (`record_donation`, `record_loan_payment`), সরাসরি `from().insert()` নয়।

4. **supabase-js-এর `rpc()`/`from()`-এ `.catch()` নেই।** `await _db.rpc(x).catch(...)` লিখলে পেজ ক্র্যাশ করে (একবার লগইনে "সংযোগ ত্রুটি" দেখিয়েছিল)। `try { await ... } catch {}` ব্যবহার করতে হবে।

4. **রিডাইরেক্ট লুপ এড়ানো।** কোনো পেজ প্রোফাইল না পেলে লগইন পেজে পাঠাবে না — বরং সেখানেই ব্যবস্থা (যেমন `my-dashboard.html`-এর "সদস্য প্রোফাইল তৈরি করুন" ফর্ম) বা বার্তা দেখাবে। লোডিং গেট যেন কখনো আটকে না থাকে (টাইমআউট + `catch`-এ ফলব্যাক)।

5. **ক্যাশ-বাস্টিং।** `kh-ui.css`/`kh-ui.js`-এ বড় পরিবর্তনের পর সব পেজে `?v=` সংখ্যা বাড়াতে হবে, নাহলে ব্রাউজার পুরনো ফাইল ধরে রাখে।

6. **🧭 প্রতিটি নতুন পেজে নেভবার থাকতে হবে (সিদ্ধান্ত: ১৩ সেপ্টেম্বর ২০২৬)।** সাইটের **সব** পেজে গ্লাস নেভবার থাকবে — লগইন, অ্যাডমিন প্যানেল, ওয়ালেট, কোনো ব্যতিক্রম নেই। নতুন পেজ বানানোর সময়:
   • `<body>`-তে **`data-kh-nav="on"`** (অথবা অ্যাট্রিবিউটটি একেবারে না দেওয়া — ডিফল্টই "on")।
   • **`data-kh-nav="off"` কখনো দেওয়া যাবে না।** থীম টগলটি এখন কেবল নেভবারের ভেতরেই থাকে (`.kh-glassnav-top.kh-theme-btn`) — আলাদা/ভাসমান থীম বাটন **মুছে ফেলা হয়েছে**, তাই নেভবার বন্ধ করলে ঐ পেজে থীম বদলানোর কোনো পথই থাকবে না।
   • নেভবার এলে চ্যাট উইজেটও আসে। অ্যাডমিন-ঘরানার পেজে সেটি অপ্রয়োজনীয় — তখন `data-kh-chat="off"` দিতে হবে (যেমন `dashboard.html`, `admin.html`)।
   • নেভবারে নতুন কোনো লিংক যোগ করতে হলে `kh-ui.js`-এর `NAV_ITEMS` তালিকায় বসাতে হবে — প্রতিটি HTML-এ আলাদা করে নয়।
   • ⚠️ নেভবার/লগইন-চিপ `_db` ছাড়া চলে না, তাই ১ নম্বর নিয়ম মেনে সুপাবেসের দুটি স্ক্রিপ্ট `kh-ui.js`-এর **আগে** লোড করতে হবে।
   বিস্তারিত ও যাচাইয়ের মান: "🌓 নেভবার সব পেজে · আলাদা থীম বাটন বাতিল" অংশে।

7. **🗄️ নতুন টেবিলে স্পষ্ট GRANT বাধ্যতামূলক (Supabase-এর নোটিশ, কার্যকর ৩০ অক্টোবর ২০২৬)।** ঐ তারিখ থেকে `public` স্কিমায় নতুন টেবিল বানালে Supabase আর নিজে থেকে Data API-র অধিকার দেবে না — GRANT ছাড়া supabase-js থেকে `permission denied` আসবে। **বিদ্যমান টেবিলে কিছু বদলায় না।**
   • প্রতিটি নতুন টেবিলের **একই মাইগ্রেশনে** GRANT লিখতে হবে।
   • ⚠️ Supabase-এর মেইলের নমুনা (anon-কে SELECT, authenticated-কে সব) **হুবহু বসানো যাবে না** — এই প্রকল্পের নিয়ম: RLS চালু, কেবল যে অধিকার সত্যিই লাগে সেটুকু; আর্থিক/ব্যক্তিগত টেবিলে anon/authenticated-কে সরাসরি লেখার অধিকার নয়, লেখা হবে SECURITY DEFINER RPC দিয়ে। শুধু RPC-র ভেতরে ব্যবহৃত টেবিলে (যেমন ভিজিটর কাউন্টার) anon/authenticated-কে **কোনো** GRANT নয়, কেবল `service_role`।
   • সাধারণ ছক: `grant select, insert, update, delete on public.<t> to service_role;` + দরকার হলে `grant select on public.<t> to authenticated;` (RLS পলিসিসহ)।

## 👥 কমিটি ও প্রার্থীর সম্মতিপত্র (committee.html)

- **ডায়নামিক কাঠামো:** `committees` ও `committee_positions` টেবিল। অ্যাডমিন dashboard → "কমিটি ব্যবস্থাপনা" থেকে কমিটি/পদ যোগ, সম্পাদনা, মুছে ফেলা, ক্রম বদল, পদের সংখ্যা নির্ধারণ করতে পারেন। কোড হার্ডকোড করা নেই।
- **আবেদন:** `committee_applications` — সম্মতিপত্রের ১৩টি ঘর + `consent_scan_url` (স্বাক্ষরিত হার্ডকপি, `committee-docs` প্রাইভেট বাকেট) + `nid_front_url`/`nid_back_url` (`nid-docs`) + `photo_url` (`avatars`)। `unique (user_id, committee_id)` — এক কমিটিতে একজন একবার।
- **⚠️ ই-মেইল OTP বন্ধ (সিদ্ধান্ত: ৩১ আগস্ট ২০২৬)।** সম্মতিপত্রে কোড দিয়ে অ্যাকাউন্ট তৈরির ধাপ সরানো হয়েছে — সবার ই-মেইল জানা থাকে না।
  • **আমন্ত্রণ টোকেন থাকলে (`?inv=`) লগইন ছাড়াই দাখিল** — `submit_application_by_invite(token, payload)`; `user_id` null থাকে, পরে একই ই-মেইলে অ্যাকাউন্ট খুললে `link_my_committee_applications()` জুড়ে দেয়।
  • লগইন ছাড়া আপলোড শুধু `committee-docs/invite/<বৈধ টোকেন>/…` পথে — স্টোরেজ পলিসি `invite_token_valid()` দিয়ে যাচাই করে।
  • টোকেন ছাড়া ও লগইন ছাড়া এলে ফর্ম দেখায় না, `user-login.html`-এ পাঠায়।
  • **আমন্ত্রণে আসা ব্যক্তি শুধু ফর্মটিই পূরণ করতে পারেন** — উপরের "কমিটির তালিকা" লিংক লুকিয়ে "হোম" দেখানো হয় (`lnkList`/`lnkHome`), দাখিলের পরও হোম পেজের লিংক যায়। কমিটির বিস্তারিত বা অন্য পদে আবেদনের পথ তাঁর সামনে থাকে না।
- **হোয়াটসঅ্যাপে আমন্ত্রণ:** `admin_create_invite`-এ ই-মেইল **ঐচ্ছিক**, মোবাইল থাকলেই চলে। ড্যাশবোর্ডের বাটন `wa.me/<৮৮০নম্বর>?text=<বাংলা বার্তা+লিংক>` খুলে দেয় — সম্পূর্ণ ফ্রি, কোনো API লাগে না। ই-মেইল জানা থাকলে `send-invite` দিয়ে মেইলেও পাঠানো যায়।
- **ফাউন্ডেশনের হোয়াটসঅ্যাপ নম্বর** এখন সুপার অ্যাডমিন → জেনারেল সেটিংস (`app_settings.whatsapp` = `{number,label,enabled}`)। `kh-ui.js`-এর `WA_NUMBER` কেবল ফলব্যাক। বিস্তারিত: `হোয়াটসঅ্যাপ_সংযুক্তি_গাইডলাইন.md`।
- **অ্যাডমিন পক্ষে পূরণ:** `user_id` nullable — যাঁর অ্যাকাউন্ট নেই তাঁর পক্ষেও ফর্ম ভরা যায়। ঐ ব্যক্তি পরে একই ই-মেইলে লগইন করলে `link_my_committee_applications()` আবেদনটি তাঁর সাথে জুড়ে দেয়।
- **কমিটি চূড়ান্ত অনুমোদিত হলে (`is_approved`) নতুন আবেদন বন্ধ** — `submit_committee_application()` তখন `applications closed` ছুড়ে দেয় (অ্যাডমিন ছাড়া)।
- **আলাদা ফর্ম পেজ:** `committee.html` শুধু কাঠামো ও নিজের আবেদনের অবস্থা দেখায়; "আবেদন" এখন একটি লিংক → `committee-form.html?c=<committee>&p=<position>`। আগে একই পেজে ফর্ম খুলত এবং `msg()`-এর `offsetTop` ভিত্তিক স্ক্রল পেজকে উপরে টেনে নিত — সেই সমস্যা আর নেই।
- **ছাপানো সম্মতিপত্র (সরল, ১ পাতা):** `KHF_সম্মতিপত্র_A4.docx` — ফর্ম নং KHF-CC-01, শিরোনাম শুধু "সম্মতিপত্র"। ঘোষণার দফাগুলো ও "কার্যালয়ের ব্যবহারের জন্য" অংশ **বাদ** (সিদ্ধান্ত: ২৫ আগস্ট ২০২৬); স্বাক্ষরের ঠিক উপরে এক লাইনের সম্মতি থাকে। ঘর: কমিটি ও পদ · নাম (বাংলা/ইংরেজি) · পিতা/স্বামী · মাতা · NID · জন্ম তারিখ · পেশা · মোবাইল · ই-মেইল · জেলা · দুই ঠিকানা · প্রস্তাবক/সমর্থক · প্রার্থীর স্বাক্ষর-তারিখ-সভাপতি/সহ-সভাপতির স্বাক্ষর · সংযুক্তির চেকলিস্ট। (বুড়ো আঙুলের ছাপের ঘর বাদ — সিদ্ধান্ত: ২৫ আগস্ট ২০২৬)
- `committee-form.html`-এর "A4 কাগজে ছাপান" বাটন হুবহু এই ফর্মটিই খোলে (`@page{size:A4}`), তবে টাইপ করা তথ্য বসানো অবস্থায় — ছাপিয়ে স্বাক্ষর করে স্ক্যান কপি আপলোড করা যায়। **কাগজের ফর্ম বদলালে এই প্রিন্ট-ভিউও একই সাথে বদলাতে হবে।**
- **অটো-ফিল:** লগইন থাকলে `prefill()` `get_my_profile` থেকে নাম, পিতার নাম, NID, জন্ম তারিখ, পেশা, মোবাইল, ঠিকানা ও সংরক্ষিত ছবি/NID বসিয়ে দেয় এবং কী কী বসল তা বার্তায় জানায়। "প্রোফাইল থেকে আবার আনুন" বাটন `prefill(true)` — পূরণ করা ঘরও নতুন করে বসায়। পেজ লোডে সেশন **আগে** পড়া হয় (`refreshSession()`), তাই লগইন করা ব্যবহারকারীকে আর কোড চাওয়া হয় না; ক্লিকের সময়ও একবার যাচাই হয়।
- **একাধিক কমিটি:** `unique (user_id, committee_id)` — একজন প্রতি কমিটিতে একটি আবেদন, কিন্তু **যত খুশি কমিটিতে**। একই কমিটিতে আবার দাখিল করলে পদ বদলে হালনাগাদ হয়। ⚠️ `prefill()`-এ প্রতিবার `btnSubmit` রিসেট করতে হয় — নাহলে এক কমিটিতে অনুমোদিত হলে বাটন লক থেকে যেত এবং অন্য কমিটিতে আবেদন করা যেত না।
- **প্রোফাইল সিঙ্ক:** `trg_sync_profile_from_capp` ট্রিগার সম্মতিপত্রের তথ্য দিয়ে `user_profiles`-এর ফাঁকা ঘর ভরে দেয় ও প্রোফাইল না থাকলে তৈরি করে। ছবি/NID আপলোডে `set_my_doc_urls()`।
- **আমন্ত্রণ (নতুন):** `committee_invites` — অ্যাডমিন dashboard → "আমন্ত্রণ পাঠান" থেকে কমিটি, পদ, নাম ও ই-মেইল দিয়ে আমন্ত্রণ তৈরি হয়; Edge Function `send-invite` (verify_jwt, ভেতরে অ্যাডমিন যাচাই) ই-মেইলে লিংক পাঠায়: `committee-form.html?inv=<token>`।
  • লিংক খুললে `get_committee_invite(token)` (anon-ও পারে) কমিটি ও পদবি বসিয়ে **লক** করে দেয়, ই-মেইলও আগেই বসে — শুধু কোড দিলেই অ্যাকাউন্ট তৈরি হয়ে যায়।
  • অবস্থা: `sent` → `opened` (প্রথমবার লিংক খুললে) → `submitted` (দাখিলের পর `mark_invite_submitted`)। মেয়াদ ৩০ দিন; একই ই-মেইলে নতুন আমন্ত্রণ পাঠালে আগেরটি নিজে থেকেই `cancelled` হয়।
  • অ্যাডমিন তালিকা থেকে লিংক কপি, আবার পাঠানো ও বাতিল করা যায়।
- **RPC:** `get_committee_structure` (পাবলিক) · `submit_committee_application` · `get_my_committee_applications` · `review_committee_application` · `admin_committee_applications` · `admin_save_committee` / `admin_delete_committee` / `admin_set_committee_state` / `admin_save_position` / `admin_delete_position` · `admin_list_users` / `admin_get_profile`।
- **NID দেখা:** `my-profile.html`-এ নিজের NID-এর দুই পাতা signed URL দিয়ে ছবি হিসেবেই দেখা যায় (ক্লিকে জুম)। অ্যাডমিন dashboard → "ব্যবহারকারী ও প্রোফাইল" থেকে যেকোনো সদস্যের প্রোফাইল ও NID দেখতে পারেন। storage policy: মালিক অথবা `is_admin()`।

## ⚠️ `is_admin()` দুই টেবিল দেখে (সংশোধন: ২৫ আগস্ট ২০২৬)

`is_admin()` আগে শুধু `admin_users` (auth_id) দেখত, কিন্তু বাস্তবে অ্যাডমিনরা আছেন `admin_roles`-এ (user_id)। ফলে **১৪টি পলিসিতে** অ্যাডমিনের লেখা নীরবে আটকে যেত — `savings_transactions` অনুমোদন ০ সারি আপডেট করত, কোনো ত্রুটি বার্তা ছাড়াই ("সঞ্চয় জমা এপ্রুভ করতে পারছি না"-র আসল কারণ)। এখন দুটো টেবিলই দেখে, সাথে `is_super_admin()` যোগ হয়েছে।
**নিয়ম:** RLS-নির্ভর `update`/`delete`-এ সবসময় `.select()` যোগ করে ফেরত সারি গুনতে হবে — নাহলে ব্যর্থতা অদৃশ্য থাকে।

## 🔔 রিমাইন্ডার সিস্টেম (মাসিক সঞ্চয় ও ঋণের কিস্তি)

- **নিয়ম:** `app_settings` → `savings_rules` (`due_day`, `grace_days`, `min_monthly`, `reminder_enabled`) ও `loan_rules`। সুপার অ্যাডমিন → জেনারেল সেটিংস থেকে বদলায়।
- **বকেয়া হিসাব:** `generate_due_reminders()` → `reminders` টেবিল। pg_cron job `kh_daily_due_scan` প্রতিদিন UTC ০২:১৫ (BD ৮:১৫) চালায়।
- **ই-মেইল:** Edge Function `send-reminders` (Gmail SMTP)। সিক্রেট: `GMAIL_USER`, `GMAIL_APP_PASSWORD`, `REMINDER_CRON_SECRET`। হেডার `x-reminder-secret` ছাড়া কল করলে 401।
- **অ্যাডমিন:** dashboard.html → "বকেয়া রিমাইন্ডার" প্যানেল (তালিকা, হিসাব, এখনই পাঠানো)।
- SMS পরে যোগ হবে — `reminders.channel` কলামে `'sms'` রাখার জায়গা আছে।

## 🧾 ডিজিটাল ভাউচার

- **ভাউচার:** `KHUI.voucher({title,no,amount,name,code,mobile,photo,rows,status,note})` — kh-ui.js-এ। সঞ্চয় জমা/উত্তোলনে স্বয়ংক্রিয়ভাবে দেখায়; দানের ভাউচার donation.html-এ আলাদা (সেখানে ছবি `paintVoucherMember()` বসায়)। সব ইনপুট `vEsc()` দিয়ে এসকেপ হয় — XSS পরীক্ষিত।
- ভাউচারের ভেতরটা সবসময় সাদা কাগজের মতো (দুই থীমে এক), কারণ এটি প্রিন্ট হয়। প্রিন্ট নতুন উইন্ডোতে HTML হিসেবে — টেক্সট ঝকঝকে থাকে।
- **ভাউচার ই-মেইল:** Edge Function `send-voucher` (verify_jwt চালু)। `KHUI.sendVoucherMail(kind, id, {node})` — kind: `donation` · `savings` · `loan_disburse` · `loan_repayment`। ই-মেইল ঠিকানা **কখনো request body থেকে নেওয়া হয় না** (spam রোধ); সদস্য শুধু নিজের লেনদেন পাঠাতে পারেন, অ্যাডমিন সংশ্লিষ্ট ব্যক্তির ঠিকানায়। ভাউচারে `email:{kind,id}` দিলে ই-মেইল স্বয়ংক্রিয়ভাবে যায় ও নিচে নোটিশ দেখায়।
- **⚠️ denomailer ব্যবহার নিষিদ্ধ (সিদ্ধান্ত: ২৩ আগস্ট ২০২৬)।** denomailer বাংলা (non-ASCII) হেডার ও multipart গঠন ভেঙে ফেলত — Gmail-এ সাবজেক্টে কাঁচা `=?utf-8?Q?…` আর বডিতে `From:`/`To:`/`Content-Type: multipart/mixed; boundary=attachment100` লাইনগুলো টেক্সট হিসেবে দেখা যেত (এটাই "আবোল-তাবোল টেক্সট"-এর আসল কারণ; অ্যাটাচমেন্ট বাদ দিয়েও সারেনি)।
  এখন `mailer.ts` — নিজের হাতে লেখা SMTP + MIME, `send-voucher` ও `send-reminders` দুটোতেই কপি আছে:
  • হেডার RFC 2047 Base64 encoded-word, ৪২ বাইটের চাঙ্কে ভাঙা (৭৫ অক্ষরের সীমা) — UTF-8 অক্ষর কখনো মাঝখানে ভাঙে না
  • text ও html দুটোই Base64, ৭৬ অক্ষরে মোড়া — quoted-printable-এর কোনো ফাঁদ নেই
  • ছবি থাকলে গঠন `multipart/related` → [ `multipart/alternative` (text+html), `image/png` + `Content-ID: <voucher>` ]; HTML-এ `<img src="cid:voucher">`
  • `Deno.connectTls` → EHLO · AUTH PLAIN · MAIL FROM · RCPT TO · DATA · QUIT; `Smtp` ক্লাসে একই সংযোগে অনেক মেইল (রিমাইন্ডার)
  যাচাই: pg_net দিয়ে ফাংশন কল করে Gmail-এর `235 Accepted` ও `250 OK` লগ মিলিয়ে দেখা হয়েছে। নতুন মেইল কোড লিখলে **অবশ্যই** এভাবে সত্যিকারের মেইল পাঠিয়ে যাচাই করতে হবে — অনুমানে নয়।
- **⚠️ `conn.write()` সব বাইট লেখে না (partial write) — ভাউচার ই-মেইল আটকে থাকার আসল কারণ।** ছোট টেস্ট মেইল (৪ KB) এক লেখাতেই যেত, তাই ঠিক দেখাত; কিন্তু ছবিসহ মেইল (কয়েক শ KB) অর্ধেক লেখা হত → Gmail শেষের `\r\n.\r\n` পেত না → উত্তরের অপেক্ষায় বসে থাকত → ক্লায়েন্টে "ভাউচারের একটি কপি ই-মেইলে পাঠানো হচ্ছে…" চিরকাল ঘুরত।
  সমাধান: `mailer.ts`-এ `writeAll()` লুপ (সব বাইট লেখা নিশ্চিত), পড়ায় ৪৫ সেকেন্ডের ডেডলাইন, আর `KHUI.sendVoucherMail()`-এ ৭৫ সেকেন্ডের `AbortController` — কোনো অবস্থাতেই UI ঝুলে থাকবে না। ছবি ৩ MB-র বেশি হলে ই-মেইলে শুধু ডাউনলোড লিংক যায়; html2canvas স্কেল ১.৭।
- **ছবি কীভাবে যায় (সংশোধন: ২৩ আগস্ট ২০২৬)।** ক্লায়েন্ট html2canvas দিয়ে PNG বানায় → Edge Function সেটি (ক) ই-মেইলের ভেতরেই inline `cid:voucher` হিসেবে বসায়, (খ) `vouchers` পাবলিক বাকেটে রেখে "ছবি ডাউনলোড" বাটনের লিংক দেয়।
  ⚠️ **SMTP attachment/`cid:` আর ব্যবহার করা যাবে না।** denomailer-এর multipart অ্যাটাচমেন্ট গঠনে ই-মেইল ভেঙে যেত এবং base64 আবোল-তাবোল টেক্সট হিসেবে দেখাত। এখন MIME শুধু text+html — সব অ্যাপে ঠিক দেখায়।
- **✅ ছবি তোলার জন্য ভাউচার পর্দায় থাকার দরকার নেই (১০ সেপ্টেম্বর ২০২৬ — দানের ভাউচার না যাওয়ার আসল কারণ)।** সঞ্চয়ে ছবি+PDF যেত কিন্তু দানে যেত না। কারণ: `donation.html`-এ ভাউচারের ওভারলে `.show` না পাওয়া পর্যন্ত `#voucherDoc`-এর মাপ **০×০**, আর `voucherToPng()` ৪০px-এর কম হলে সরাসরি `null` দিত — অর্থাৎ ছবি/PDF **তৈরিই হত না**, শুধু cron-এর সাধারণ রশিদ যেত (৯৮ সেকেন্ড পর)। সঞ্চয়ে কিটের নিজের ভাউচার সবসময় দৃশ্যমান, তাই ওটি কাজ করত — এটাই ছিল দুটোর পার্থক্য।
  লাইভ প্রমাণ: ওভারলে বন্ধ → `[0,0]`, খোলা → `[580,936]`; আর দৃশ্যমান নোড হাতে দিয়ে `sendVoucherMail` চালালে `{image:true, pdf:true}`।
  **সমাধান:** নোড লুকানো/০-মাপ হলে `voucherToPng()` এখন নোডটি ক্লোন করে `position:fixed;left:-10000px` হোস্টে বসায় (লেআউট পায়, চোখে পড়ে না), সেটির ছবি তোলে, তারপর হোস্ট মুছে দেয় — সফল, ব্যর্থ ও টাইমআউট, তিন পথেই `cleanup()`। উচ্চতা ১১০০px-এর বেশি হলে স্কেল ১.৩৫ (নাহলে ১.৭) যাতে মেইল ভারী না হয়।
  **নিয়ম:** ভাউচারের ছবি লাগে এমন নতুন কোডে ওভারলে খোলা থাকা বা `setTimeout`-এর উপর নির্ভর করা যাবে না — `voucherToPng()` নিজেই সামলায়।
- **(পুরনো, এখন অপ্রযোজ্য) ছবি তোলার শর্ত: ভাউচার অবশ্যই পর্দায় থাকতে হবে।** `KHUI.voucherToPng()` নোডের মাপ ৪০px-এর কম হলে (লুকানো/`display:none`) `null` দেয়, আর html2canvas আটকে গেলে ১২ সেকেন্ডে টাইমআউট হয় — তাই ই-মেইল কখনো ঝুলে থাকে না।
  তাই দানে (`donation.html`) সফল লেনদেনের পর আগে `openVoucher()` চলে (ভাউচার পূরণ ও দেখানো), তার ~০.৯ সেকেন্ড পর `autoMailDonation()` — আগে উল্টো ছিল, ফলে ভাউচার ফাঁকা/লুকানো থাকায় দানের ই-মেইল যেতই না। অ্যাডমিনের ঋণ-বিতরণ বাটনও (`mailLoanVoucher`) এখন `KHUI.voucher()` দিয়ে ভাউচার দেখিয়ে তারপর মেইল করে।
- **⚠️ বেনামী দানে রশিদ যেত না — আসল কারণ (৭ সেপ্টেম্বর ২০২৬)।** `record_donation`-এ শর্ত ছিল `if not p_is_anonymous and mobile <> ''` — অর্থাৎ **বেনামী দানে দাতার রেকর্ডই তৈরি/খোঁজা হত না**, ফলে `donations.donor_id` null থাকত। `buildVoucher` ঠিকানা খোঁজে `user_profiles.donor_id` / `donors.id` দিয়ে — তাই `no email` হয়ে রশিদ কোথাও যেত না, ব্যাচও `skipped` লিখত। ভাউচার পর্দায় তৈরি হত, তাই বাইরে থেকে মনে হত "ভাউচার হয়েছে কিন্তু মেইল যায়নি"।
  **নিয়ম:** "বেনামী" মানে **শুধু প্রকাশ্য তালিকায় নাম না দেখানো** — দাতার রেকর্ড, রশিদ ও আয়কর প্রত্যয়নপত্র সব আগের মতোই থাকবে। এখন মোবাইল বা ই-মেইল যেকোনোটি থাকলেই donor খোঁজা/তৈরি হয় (ই-মেইল দিয়েও মেলানো হয়), `donations.donor_name` থাকে `'Anonymous'` ও `is_anonymous=true`। দান/দাতা টেবিল কেবল অ্যাডমিন পড়তে পারে, তাই লিংক করায় কিছু ফাঁস হয় না।
  `record_donation` এখন `has_email` ফেরত দেয় — ঠিকানা না থাকলে `donation.html` মিথ্যা আশ্বাস না দিয়ে স্পষ্ট করে জানায় (`#noMailWarn` ফর্মেই আগাম সতর্ক করে)। ⚠️ `KHUI.sendVoucherMail` লগইন ছাড়া চলে না — তাই অতিথির দানে ক্লায়েন্ট থেকে মেইল যায় না, ১৫ মিনিটের ব্যাচই পাঠায়; বার্তাও এখন সেটিই বলে।
  যাচাই: পুরনো রেকর্ড মোবাইল দিয়ে ব্যাকফিল করে ৭ সেপ্টেম্বরের ৳৫,০০০-এর রশিদ সত্যিই গেছে, এবং নতুন কোড দিয়ে একটি বেনামী দান করে দেখা গেছে `donor_id` বসে ও মেইল `sent` হয়।
- **✅ হুবহু ভাউচার এখন "জমা দেওয়া" হয়, ব্রাউজার নিজে মেইল করে না (১০ সেপ্টেম্বর ২০২৬ — চূড়ান্ত ব্যবস্থা)।** `verify_jwt` ঠিক করার পরেও ছবি/PDF যায়নি: ৳১৭,০০০-এর দানেও লগে সারি লেখা হয়নি, আবার ১৬৭ সেকেন্ড পর cron ফলব্যাক গেছে। কারণ **`KHUI.sendVoucherMail` লগইন ছাড়া চলতই না** (`if (!sess) throw 'আগে লগইন করুন'`) — আর দানের পেজে লগইন থাকার নিশ্চয়তা নেই (সেশন মেয়াদ শেষ হলেও পেজ কাজ করে, দাতা মোবাইল দিয়েই মিলে যায়)। **ব্রাউজার→Edge Function পথটাই তাই বাদ।**
  • **নতুন পথ:** ব্রাউজার ছবি ও A4 PDF বানিয়ে `attach_voucher_asset(kind, ref_id, png, pdf)` RPC-তে **জমা দেয়** → তারপর `request_voucher_mail(kind, ref_id)` সার্ভারকে বলে "এখনই পাঠাও" (`run_voucher_mailer(0)`; গোপন কি ডাটাবেজের বাইরে যায় না)। ব্যাচ পাঠানোর সময় `voucher_assets` থেকে কপিটি তুলে নেয়, ছবি ইনলাইন + PDF সংযুক্ত করে, পাঠানো হলে সারি মুছে দেয়। **লগইন লাগে না** — `record_donation` যে PostgREST পথে কাজ করে, ঠিক সেই পথ।
  • **নিরাপত্তা:** `voucher_assets`-এ RLS, anon/authenticated-এর কোনো সরাসরি অধিকার নেই। `attach_voucher_asset` (SECURITY DEFINER, anon-ও পারে) শুধু **১৫ মিনিটের ভেতরের সত্যিকারের লেনদেনে** কাজ করে (`voucher_ref_is_fresh`), প্রতি লেনদেনে **একবারই**, আর base64-এর অক্ষর ও মাপ যাচাই করে। ই-মেইল ঠিকানা আগের মতোই কেবল সার্ভার ঠিক করে — request body থেকে কখনো নয়।
  • **⚠️ `run_voucher_mailer()` এখন `run_voucher_mailer(p_grace int default 90)`** — পুরনো ০-প্যারামিটার সংস্করণ ড্রপ করা হয়েছে। সবাই `90` দেয়।
  • **⚠️ দৌড়ের ফাঁদ (race) — grace ০ দিয়ে পাঠানো নিষিদ্ধ।** আগে `request_voucher_mail` grace=0 দিয়ে **সব** বাকি রেকর্ড পাঠাত; দুজন প্রায় একসাথে দান করলে যাঁর ব্রাউজার আগে শেষ করত তাঁর অনুরোধেই অন্যজনের রশিদ ছবি/PDF ছাড়া বেরিয়ে যেত। **নিয়ম:** ৯০ সেকেন্ডের অপেক্ষা আসলে ব্রাউজারের কপির জন্য — তাই `pending_voucher_mails`-এর শর্ত এখন **বয়স ≥ grace অথবা কপি জমা আছে** (`exists voucher_assets`)। কপি এলেই কেবল সেই রেকর্ডটি যায়, বাকিরা নিজের সময় পায়। যাচাই: দুটি দান পাশাপাশি বানিয়ে দেখা গেছে কপি ছাড়া তালিকা ফাঁকা, A-এর কপি জমা পড়লে তালিকায় **শুধু A**।
  • অব্যবহৃত কপি ৭ দিন পর `run_voucher_mailer()` নিজেই মুছে দেয় (pending জানালাও ৭ দিন)।
  • **⚠️ সঞ্চয় জমার রশিদ `pending` অবস্থাতেই যায় (১০ সেপ্টেম্বর ২০২৬)।** তালিকার শর্ত ছিল `status='approved'` — কিন্তু সদস্য জমা দেওয়ামাত্রই পর্দায় ভাউচার দেখেন ("অপেক্ষমাণ") ও সেটিই মেইলে চান; অনুমোদন আসতে দিন লেগে যেতে পারে, ততক্ষণ কপিটি `voucher_assets`-এ পড়ে থাকত আর ৭ দিনে মুছে যেত। এখন `status in ('approved','pending')`। লগ একই ref-এ একবারই লেখা হয়, তাই অনুমোদনের পর দ্বিতীয় কপি যায় না — **অনুমোদনের আলাদা নিশ্চিতকরণ মেইল এখনো নেই** (দরকার হলে `savings_approved` নামে নতুন kind লাগবে)।
    যাচাই: ৳৬০০-এর `pending` জমার রশিদ কপিসহ গেছে (`KH-SD-940A86`, ৮৭৮ KB — ছবি + PDF), পাঠানোর পর `voucher_assets` সারি মুছে গেছে।
  • **দেরির কারণ ছিল এটাই।** কপি জমা পড়লে রেকর্ড সাথে সাথেই "তৈরি" হয় ও `request_voucher_mail` মেইল পাঠায় — সেকেন্ডের মধ্যে। কপি জমা না পড়লে (পুরনো ক্যাশে করা JS, ব্রাউজার বন্ধ) ৯০ সেকেন্ড অপেক্ষা + ২ মিনিটের cron = ~৩ মিনিট দেরি। অর্থাৎ **দেরি হলেই বোঝা যাবে কপি জমা পড়েনি।**
  • **যাচাই (লগইন ছাড়া, লাইভ পেজ থেকে):** `attach_voucher_asset` → `{"ok":true,"png":true,"pdf":true}`, `request_voucher_mail` → `{"ok":true}`, ব্যাচ → `{"sent":1,"with_exact":1}`; ফাউন্ডেশনের মেইলবক্স থেকে পড়ে দেখা গেছে সংযুক্তি দুটি — `voucher.png` ও `KH-DON-260910-91403.pdf`, বাংলা অটুট।
  • `KHUI.sendVoucherMailDirect()` — পুরনো লগইন-নির্ভর পথটি অ্যাডমিনের ম্যানুয়াল বাটনের জন্য রাখা হয়েছে।
- **⚠️⚠️ `verify_jwt` অবশ্যই `false` — নাহলে ব্রাউজার থেকে ভাউচার পাঠানোই যায় না (১০ সেপ্টেম্বর ২০২৬)।** ৭ সেপ্টেম্বরের v8 ডিপ্লয়ে MCP টুলের ডিফল্টে `verify_jwt=true` হয়ে গিয়েছিল। ফলে ব্রাউজারের **CORS preflight** (`OPTIONS` — যাতে `Authorization` হেডার থাকে না) সুপাবেস গেটওয়েতেই 401 হত, আসল POST আর যেতই না। উপসর্গ: টানা তিনটি দানে (৭ সেপ্টে ২১:৪৫, ৯ সেপ্টে ২৩:৫৯, ১০ সেপ্টে ০৬:০৭) `voucher_mail_log`-এ কোনো সারি লেখা হয়নি, প্রতিবার ঠিক ~৩ মিনিট পর cron ফলব্যাক সাধারণ HTML রশিদ পাঠিয়েছে — অর্থাৎ ছবি/PDF কখনোই যায়নি।
  **নিয়ম:** `deploy_edge_function`-এ **সবসময় `verify_jwt: false` স্পষ্ট করে দিতে হবে** (ফাংশন নিজেই `getUser()` ও `x-voucher-secret` দিয়ে অনুমতি যাচাই করে)। ডিপ্লয়ের পর ফেরত JSON-এ `verify_jwt` মিলিয়ে দেখতে হবে।
  যাচাই: লাইভ পেজ থেকে ভুল টোকেন দিয়ে POST করে দেখা গেছে preflight পাস করে ও ফাংশনের নিজের `{"error":"unauthorized"}` (401) আসে — গেটওয়ের ব্লক নয়।
- **⚠️ ভাউচারের তারিখ/সময় ঢাকার সময়ে (১০ সেপ্টেম্বর ২০২৬)।** Edge Function চলে UTC-তে, তাই `toLocaleString` টাইমজোন ছাড়া ৬ ঘণ্টা পিছিয়ে দেখাত (ভোর ৬:০৭-এর দানে লেখা থাকত ০০:০৭), আর মাসের নাম ইংরেজিতে ("১০ Sept ২০২৬") আসত। `voucher.ts`-এ এখন `TZ = "Asia/Dhaka"` + `Intl.DateTimeFormat(...).formatToParts()` ও বাংলা মাসের নাম — `fmtDate()` (তারিখ+সময়) ও `fmtDay()` (শুধু তারিখ)। **নতুন কোনো তারিখ দেখানোর সময় এই দুটিই ব্যবহার করতে হবে**, সরাসরি `toLocaleDateString` নয়। যাচাই: একই ভাউচার আবার পাঠিয়ে "১০ সেপ্টেম্বর ২০২৬, ০৬:০৭" এসেছে।
- **⚠️ PDF সবসময় সত্যিকারের A4 (১০ সেপ্টেম্বর ২০২৬)।** আগে পাতার উচ্চতা ছবির অনুপাত থেকে হিসাব হত (`format:[210, proportionalH]`) — লম্বা ভাউচারে পাতা হয়ে যেত ২১০×৪২০ মিমি, A4 নয়; ছাপাতে দিলে মাপ মিলত না। এখন তিন জায়গাতেই একই যুক্তি: `format:'a4'` (২১০×২৯৭), ১০ মিমি মার্জিন, ছবি অনুপাত রেখে ভেতরে ফিট ও পাতার মাঝখানে —
  `KHUI.pngToPdfBase64()` (মেইলের সংযুক্তি) · `downloadVoucherPDF()` · `downloadCertPDF()`; `printVoucher()`-এর প্রিন্ট পাতাতেও `@page{size:A4 portrait;margin:10mm}`।
- **📎 হুবহু ভাউচার — PDF সংযুক্তি + ইনলাইন ছবি (৭ সেপ্টেম্বর ২০২৬)।** দাবি ছিল "পর্দায় যে ভাউচার দেখি সেটিই মেইলে আসবে"।
  • **কেন সার্ভার একা পারে না:** হুবহু ছবি বানায় `html2canvas` — সেটি ব্রাউজারের কাজ। Deno-তে হেডলেস ক্রোম নেই, আর Deno-র PDF লাইব্রেরিতে **বাংলা shaping নেই** (যুক্তাক্ষর/কার ভেঙে যায়) — ব্রাউজার-OCR-এর মতোই বাধা। তাই সার্ভার নিজে PDF বানাবে না।
  • **সমাধান (হাইব্রিড):** ব্রাউজার `voucherToPng()` → `KHUI.pngToPdfBase64()` (jsPDF, ছবিটিকে A4 পাতায় বসায় — লেখা ছবির অংশ, তাই বাংলা নিখুঁত) → `{image_base64, pdf_base64}` পাঠায়। `send-voucher` ছবিটি ইনলাইন (`cid:voucher`) বসায়, PDF `<ভাউচার নং>.pdf` নামে সংযুক্ত করে, আর ছবিটি `vouchers` বাকেটেও রাখে (ডাউনলোড লিংক)।
  • **MIME গঠন** (`mailer.ts`): সংযুক্তি+ছবি → `mixed > [ related > [ alternative, image ], files ]`; শুধু ছবি → `related`; শুধু সংযুক্তি → `mixed`; কিছুই না → `alternative`। ফাইলের নাম ASCII না হলে RFC 2047 encoded-word।
  • **৯০ সেকেন্ডের সুযোগ:** `pending_voucher_mails(p_limit, p_grace default 90)` — এর চেয়ে নতুন রেকর্ড ব্যাচ ছোঁয় না, তাই ব্রাউজার আগে হুবহু কপিটি পাঠাতে পারে। ব্রাউজার ব্যর্থ হলে pg_cron (**এখন প্রতি ২ মিনিটে**) সাধারণ HTML রশিদ পাঠায়। ⚠️ পুরনো ১-প্যারামিটার `pending_voucher_mails(integer)` **ড্রপ করা হয়েছে** — নাহলে Edge Function exact-match-এ ওটিই ডাকত ও grace কাজ করত না।
  • ⚠️ **v8 ডিপ্লয়ে `verify_jwt` আবার `true` হয়ে গিয়েছিল** (MCP টুলের ডিফল্ট), ফলে pg_net-এর সিস্টেম কল 401 পেত। সমাধান: `run_voucher_mailer()` এখন `Authorization: Bearer <anon key>` ও `apikey` পাঠায় (anon কি পাবলিক, ক্লায়েন্টের HTML-এ এমনিতেই আছে); আসল অনুমতি যাচাই ভেতরে `x-voucher-secret` দিয়েই হয়। **নতুন করে ডিপ্লয় করলে verify_jwt কী হলো তা মিলিয়ে দেখতে হবে।**
  • **স্পামের জন্য:** `Reply-To`, বৈধ ডোমেইনের `Message-ID` (আগে `@korje-hasana` — TLD ছাড়া) ও `Auto-Submitted` হেডার যোগ হয়েছে। প্রথম কয়েকটি মেইল Gmail স্পামে ফেলতে পারে — "Not spam" দিলে ঠিক হয়ে যায় (৭ সেপ্টেম্বরের ভাউচারগুলো স্পামেই ছিল)।
  • **যাচাই:** ব্রাউজারে সত্যিকারের html2canvas+jsPDF দিয়ে PNG (১৬৬ KB) ও PDF (১১১ KB) বানিয়ে পাঠানো হয়েছে; ফাউন্ডেশনের মেইলবক্স থেকে পড়ে দেখা গেছে সংযুক্তি দুটি — `voucher.png` (image/png) ও `KH-DON-260907-49173.pdf` (application/pdf), আর প্লেইন-টেক্সটে বাংলা অটুট।
- **⚠️ ভাউচারের মেইল আর ব্রাউজারের উপর নির্ভর করে না (৭ সেপ্টেম্বর ২০২৬)।** নামসহ ৳১০,০০০ দান করেও মেইল আসেনি — `voucher_mail_log`-এ কোনো সারিই লেখা হয়নি, অর্থাৎ **ব্রাউজার Edge Function-কে ডাকেইনি** (লগইন ছিল, দাতাও লিংক করা ছিল, ম্যানুয়াল ব্যাচ চালাতেই মেইল গেছে)। ব্রাউজারে কী ভেঙেছে তা বাইরে থেকে জানা যায় না — তাই ব্রাউজারকে জরুরি পথ থেকে সরানো হয়েছে।
  • `kick_voucher_mailer()` — statement-level AFTER ট্রিগার: `donations` (status), `savings_transactions` (status), `loans` (disbursement_date), `repayments` (is_paid)। কিছু বাকি থাকলে সাথে সাথে `run_voucher_mailer()` ডাকে; ব্যর্থ হলেও `exception when others → return null`, তাই **লেনদেন কখনো আটকায় না**। pg_net-এর কল লেনদেনের সাথেই কিউ হয়, রোলব্যাক হলে কলও বাতিল।
  • যাচাই: `record_donation` ডাকার ৩.৫ সেকেন্ডের মধ্যে মেইল `sent` — কোনো ব্রাউজার ছাড়াই। ১৫ মিনিটের pg_cron ব্যাচ এখন কেবল ব্যাকআপ।
  • **দ্বিগুণ কপি ঠেকানো:** ট্রিগার আগে পাঠিয়ে দিলে ব্রাউজার যেন আবার না পাঠায় — `voucher_mail_status(kind,id)` RPC দিয়ে `KHUI.sendVoucherMail()` আগে দেখে নেয়; `sent` হলে `{ok:true, already:true}` ফেরত দেয়, নতুন করে পাঠায় না।
- **সার্ভার-সাইড সেফটি নেট (২ সেপ্টেম্বর ২০২৬)।** ব্রাউজার বন্ধ হয়ে গেলে বা html2canvas ব্যর্থ হলে যেন ভাউচার হারিয়ে না যায় —
  • `voucher_mail_log` (PK `kind, ref_id`) — প্রতিটি পাঠানোর চেষ্টা লেখা হয় (`sent`/`failed`/`skipped`)। `send-voucher`-এর `logMail()` সব পথেই ডাকে।
  • `pending_voucher_mails(p_limit)` — গত ৭ দিনের যে দান/সঞ্চয়/ঋণ বিতরণ/কিস্তির মেইল এখনো যায়নি তার তালিকা।
  • `run_voucher_mailer()` → pg_cron `kh_voucher_mailer` প্রতি ১৫ মিনিটে `{mode:'pending'}` পাঠায়। ⚠️ `net.http_post` — `extensions.net` নয় (cross-database error); ফাংশনে `set search_path to 'public, net'`।
  • সিস্টেম মোডের চাবি `x-voucher-secret` — env `REMINDER_CRON_SECRET` অথবা `app_settings.voucher_cron_secret`। তাই `app_settings`-এর RLS এখন `%secret%` কি anon/authenticated-এর কাছে লুকায়।
  • ব্যবস্থা চালুর আগের সব রেকর্ড `skipped` হিসেবে সিড করা — পুরনো টেস্ট লেনদেনে হঠাৎ মেইল যাবে না।
  • যাচাই: একটি দানের লগ মুছে ব্যাচ চালিয়ে সত্যিকারের মেইল গেছে (`status='sent'`), এবং খালি অবস্থায় `{"ok":true,"sent":0,"considered":0}`।
- **আয়কর রেয়াতের প্রত্যয়নপত্র (স্বয়ংক্রিয় নয় — বাটনে)।** dashboard.html → দান ড্যাশবোর্ড → "🧾 আয়কর রেয়াতের প্রত্যয়নপত্র" কার্ড। করবর্ষ দিলে `admin_donors_by_year(p_year)` দাতার তালিকা (নাম, ই-মেইল, সংখ্যা, সর্বমোট) দেয়; প্রতি সারির "ই-মেইল করুন" বাটন `send-voucher`-এ `{kind:'tax_certificate', donor_id, year}` পাঠায় (অ্যাডমিন JWT)। ভেতরে `donation_year_summary(p_donor,p_year)` বছরের সব দান একত্র করে। ই-মেইল না থাকলে বাটন দেখানোই হয় না। যাচাই: সত্যিকারের মেইল গেছে — `KH-TAX-2026-1BF98F`।
- **ক্যামেরা:** `KHUI.camera({facing,title,guide,onShot})` — লাইভ প্রিভিউ, সেলফিতে ওভাল গাইড ও মিরর (সংরক্ষণে সোজা), পেছনের ক্যামেরায় কার্ড ফ্রেম। প্রোফাইল ছবি ও NID — দুটোতেই ক্যামেরা বা গ্যালারি বেছে নেওয়া যায়।
- **OCR বন্ধ (সিদ্ধান্ত: ২৩ আগস্ট ২০২৬)।** NID-এর ছবি শুধু সংরক্ষণ হয় (`nid-docs` বাকেট); নাম, জন্ম তারিখ, পিতা/মাতার নাম, NID নম্বর — সব সদস্য/অ্যাডমিন নিজে হাতে লেখেন।
  কারণ: Tesseract.js-এর বাংলা মডেল আসল NID কার্ডের বাংলা লেখা পড়তে পারে না (বারবার চেষ্টা করেও আউটপুট অর্থহীন ছিল)। সমস্যা এক্সট্র্যাকশন কোডে নয়, OCR ইঞ্জিনে।
  `kh-ocr.js` ফাইলটি রিপোতে আর কপি হয় না এবং `my-profile.html` থেকে সব OCR কোড/UI/CSS সরানো হয়েছে। ভবিষ্যতে দরকার হলে সার্ভার-সাইড OCR (Google Vision / Azure Read) লাগবে — ব্রাউজার OCR দিয়ে আর চেষ্টা করা যাবে না।

## 👤 নমিনি (savings-portal.html · সিদ্ধান্ত: ২ সেপ্টেম্বর ২০২৬)

- **যেকোনো স্কীমে (দৈনিক/পাক্ষিক/মাসিক/যখন খুশি) নমিনি বাধ্যতামূলক।** `create_depositor`-এ নতুন প্যারামিটার `p_nominees jsonb` — না দিলে ও আগের সেট না থাকলে ফাংশনই `নমিনির তথ্য পূরণ করা বাধ্যতামূলক` ছুড়ে দেয়। ⚠️ পুরনো ৮-প্যারামিটার সংস্করণ **ড্রপ করা হয়েছে**, নাহলে নমিনি ছাড়া হিসাব খোলা যেত।
- **একাধিক নমিনি, শতকরা অংশসহ** (সর্বোচ্চ ৫ জন)। যোগফল ঠিক ১০০ না হলে ক্লায়েন্ট ও `save_my_nominees` — দুই জায়গাতেই বাতিল। "সমান ভাগ" বাটন শেষ জনে অবশিষ্টটুকু বসায় (৩ জনে ৩৩.৩৩+৩৩.৩৩+৩৩.৩৪)।
- **ঘর:** নাম*, সম্পর্ক* (datalist `#nomRelList` — স্ত্রী/স্বামী/পুত্র/…), মোবাইল* (১১ সংখ্যা), NID, জন্ম তারিখ, ঠিকানা, ছবি। ছবি `avatars/<uid>/nominee-<ts>-<i>.<ext>` — ⚠️ স্টোরেজ পলিসি প্রথম ফোল্ডার = `auth.uid()` চায়, তাই পথ অবশ্যই uid দিয়ে শুরু হতে হবে।
- **অটো-ফিল:** `get_my_nominees(kind)` — ঐ ধরনের হিসাবে নমিনি না থাকলে **আগের যেকোনো হিসাবের সর্বশেষ সেট** ফেরত দেয় (`inherited:true`) এবং ফর্মে বসে যায় সবুজ নোটিশসহ; সব ঘর সম্পূর্ণ পরিবর্তনযোগ্য। উত্তরাধিকার `updated_at desc` দেখে বাছে — তাই `nominees_touch` ট্রিগার আবশ্যক।
- **টেবিল:** `nominees(user_id, account_kind, …, share, sort_order)`। RLS: SELECT কেবল নিজের বা `is_admin()`; anon/authenticated-এর কোনো INSERT/UPDATE/DELETE নেই — সব লেখা `save_my_nominees` (SECURITY DEFINER) দিয়ে, যা পুরো সেট মুছে নতুন করে লেখে। `account_kind` রাখা আছে যাতে পরে ঋণের নমিনি যোগ করা যায়।
- ⚠️ **`revoke execute ... from anon` কাজ করে না** — ফাংশনে EXECUTE ডিফল্টভাবে `PUBLIC`-এর, তাই `revoke ... from public` করে তারপর `grant ... to authenticated, service_role` দিতে হয়। (নমিনির তিন ফাংশনে এভাবেই করা হয়েছে।)
- **UI কোড container-সচেতন:** একই ফাংশনগুলো দুই জায়গায় চলে — হিসাব খোলার ফর্ম (`#nomList`) ও হিসাব প্যানেলের সম্পাদনা (`#accNomList`)। তাই `addNominee(data,{wrap})`, `collectNominees(w)`, `nomSyncSum(w)` — সবেতে wrap আইডি যায়, আর পাশের উপাদান খোঁজা হয় `[data-nom="sum"][data-for="<wrap>"]` দিয়ে। নতুন কোথাও বসালে এই দুটি data-অ্যাট্রিবিউট দিতে হবে।
- **থীম:** ফর্মের কার্ড (`.kh-slider`) ও `.section-card` — দুটোই দুই থীমে সাদা, তাই নমিনির সব রঙ গাঢ় লেখা/হালকা পটভূমি। ⚠️ কার্ডের `#faf7fd` পটভূমিতে ডিফল্ট লেবেল রঙ `--kh-mute` (#7b7288) মাত্র ৪.৩:১ দেয় — তাই `.nom-card .kh-field label{color:#6d6379}` (৪.৫২:১)।

## 🏦 একাধিক আমানত হিসাব (সিদ্ধান্ত ও বাস্তবায়ন: ১০–১১ সেপ্টেম্বর ২০২৬)

- **⚠️ আগে এক সদস্য = এক হিসাব** — `depositors`-এর PK-ই ছিল ইউজার আইডি। তাই "যখন খুশি" হিসাব থাকলে মাসিক হিসাব আর খোলা যেত না। এখন **`savings_accounts`** — প্রতিটি হিসাব আলাদা সারি, নিজের নম্বরসহ। `depositors` মুছিনি (পুরনো কোড ও রিমাইন্ডার যেন না ভাঙে), কিন্তু **নতুন কোডে সবসময় `savings_accounts` ব্যবহার করতে হবে**।
- **হিসাব নম্বর:** `KH-S-0001` ক্রমিক — `savings_account_seq` + `next_savings_account_no()`। পুরো ফাউন্ডেশনে একটি চলমান ক্রম।
- **নিয়ম (ব্যবহারকারীর সিদ্ধান্ত):** ① কিস্তির অঙ্ক **লক** — জমার সময় বদলানো যায় না; ② সিস্টেম নিজেই **সবচেয়ে পুরনো বকেয়া মাস** ধরে; ③ এক জমা = **এক মাসের** কিস্তি, বকেয়া বাদ দিয়ে পরের মাস দেওয়া যায় না; ④ **প্রতি হিসাবে আলাদা নমিনি**।
- **RPC:** `my_savings_accounts()` · `savings_due_status(account)` · `open_savings_account(scheme, instalment, …, nominees)` · `deposit_to_savings(account, ref, notes, amount)` · `save_account_nominees(account, list)` / `get_account_nominees(account)`।
  ⚠️ জমা **কখনো সরাসরি `insert` নয়** — `deposit_to_savings` অঙ্ক ও মাস নিজে ঠিক করে, `p_amount` কেবল "যখন খুশি" স্কীমে কাজে লাগে।
- **টেবিল:** `savings_transactions`-এ নতুন `account_id` ও `period_month`; ইউনিক ইনডেক্স `(account_id, period_month)` — এক মাসের কিস্তি দুবার জমা হতে পারে না (`rejected` বাদে)। `nominees`-এ `account_id`।
- **পুরনো ডেটা:** ৩টি `depositors` সারি → `KH-S-0001..0003`, সব লেনদেন ও নমিনি জুড়ে দেওয়া হয়েছে (অনাথ শূন্য)।
- **সাইন-আপও হিসাব খোলে:** `create_depositor` এখন প্রথম `savings_accounts` সারিটিও বানায় — নাহলে নতুন সদস্য পোর্টালে ঢুকে "কোনো হিসাব নেই" দেখতেন।
- **পোর্টালের পর্দা:** উপরে **হিসাব নির্বাচনের বার** (ধরন + নম্বর + স্থিতি + বকেয়ার চিপ) — জমা, উত্তোলন, ইতিহাস ও স্টেটমেন্ট সবই নির্বাচিত হিসাবের। জমার পর্দায় হিসাব নিশ্চিত হয়, কিস্তির অঙ্ক লক দেখায় ও কোন মাসের কিস্তি তা লেখা থাকে। নতুন হিসাব খোলার আলাদা প্যানেল (`panel-newaccount`), নমিনি আগের হিসাব থেকে অটো-ফিল। নির্বাচিত হিসাব `localStorage.kh_acc`-এ মনে রাখা হয়।
- **যাচাই (সব SQL পরীক্ষা পাস):** "যখন খুশি" হিসাব থাকা সত্ত্বেও মাসিক হিসাব খোলা গেছে, এবং **একই ধরনের দ্বিতীয় হিসাবও** (KH-S-0005, KH-S-0006); দুই হিসাবের নমিনি আলাদা থেকেছে (মিতালী / রাহাত); ৳৫৫,৫৫৫ চেয়েও বসেছে নিজ নিজ লক অঙ্ক (৳১,২০০ ও ৳৩০০); লেনদেন নিজ হিসাবেই থেকেছে। বকেয়ার পরীক্ষায়: ২ মাস বকেয়া ধরা পড়েছে, জুলাই→আগস্ট→সেপ্টেম্বর ক্রমে জমা হয়েছে, সব কিস্তি শেষে এগিয়ে দেওয়া আটকেছে, অন্যের হিসাবে জমাও আটকেছে।
- **ড্যাশবোর্ড এখন সমন্বিত, একটি হিসাবের নয় (১১ সেপ্টেম্বর ২০২৬)।** `my_savings_summary()` RPC — ধরনভিত্তিক হিসাব-সংখ্যা ও জমা/উত্তোলন/স্থিতি, সব মিলিয়ে সর্বমোট, অনুমোদনের অপেক্ষায় থাকা অঙ্ক, আর **কোন হিসাবে কত কিস্তি বাকি ও মোট কত প্রদেয়**। উপরের তিনটি কার্ড ও সাইডবারের অঙ্ক এখন **সব হিসাবের যোগফল** — `paintAccount()` থেকে ঐ তিনটি আর লেখা হয় না (নাহলে নির্বাচিত হিসাবের অঙ্ক বসে যেত)। হিসাব-নির্বাচনের বার ড্যাশবোর্ড ও নতুন-হিসাবের পর্দায় লুকানো থাকে।
  যাচাই: ধরনভিত্তিক স্থিতির যোগফল সর্বমোটের সমান (৳৫,০০০), ৩ ধরনের ৩টি হিসাব, বকেয়া তালিকায় `KH-S-0008` (দৈনিক) — বাকি ১ কিস্তি, প্রদেয় ৳১০০।
- **⚠️ মাসের নাম UI-তে লেখা হয় না (সিদ্ধান্ত: ১১ সেপ্টেম্বর ২০২৬)।** জমার পর্দায় "সেপ্টেম্বর ২০২৬-এর কিস্তি" লেখা থাকত — **দৈনিক হিসাবে সেটি অর্থহীন ঠেকে**। তাই জমার পর্দা, হিসাব-বারের চিপ, সাফল্যের বার্তা ও ভাউচার — কোথাও মাসের নাম নেই; কেবল হিসাব নম্বর ও লক করা অঙ্ক দেখানো হয়। `period_month` আগের মতোই ডাটাবেজে থাকে ও বকেয়ার হিসাব তাতেই চলে; বকেয়ার বিস্তারিত এখন **ড্যাশবোর্ডে**।
- **⚠️ মাইগ্রেশনের শেষে `auth.uid()`-নির্ভর `select` দেওয়া যাবে না** — service_role-এ uid থাকে না, ফাংশন `not_authenticated` ছুড়ে দেয় এবং **পুরো মাইগ্রেশন রোলব্যাক** হয়ে যায় (ফাংশনটিও তৈরি হয় না)। পরীক্ষা আলাদা `execute_sql`-এ `set_config('request.jwt.claims', …)` দিয়ে করতে হবে।
## ✅ সঞ্চয় অনুমোদন ও নিশ্চিতকরণ মেইল (১১ সেপ্টেম্বর ২০২৬)

- **⚠️ অনুমোদনের বাটন খুঁজে পাওয়া যাচ্ছিল না — আসল কারণ।** `dashboard.html`-এর `loadSavingsApprovals()` তালিকায় `.eq('type','withdrawal')` ফিল্টার ছিল, অর্থাৎ **অপেক্ষমাণ জমা কখনো দেখানোই হত না**। ড্যাশবোর্ডের ব্যাজ "অনুমোদনের অপেক্ষায় ৮" বলত, কিন্তু প্যানেল ফাঁকা — কারণ ডাটাবেজে ছিল `pending_deposits: 8, pending_withdrawals: 0`।
  **সমাধান:** এখন জমা ও উত্তোলন দুটোই আসে (`status='pending'`), সাথে হিসাব নম্বর/স্কীম/রেফারেন্স দেখানো হয়, জমা সবুজ `+৳` ও উত্তোলন লাল `−৳`, আর **সব / জমা / উত্তোলন** ফিল্টার পিল আছে। ব্যাজের গণনা থেকেও উত্তোলন-ফিল্টার সরানো হয়েছে।
  **⚠️ মেনুর নাম বিভ্রান্তিকর ছিল।** সাইডবারে প্যানেলটির নাম ছিল "সঞ্চয় উত্তোলন" — তাই জমার অনুমোদন যে ওখানেই তা বোঝার উপায় ছিল না (ব্যবহারকারী নিজে খুঁজে না পেয়ে জানিয়েছেন)। এখন সাইডবার, হেডার-শিরোনাম, প্যানেলের শিরোনাম ও অ্যাডমিন পারমিশনের লেবেল — **চারটিই "সঞ্চয় জমা ও উত্তোলন অনুমোদন"** (`savings_approve` কি অপরিবর্তিত)। **নিয়ম:** মেনু, হেডার-ম্যাপ (`PANEL_TITLES`), প্যানেল-শিরোনাম ও পারমিশন-লেবেল — একই জিনিসের নাম চার জায়গায় আছে, একটি বদলালে চারটিই বদলাতে হবে।
  **নিয়ম:** অনুমোদনের কোনো তালিকায় `type` দিয়ে ফিল্টার বসানোর আগে ডাটাবেজে গুনে দেখতে হবে কোন ধরনের রেকর্ড আসলে জমে আছে।
- **নিশ্চিতকরণ মেইল — নতুন kind `savings_approved`।** অ্যাডমিন অনুমোদন দিলে সদস্য আলাদা একটি মেইল পান: শিরোনাম "জমা অনুমোদিত হয়েছে" (উত্তোলনে "উত্তোলন অনুমোদিত হয়েছে"), ভাউচার নং `KH-SDA-…` / `KH-SWA-…`, হিসাব নম্বর, স্কীম, লক করা কিস্তি ও **হিসাবের বর্তমান স্থিতি** (শুধু `approved` লেনদেন যোগ-বিয়োগ করে)।
  • `pending_voucher_mails`-এ নতুন শাখা, সময় ধরা হয় `coalesce(approved_at, processed_at, created_at)`; **এই শাখায় grace নেই** — কারণ অনুমোদনের মেইলে ব্রাউজারের কোনো কপি আসার কথাই নয়, তাই অপেক্ষা করার মানে হয় না। অনুমোদনের কয়েক সেকেন্ডের মধ্যেই যায়।
  • ট্রিগার `savings_stamp_approved` (BEFORE UPDATE OF status) `approved_at` বসায়; `trg_kick_mail_savings` (AFTER, statement-level) সাথে সাথে `run_voucher_mailer()` ডাকে।
  • `voucher.ts`-এ `savings` ও `savings_approved` একই শাখায়; `savings_approved` হলে `status='approved'` না হলে `not approved yet` দিয়ে ফিরে যায়।
  • ব্যবস্থা চালুর আগের সব অনুমোদিত রেকর্ড `voucher_mail_log`-এ `skipped` হিসেবে সিড করা — পুরনো লেনদেনে হঠাৎ মেইলের ঢল নামবে না।
  • যাচাই: `KH-SDA-6246A2` (৳১০,০০০ জমা) সত্যিকারের মেইল হিসেবে গেছে — pg_net-এর উত্তর `{"ok":true,"sent_to":"t****@gmail.com"}`।
- **📱 SMS এখনো নেই।** `reminders.channel`-এ `'sms'` রাখার জায়গা আছে, কিন্তু কোনো গেটওয়ে যুক্ত নয়। যোগ করতে হলে সিদ্ধান্ত লাগবে — WhatsApp Cloud API (ফ্রি টেমপ্লেট বার্তা) নাকি দেশীয় SMS প্রোভাইডার (Bulk SMS BD / SSL Wireless — প্রতি বার্তায় খরচ)।

## 💰 আমানত পোর্টাল (savings-portal.html)

- লগইন থাকলে পেজ লোডেই `loadPortal(session.user)` চলে (`depositors.id = auth uid`), তাই দ্বিতীয়বার লগইন লাগে না।
- `openPanelFromHash()` — `savings-portal.html#deposit` / `#history` / `#withdraw` / `#account` দিয়ে সরাসরি সেই প্যানেল খোলে; `my-dashboard.html`-এর আমানত কার্ডে "টাকা জমা করুন" ও "স্টেটমেন্ট" বাটন এই লিংক ব্যবহার করে।
- **স্টেটমেন্ট:** ইতিহাস প্যানেলের `printStatement()` — চলমান স্থিতিসহ পূর্ণ বিবরণ, নতুন উইন্ডোতে HTML (ব্রাউজারের "Save as PDF")। শুধু `approved` লেনদেন স্থিতিতে ধরা হয়; `pending` জমা আলাদা দেখানো হয়।

## 📱 মোবাইল রেসপনসিভ (kh-ui.css সেকশন ১১.৯৫)

- **টপবার মোবাইলে লুকানো থাকে** (`.topbar{display:none}`), তাই লগইন/প্রোফাইল হ্যামবার্গার মেনুতে (`.nav-links`) বসে — `KHUI._addNavMenuItems()` (লগইন থাকলে) ও `KHUI._addNavLoginItem()` (না থাকলে)।
- **গ্লাস নেভে `a span{display:none}` ছিল** — এতে প্রোফাইল ছবির রিংও লুকিয়ে যেত; এখন `span:not(.kh-gav-ring)`।
- সব বহু-কলাম গ্রিডের নাম kh-ui.css-এর ৮২০px/৬৪০px ব্লকে তালিকাভুক্ত (নতুন গ্রিড ক্লাস বানালে ওখানে যোগ করতে হবে)।
- **চওড়া টেবিল** `KHUI.makeTablesScrollable()` নিজে থেকেই `.kh-scrollx`-এ মুড়ে দেয় (MutationObserver দিয়ে নতুন টেবিলও ধরা পড়ে) — CSS-এ parent selector নেই বলে JS লাগে।
- ইনপুটে `font-size:16px` (iOS জুম ঠেকাতে), বাটনে `min-height:44px`, `body{padding-bottom:78px}` (গ্লাস নেভের জন্য)।

### 🗂️ অ্যাডমিন সাইডবার = মোবাইলে ড্রয়ার (১৪ সেপ্টেম্বর ২০২৬)

- **⚠️ আগে সাইডবারটি মোবাইলে উপরে সরে আসত** — `position:static`, পুরো প্রস্থে, আর `.sb-nav` দুই-কলামে `flex-wrap`। **২০টি মেনু আইটেম + ৭টি বিভাগ-শিরোনাম + ফুটার** মিলে পর্দার প্রায় পুরোটাই খেয়ে ফেলত, আসল কাজের অংশ অনেক নিচে নেমে যেত (ব্যবহারকারীর পর্যবেক্ষণ)।
- **এখন বাঁ দিক থেকে স্লাইড করে আসা ড্রয়ার।** টপবারের বাঁ কোণে হ্যামবার্গার (`.kh-sb-toggle`, `#sbToggle`) → ড্রয়ার খোলে; ব্যাকড্রপে ক্লিক, `Esc`, অথবা যেকোনো মেনু বাছলে বন্ধ হয়। খোলা অবস্থায় আইকন `ti-menu-2` → `ti-x`, `aria-expanded` ও `aria-label`ও বদলায়।
- **CSS কোথায়:** `kh-ui.css`-এর **৮২০px ব্লকে** (`#sidebar` → `position:fixed`, `86vw`/সর্বোচ্চ `320px`, `translateX(-100%)`, `.kh-sb-open`-এ `0`)। `z-index: 9500` — গ্লাস নেভ (৯০০০) ও টপবার (৫০০)-এর উপরে; ব্যাকড্রপ `.kh-sb-backdrop` ৯৪৯০। বাটন ও ব্যাকড্রপের মার্কআপ + JS আছে `dashboard.html`-এ।
- **⚠️ পুরো ড্রয়ারটাই স্ক্রল করে, `.sb-nav` আলাদা করে নয়** — ডেস্কটপে `.sb-nav{flex:1;overflow-y:auto}`, সেটি মোবাইলে রাখলে মাথা ও ফুটার আটকে থেকে কেবল মাঝের অংশটুকু স্ক্রল করত। তাই মোবাইলে `.sb-nav{display:block;flex:none;overflow:visible}` আর `#sidebar{overflow-y:auto}`। **ব্যবহারকারীর শর্ত ছিল "কোনো মেনু বা অপশন যেন বাদ না যায়" — এটিই তা নিশ্চিত করে।**
- **⚠️ `switchPanel()`-এর একদম শুরুতে `closeAdminDrawer()`** — প্রতিটি মেনু আইটেম ওটিই ডাকে, তাই একটিমাত্র জায়গায় বসালেই সব পথ ধরা পড়ে। ফুটারের "প্রধান ওয়েবসাইট" ও "লগআউট" `switchPanel()` ডাকে না, তাই তাদের আলাদা লিসেনার আছে।
- **⚠️ বিভাগের শিরোনামের রঙ বদলাতে হয়েছে** — `.sb-section` ছিল `rgba(255,255,255,.25)`, সাইডবারের গাঢ় সবুজে মেপে মাত্র **২.২৫:১**। ডেস্কটপে সাইডবার সবসময় চোখের সামনে থাকে বলে চলে যেত, কিন্তু ড্রয়ার হওয়ার পর এটিই মোবাইলের **একমাত্র** মেনু — বিভাগের নাম না পড়তে পারলে খুঁজে পাওয়া কঠিন। এখন ভরাট `#9fb3a8` — গ্র্যাডিয়েন্টের দুই প্রান্তেই **৭.৫–৭.৮:১**, অথচ সাদা আইটেমের (১৭.২:১) চেয়ে স্পষ্ট হালকা, তাই স্তরবিন্যাস বজায় থাকে।
- **যাচাই (৩৭৫×৮১২ ভিউপোর্ট এমুলেশনে, লাইভ পেজে নতুন আচরণ বসিয়ে):** ২০টি মেনু আইটেম + ৭টি শিরোনাম + ২টি ফুটার আইটেম — **প্রতিটিতে স্ক্রল করে পৌঁছানো গেছে, একটিও বাদ পড়েনি** (`unreachable: []`); ড্রয়ার `scrollHeight 1394 > clientHeight 812`, শেষ আইটেম "লগআউট" পর্যন্ত পৌঁছায়; বন্ধ অবস্থায় `left:-320`, খোলা অবস্থায় `left:0`; ব্যাকড্রপ-ক্লিক, Esc ও আইটেম-ক্লিক — তিন পথেই বন্ধ হয় ও প্যানেল বদলায়; আড়াআড়ি স্ক্রল নেই।
  **ডেস্কটপে কিছুই বদলায়নি** — ব্রেকপয়েন্ট সরিয়ে পরীক্ষা করে দেখা গেছে সাইডবার আবার `position:sticky`, ২৬০px, `#mainWrap` তার পাশে (`left:260`), হ্যামবার্গার ও ব্যাকড্রপ `display:none`। **`.kh-sb-open` ক্লাসটি ভুলে থেকে গেলেও ডেস্কটপে কোনো প্রভাব ফেলে না**, কারণ ঐ নিয়মগুলো কেবল ৮২০px-এর নিচে।
- **🛡️ মোবাইলে অ্যাডমিন লগইন (১৩ সেপ্টেম্বর ২০২৬)।** "Admin" বাটনটি আছে `.topbar`-এ, আর মোবাইলে `.topbar { display:none }` — তাই **মোবাইল থেকে অ্যাডমিন লগইনের কোনো পথই ছিল না** (ব্যবহারকারীর পর্যবেক্ষণ)। এখন `KHUI._addNavAdminItem(signedIn)` হ্যামবার্গার মেনুতে (`.nav-links`) আইটেম বসায় — `_addNavMenuItems()` ও `_addNavLoginItem()` **দুটোর শেষেই** ডাকা হয়, কারণ একজন অ্যাডমিন সদস্য হিসেবেও লগইন থাকতে পারেন। `admin-login.html`-এ নিজে থেকেই বসে না; বন্ধ করতে `<body data-kh-admin-link="off">`।
  ⚠️ **ডেস্কটপে লুকানো** (`display:none`, ৮২০px-এ খোলে) — ওখানে টপবারের Admin বাটনটি এমনিতেই আছে, দুটো থাকলে একই জিনিস দুবার দেখাত। `.kh-navme` `<div>` বলে ডেস্কটপে নিজে থেকেই লুকায়, কিন্তু এটি `<a>` — তাই স্পষ্ট করে লিখতে হয়েছে।
  ⚠️ **বাটনটি নিজের ভরাট পটভূমি বহন করে।** প্রথমে স্বচ্ছ `rgba(240,140,40,.16)` + হালকা কমলা লেখা ছিল; মেনুর পটভূমি থীমভেদে গাঢ় বা হালকা হওয়ায় হালকা পর্দায় কনট্রাস্ট **১.১৬:১**-এ নেমে যাচ্ছিল (ব্রাউজারে মেপে ধরা পড়ে)। **নিয়ম: মেনুর ভেতরের বাটনে স্বচ্ছ পটভূমি দেওয়া যাবে না — পটভূমি থীমভেদে বদলায়।**

### 🛡️ দুটি পথ, আর কোনো জ্বলজ্বলে বাটন নয় (১৪ সেপ্টেম্বর ২০২৬)

ব্যবহারকারী: *"খুব হাইলাইট করার দরকার নেই, ছোট করে কোথাও রাখলেই হবে। অথবা এডমিনগণের ইউজার প্রোফাইল দিয়ে লগইন করে তারপর এডমিন একাউন্টের এক্সেস নেয়ার ব্যবস্থাও রাখা যেতে পারে।"* — **দুটোই করা হয়েছে।**

- **লগ-আউট অবস্থায়** — মেনুর শেষে একটি **ছোট, শান্ত পিল** (`.kh-navadmin`) → `admin-login.html`। আগের পূর্ণ-প্রস্থের অ্যাম্বার গ্র্যাডিয়েন্ট বাটনটি বাদ; এখন **১২৯×৩৬px**, ফন্ট ১২.৫px, ভরাট স্লেট `#3b3450` + `#e8e3f2` লেখা — **৯.৩১:১**, দুই থীমেই এক (নিজের পটভূমি বহন করে, তাই উপরের নিয়মটিও মানা হয়)।
- **লগইন অবস্থায় ও তিনি সত্যিই অ্যাডমিন হলে** — সদস্যের কার্ডের ভেতরে, "সাইন আউট"-এর ঠিক উপরে **"🛡 অ্যাডমিন প্যানেল"** (`.kh-navadmin-in`) → সরাসরি `dashboard.html`। **আলাদা করে অ্যাডমিন লগইন করতে হয় না** — `initAdmin()` কেবল `get_my_admin_info()` দেখে, একই সেশনেই চলে। রঙ `.kh-navme a`-র হুবহু (দুই থীমেই আগে থেকে মাপা), তাই আলাদা করে চোখে লাগে না।
- **সাধারণ সদস্য কিছুই দেখেন না** — মেনু অকারণে ভরে না।
- **⚠️ যাচাই একই ফাংশনে যেটি ড্যাশবোর্ড নিজে ব্যবহার করে** — `get_my_admin_info()` (SECURITY DEFINER, কেবল `auth.uid()`-এর নিজের সারি, `authenticated`-কে EXECUTE দেওয়া, অ্যাডমিন না হলে ০ সারি)। `is_admin()` ব্যবহার করা হয়নি **ইচ্ছে করেই**: সেটি `admin_users`+`admin_roles` দুটোই দেখে, কিন্তু ড্যাশবোর্ড গেট করে কেবল `admin_roles` দিয়ে — তাই `is_admin()` দিলে "লিংক দেখাল অথচ ঢুকতে দিল না" হতে পারত।
- **⚠️ `rpc()`-এ `.catch()` নেই** (প্রকল্পের ৪ নম্বর নিয়ম) — তাই `khAdminPanelItem()` একটি `async` ফাংশন, ভেতরে `try/catch`। ব্যর্থ হলে বা permission denied হলে চুপচাপ কিছুই বসে না।
- **⚠️⚠️ `dashboard.html` সদস্যকে লগ-আউট করে দিত — সংশোধন করা হয়েছে।** `initAdmin()`-এ ছিল `if (!info.length) { await _db.auth.signOut(); showAccessDenied(); }` — অর্থাৎ কোনো সাধারণ সদস্য কৌতূহলে এই পেজে এলে **তাঁর নিজের সেশনটিও মুছে যেত**, সঞ্চয় পোর্টাল থেকেও বেরিয়ে যেতেন। মেনুতে অ্যাডমিন-পথ যোগ করার আগে এটি ঠিক করা জরুরি ছিল। এখন `showAccessDenied(true)` — সেশন অটুট থাকে, বার্তা হয় "আপনি লগইন করা আছেন, তবে এই অ্যাকাউন্টটি অ্যাডমিন হিসেবে অনুমোদিত নয়", আর বাটনটি "আমার ড্যাশবোর্ডে যান" (`my-dashboard.html`)। **নিয়ম: অনুমতি না থাকা আর লগ-আউট করে দেওয়া এক জিনিস নয়।**
- **যাচাই (ব্রাউজারে, ছয়টি অবস্থায়):** লগ-আউট → `অ্যাডমিন লগইন → admin-login.html`; লগইন+অ্যাডমিন → `অ্যাডমিন প্যানেল → dashboard.html`, সাইন-আউটের ঠিক উপরে; লগইন+সাধারণ সদস্য → কিছুই নয়; RPC permission-denied → কিছুই নয়; RPC throw → কিছুই নয়; দুবার ডাকলে ডুপ্লিকেট হয় না। কনট্রাস্ট ডার্ক ও লাইট দুই থীমেই মাপা।
- **📱 হোম পেজে মোবাইলের লগইন বাটন (১১ সেপ্টেম্বর ২০২৬)।** মোবাইলে `.topbar` ও `.nav-cta` দুটোই `display:none` — তাই লগইনের একমাত্র পথ ছিল হ্যামবার্গার মেনু, যা নতুন দর্শক খুঁজে পান না। `index.html`-এর হিরোতে এখন `.kh-mauth` বাটন (`#khMobileAuth`) — **শুধু ৭৬৮px-এর নিচে দেখা যায়**, উপরে `display:none`। লগইন থাকলে পেজের শেষের ছোট স্ক্রিপ্ট (`_db.auth.getSession()`, try/catch-এ মোড়া) লিংক ও লেখা বদলে "আমার ড্যাশবোর্ড" করে দেয়। রঙ: গ্র্যাডিয়েন্ট `#c2255c → #8b1fb0 → #c0307a` সাদা লেখায় — কনট্রাস্ট ৫.৩:১ থেকে ৭.২:১, দুই থীমেই এক (বাটন নিজের পটভূমি বহন করে)। ⚠️ ব্র্যান্ডের ডিফল্ট `#e83e8c` সাদা লেখায় মাত্র ৩.৮:১ দেয় — তাই গাঢ় ভ্যারিয়েন্ট ব্যবহার করা হয়েছে।

## ☎️ কাস্টমার কেয়ার স্ট্রিপ (`.cc-strip` · index.html · ১৩ সেপ্টেম্বর ২০২৬)

- **⚠️ "Call us" বাটনটি দুই থীমেই অদৃশ্য ছিল — আসল কারণ দুটি ফাইলের সংঘর্ষ।** `index.html`-এ `.call-btn` ছিল `background: transparent` + `color: var(--green)` (আউটলাইন বাটন), কিন্তু `kh-ui.css`-এর ৯৩৭ নম্বর লাইনে "রঙিন ফিলের উপর বসা বাটন" তালিকায় `.call-btn`ও আছে — `color: #fff !important`। ফলে ক্রিম `#fff6ef` স্ট্রিপে **সাদা লেখা**, কনট্রাস্ট মেপে পাওয়া গেছে **১.০৭:১** (লাইভ পেজে যাচাই)। থীম বদলালেও কিছু হত না, কারণ দুটোর কোনোটাই থীম-নির্ভর নয়।
  **সমাধান:** বাটনটি এখন নিজের গ্র্যাডিয়েন্ট বহন করে — `linear-gradient(112deg,#c2255c,#8b1fb0 55%,#7048e8)`, সাদা লেখা, বর্ডার নেই। কনট্রাস্ট **৫.৫৫ – ৭.১৯:১**, দুই থীমে হুবহু এক (প্রকল্পের ২ নম্বর নিয়ম)।
  **নিয়ম: `kh-ui.css`-এর ঐ `color:#fff !important` তালিকায় কোনো ক্লাস থাকলে সেটিকে কখনো স্বচ্ছ/আউটলাইন বাটন বানানো যাবে না — তালিকাটি ধরেই নেয় বাটনটি রঙিন পটভূমি বহন করে।** নতুন বাটন যোগ করার আগে ঐ তালিকায় নামটি আছে কি না দেখে নিতে হবে।
- **মোবাইল সাজ ছিলই না।** স্ট্রিপটি `display:flex; justify-content:space-between; padding:20px 40px` — কোনো মিডিয়া কোয়েরি ছিল না, তাই ছোট পর্দায় বাঁয়ের দুই লাইনের লম্বা লেখা আর ডানের দুটি বাটন একে অপরকে চেপে ধরত। এখন —
  • **≤৮৬০px:** স্ট্রিপ ও `.cc-left` খাড়া, লেখা মাঝবরাবর, প্যাডিং `24px 20px`, বাটন দুটি `flex:1` ও `min-height:46px`, `.cc-actions` সর্বোচ্চ ৪৩০px
  • **≤৪৩০px:** বাটন দুটিও খাড়া (একটির নিচে আরেকটি)
  যাচাই (৩৯০px iframe-এ লাইভ পেজ): স্ট্রিপ `column`, দুটি বাটনই ৩৩৫×৪৬px পূর্ণ প্রস্থে, **আড়াআড়ি স্ক্রল নেই**; শিরোনাম ১২.১৫:১, উপশিরোনাম ৬.৪৬:১।

## 🌓 নেভবার সব পেজে · আলাদা থীম বাটন বাতিল (kh-ui · ১৩ সেপ্টেম্বর ২০২৬)

- **⚠️ ভাসমান থীম বাটনটি অ্যাডমিন ড্যাশবোর্ডের "লাইভ" ব্যাজ ঢেকে দিত** (ব্যবহারকারীর পর্যবেক্ষণ)। `.kh-theme-float` বসত `top:18px; right:18px` — লাইভ পেজে মেপে দেখা গেছে বাটন `x 1605–1647, y 18–60` আর `.tb-live` `x 1582–1641, y 14–43`, অর্থাৎ প্রায় পুরোটাই ঢাকা।
- **⚠️ সিদ্ধান্ত (ব্যবহারকারী): সব পেজেই নেভবার থাকবে, আলাদা থীম বাটন থাকবে না।** থীম টগলটি গ্লাস নেভবারের ভেতরেই আছে (`mountNav()`-এর `.kh-glassnav-top.kh-theme-btn`), তাই নেভবার থাকলে দ্বিতীয় বাটনের কোনো দরকার নেই।
  • **`data-kh-nav="off"` → `"on"`** করা হয়েছে ছয়টি পেজে: `admin-login` · `user-login` · `reset-password` · `wallet` · `dashboard` · `admin`। এখন **১৮টি পেজের সবগুলোতেই** নেভবার আছে।
  • **`KHUI.mountThemeButton()`, `.kh-theme-float` ও `.kh-theme-inline` সম্পূর্ণ মুছে ফেলা হয়েছে**, সাথে `boot()`-এর `else if (… khTheme …)` শাখাটিও। `data-kh-theme` অ্যাট্রিবিউটটির আর কোনো কাজ নেই।
  • **⚠️ নিয়ম: নতুন কোনো পেজে `data-kh-nav="off"` দেওয়া যাবে না** — দিলে ঐ পেজে থীম বদলানোর কোনো পথই থাকবে না।
- **⚠️ অ্যাডমিন প্যানেলে চ্যাট উইজেট বন্ধ** — `mountNav()` চললে `mountChat()`ও চলে, আর দর্শকদের কাস্টমার-কেয়ার চ্যাট অ্যাডমিনের পর্দায় অর্থহীন। তাই `dashboard.html` ও `admin.html`-এ `data-kh-chat="off"` (ব্যবহারকারীর সিদ্ধান্ত)। লগইন পেজে চ্যাট চালুই থাকে।
- **যাচাই (লাইভ পেজে নতুন আচরণ সিমুলেট করে, ছয়টি পেজেই):** নেভবার বসে (`হোম · দান · ঋণ · সঞ্চয় · লগইন`), ভেতরের থীম বাটনে ক্লিকে `dark → light → dark`, কোনো ভাসমান বাটন অবশিষ্ট নেই, লিংকের কনট্রাস্ট ৭.৭৩:১, ৩৯০px-এ নেভবার ভিউপোর্টের ভেতরেই (আড়াআড়ি স্ক্রল নেই), আর `.tb-live` ব্যাজ নিজের জায়গায় অটুট।
  ⚠️ **পরীক্ষার সময় একটি ফাঁদ:** ব্যাকগ্রাউন্ড ট্যাবে (`document.hidden`) `requestAnimationFrame` ও CSS ট্রানজিশন চলে না, তাই নেভবার `.kh-hidden` (`translateY(140%)`) অবস্থাতেই আটকে থাকে ও মাপে ভিউপোর্টের ৫৬px নিচে দেখায়। `transition:none` দিয়ে মাপলে ঠিক `bottom:24px` আসে — **এটি বাগ নয়, পরীক্ষার artifact**।

## 🖼️ হোম পেজের ফটো ক্যারোসেল (index.html · ১১ সেপ্টেম্বর ২০২৬)

- **ছবিগুলো এখন নিজস্ব** — আগে Unsplash-এর লিংক ছিল, ছবির সাথে ঋণের নামের কোনো মিল ছিল না। এখন `images/kh-*.jpg` — Gemini দিয়ে তৈরি, বাংলাদেশি গ্রামীণ ও ইসলামিক প্রেক্ষাপটে, প্রতিটি ছবি দেখেই বোঝা যায় কোন সেবা। প্রম্পটগুলো `ফটোকার্ড_প্রম্পট_Gemini.md`-এ রাখা আছে — **নতুন ছবি বানাতে হলে ঐ ফাইলের STYLE BLOCK ও NEGATIVE LIST হুবহু ব্যবহার করতে হবে**, নাহলে সেটি বাকিগুলোর সাথে মিলবে না।
- **১১টি কার্ড:** `kh-qard` (প্রথমে, ব্র্যান্ড কার্ড) → `kh-agri` · `kh-business` · `kh-education` · `kh-medical` · `kh-family` · `kh-emergency` → `kh-save-daily` · `kh-save-fortnightly` · `kh-save-monthly` · `kh-save-flexible`।
- **ছবির নিয়ম:** ২:৩ অনুপাত, নিচের এক-তৃতীয়াংশ শান্ত ও গাঢ় (ওখানে `.khc-label` বসে), ছবির ভেতরে **কোনো লেখা নয়** — AI-এর বসানো বাংলা লেখা ভেঙে আসে, নামটা CSS-ই বসায়।
- **⚠️ `shift()`-এর পুরনো কৌশল ঠিক ৭টি কার্ডের জন্য লেখা ছিল।** "যে কার্ডটি ঘুরে আসে তাকে লাফ দিয়ে সরানো" (`jumpIdx = prev + dir*3`) — ১১টি কার্ডে দৃশ্যমান জানালার (±৩) ভেতরে কিছুই ঘুরে আসে না, তাই ঐ কৌশল একটি কার্ডকে বাঁ কিনারায় আটকে রাখত ও ডানে একটি ঘর ফাঁকা পড়ে থাকত। এখন `shift()` শুধু `active` বদলে `render(true)` ডাকে।
- **⚠️ `applyPos()`-এ পর্দার বাইরের কার্ড শুধু `opacity:0` করলে হয় না** — পুরনো `transform` থেকে যায়, ফলে ফিরে আসার সময় কার্ডটি পুরো মঞ্চ পেরিয়ে উড়ে আসে। এখন যে কিনারা দিয়ে ঢুকবে (`rel>0` হলে ডান, নাহলে বাঁ) সেখানেই আগেভাগে বসিয়ে রাখা হয়, `transition` শুধু `opacity`-তে। **কার্ডের সংখ্যা বাড়ালে এই দুটি জায়গা আবার মিলিয়ে দেখতে হবে।**
- **⚠️ `kh-save-flexible.jpg` বাতিল (১১ সেপ্টেম্বর ২০২৬)** — ১১টির মধ্যে ওটিই একমাত্র ছবি ছিল যাতে কোনো মানুষ নেই (শুধু থলি, মুদ্রা ও একটি হাত), তাই বাকিগুলোর পাশে বেমানান লাগত। "যখন খুশি আমানত" কার্ডে **সাময়িকভাবে `kh-save-daily.jpg`** বসানো — অর্থাৎ ঐ ছবিটি এখন ক্যারোসেলে দুবার আসে। নতুন ছবির প্রম্পট `ফটোকার্ড_প্রম্পট_Gemini.md`-এর "বদলি প্রম্পট" অংশে; ছবি পেলে শুধু `ITEMS`-এর ঐ সারিটি বদলালেই হবে।
- **ছবি বড় করে দেখা (`.khc-lb`):** মাঝের কার্ডে ট্যাপ করলে পুরো পর্দায় ছবিটি খোলে — তীর, সোয়াইপ, Esc, ব্যাকড্রপে ক্লিক, আর `১ / ১১` গণনা। মোবাইলের জন্যই মূলত, কারণ কার্ড ছোট।
  ⚠️ **ছবির উপরে কোনো ব্যাজ বসানো হয় না** (সিদ্ধান্ত: ১১ সেপ্টেম্বর ২০২৬)। আগে "🔍 বড় করে দেখুন" চিপ ছিল — ছবিই ঢেকে দিত। এখন শুধু `card.title` (হোভারে টুলটিপ) ও `cursor:pointer`।
  ⚠️ **`z-index: 10000`** — হোয়াটসঅ্যাপ বাটন ও "উপরে যান" (৯৯৯৯), গ্লাস নেভ (৯০০০), চ্যাট প্যানেল (৯৪০০) — সবার উপরে থাকতে হবে। নতুন কোনো ফ্লোটিং উপাদান যোগ করলে এই সংখ্যা মিলিয়ে দেখতে হবে।
  ⚠️ এই স্ক্রিপ্ট `kh-ui.js`-এর **আগে** চলে, তাই `KHUI.bn()` ব্যবহার করা যায় না — ভেতরে নিজস্ব `bnNum()` আছে।
- **⚠️ লাইট থীমে কার্ডের নাম অদৃশ্য হয়ে যাচ্ছিল (১৩ সেপ্টেম্বর ২০২৬)।** `.khc-label` একটি `<div>`, ভেতরে একটি `<span>` — আর সেকশন ৯-এর ঢালাও নিয়ম `html[data-theme="light"] :is(…, .hero, …) :is(…, div, span, …) { color:#35204f !important }` সেগুলোকেও ধরে ফেলত। নামগুলো বসে ছবির গাঢ় নিচের অংশে, তাই গাঢ় বেগুনি লেখা কালো গ্র্যাডিয়েন্টের উপর কার্যত মিলিয়ে যেত।
  **⚠️ স্পেসিফিসিটি মিলিয়ে লিখতে হয়েছে।** ঢালাও নিয়মটির ওজন **(০,২,২)**; শুধু `html[data-theme="light"] .khc-label` দিলে হয় (০,২,১) — দুই দিকেই `!important` থাকায় ঢালাওটিই জিতত (ব্রাউজারে মেপে দেখা গেছে রঙ `rgb(53,32,79)`-ই থেকে যাচ্ছিল)। তাই `.khc-card` যোগ করে **(০,৩,১)/(০,৩,২)** করা হয়েছে।
  সাথে গ্র্যাডিয়েন্ট একটু গাঢ় (`transparent → .55 → .88`) ও `text-shadow` — ছবি একেকটা একেক রকম, উজ্জ্বল ছবিতেও নাম পড়া যাবে। **নিয়ম: ঐ ঢালাও নিয়মের ভেতরে পড়ে এমন কোনো সাদা লেখা বাঁচাতে হলে নির্বাচকের ওজন (০,২,২)-এর বেশি করতে হবে; `!important` একা যথেষ্ট নয়।**
- **মাপ (মূল ২১০×৩১৫ থেকে দুই ধাপে বড় করা):** ডেস্কটপে **২৮০×৪২০**, ঘর ৪৯০px; ≤৯০০px-এ **২৭৬×৪১৪**, ঘর ৪৫৬px; ≤৪৮০px-এ **২৩৬×৩৫৪**, ঘর ৩৯২px। কার্ডের মাপ বদলালে `POS`-এর সাতটি দূরত্বও একই অনুপাতে বদলাতে হবে (এখন ±৪১০/±২৬০/±১৩৫)।
  ⚠️ **কার্ড কখনো ঘরের চেয়ে লম্বা হতে পারবে না** — কার্ড `top:50%` + `translateY(-50%)` দিয়ে বসে আর `.hero`-তে `overflow:hidden`, তাই লম্বা হলে নিচের নামটুকু কেটে যায় (৪৮০px ব্লকে ঠিক এটিই হয়েছিল)। ঘরের উচ্চতা সবসময় কার্ডের চেয়ে ~৪০px বেশি রাখা হয়।

## 📖 কুরআন ও হাদিসের স্ট্রিপ — "পান্ডুলিপি" থীম (index.html · ১২ সেপ্টেম্বর ২০২৬)

- **⚠️ আসল সমস্যা: পটভূমি ছিল `var(--dkgreen)` — আর `--dkgreen` আসলে `#160d26`, সবুজ নয়, গাঢ় প্লাম।** অর্থাৎ ব্লকটির রঙ ছিল টপবার ও হিরোর প্রায় একই। তাই হিরোর নিচে ওটি আলাদা করে চোখেই পড়ত না, কুরআন-হাদিসের অংশ বলেও বোঝা যেত না (ব্যবহারকারীর পর্যবেক্ষণ)। **নিয়ম: `--green` / `--dkgreen` / `--ltgreen` নামগুলো ভুলপথে নিয়ে যায় — তিনটিই আসলে বেগুনি/প্লাম ঘরানার। রঙ বাছার আগে মান দেখে নিতে হবে।**
- **নতুন থীম — হালকা পার্চমেন্ট পাতা।** গাঢ় হিরোর ঠিক পরেই উল্টো উজ্জ্বলতা, তাই ব্লকটি এক নজরেই আলাদা।
  • পটভূমি `radial-gradient(125% 95% at 50% 0%, #fffdf6, #fbf3e2 55%, #f2e4c9)`
  • উপরে ও নিচে সোনালি রেখা (`#c8912f → #f0c26b → #c8912f`, দুই প্রান্তে মিলিয়ে যায়) — নিচেরটি `background`-এর স্তরে, উপরেরটি `::after`
  • `::before`-এ **লগইন পেজের ক্যালিগ্রাফি ছবি** (নিচে দেখুন)
  • সবুজ কালি (`#123a2c`), সবুজ গোল মেডেলিয়ন আইকন, সবুজ গ্র্যাডিয়েন্ট চিপ
- **⚠️ পটভূমির নকশা এখন `images/auth-bg.jpg` — লগইন পেজেরটিই (সিদ্ধান্ত: ১৩ সেপ্টেম্বর ২০২৬)।** আগে হাতে আঁকা গিরিহ তারা (ইনলাইন SVG, `opacity:.09`) ছিল; ব্যবহারকারী চেয়েছেন সাইটজুড়ে একই ইসলামি ভাষা। `center / cover no-repeat`, **`opacity: .18`**।
  • ছবিটি `user-login.html`-এও ব্যবহৃত (১২০ KB), তাই বেশিরভাগ দর্শকের কাছে ক্যাশ থেকেই আসে।
  • **⚠️ ছবির দুই কিনারায় গাঢ় নেভি আছে** — ক্যানভাসে ছবিটি হুবহু একই `cover` ক্রপে এঁকে প্রতিটি পিক্সেল মেপে দেখা গেছে লেখার এলাকায় সবচেয়ে গাঢ় পটভূমি দাঁড়ায় `rgb(199,190,171)`। তাই:
    – বাংলা বাণী `#123a2c` → **৬.৮:১** ✓
    – ইংরেজি অনুবাদ `#4a5f52` → **৩.৭:১ ✗** — তাই গাঢ় করে `#37493f` (**৫.২:১**)
    – চিপ ও রেফারেন্স নিজের সবুজ পটভূমি বহন করে → ৪.৯:১ ✓ (ছবি এদের ছোঁয় না)
    – শিরোনামের দুই পাশের সোনালি রেখা `#cfa04a` → ছবির উপর মাত্র **১.৩:১**, প্রায় মিলিয়ে যেত; এখন `#a97615` (২.১:১ — নিছক সাজসজ্জা, `aria-hidden`)
  **নিয়ম: পটভূমিতে ছবি বসালে অনুমান নয় — ছবিটি ক্যানভাসে একই ক্রপে এঁকে, একই `opacity`-তে ব্লেন্ড করে, লেখার এলাকার সবচেয়ে গাঢ় পিক্সেল বের করে তবেই রঙ ঠিক করতে হবে।**
- **নতুন শিরোনাম** — `.quran-head`-এ "কুরআন ও হাদিসের আলো" চিপ, দুই পাশে সোনালি রেখা। আগে কোনো শিরোনাম ছিল না, তাই ব্লকটি কী তা বোঝার উপায়ও ছিল না।
- **⚠️ উচ্চতা দুবার ঠিক করতে হয়েছে (ব্যবহারকারী: "চওড়া বেশী হয়ে গেছে, আগের সাইজ ঠিক ছিল")।** প্রথম সংস্করণে প্যাডিং `44px 68px 36px` ও শিরোনামটি **দুই সারিতে** (চিপ, তার নিচে অলংকার) ছিল — শুধু মাথাটিই ৭৩px খেত, মোট ৩১৪px। মেপে দেখা গেল স্লাইডের অংশ (২৪১px) মূল নকশার প্রায় সমানই; পুরো বাড়তিটা ছিল মাথায়। এখন প্যাডিং `24px 60px 18px`, আইকন ৩৪px, আর **শিরোনাম ও দুই পাশের রেখা এক সারিতে** (মাথা ~৩৪px) → মোট **২৮৫px** (মূল ~২৪৪px; পার্থক্যটুকু নতুন শিরোনামের)।
  **নিয়ম: "বেশী চওড়া/বড় লাগছে" শুনলে অনুমান না করে প্রতিটি অংশের `offsetHeight` মেপে দেখতে হবে কোনটা আসলে জায়গা খাচ্ছে।**
  ⚠️ মোবাইলে (≤৬৪০px) দুই পাশের রেখা লুকানো — শুধু চিপটিই থাকে।
- **⚠️ `kh-ui.css`-এর সেকশন ৯-এর দুটি ঢালাও লাইট-থীম নিয়ম থেকে `.quran-strip` বাদ দেওয়া হয়েছে।** ঐ দুটি নিয়ম (`background … !important` ও `:is(…) :is(div, span, …) { color:#35204f !important }`) পার্চমেন্ট পটভূমি ও সবুজ কালি দুটোই চাপা দিত। **`!important` দিয়ে ঠেকানো যায়নি** — দুই দিকেই `!important` থাকায় স্পেসিফিসিটি জেতে, আর ঢালাও সিলেক্টরটি বেশি নির্দিষ্ট (ব্রাউজারে মেপে ধরা পড়ে: লাইট থীমে চিপের সাদা লেখা `#35204f` হয়ে ২.৯১:১ হয়ে যেত)। তাই তালিকা থেকে বাদ দেওয়াই একমাত্র সঠিক পথ।
- **ভাউচার ও প্রোফাইল কার্ডের মতোই দুই থীমে এক** — এটি "কাগজ", থীমের সাথে বদলায় না। যাচাই: ডার্ক, লাইট ও থীম-হীন — তিন অবস্থাতেই প্রতিটি মাপ হুবহু একই এসেছে।
- **কনট্রাস্ট** — উপরের ক্যালিগ্রাফি-ছবির অনুচ্ছেদে মাপা মানগুলো দেখুন। তীরের বাটন নিজের ক্রিম পটভূমি বহন করে (আইকন ৭.৯:১)।
- **⚠️ পার্চমেন্টে কমলা (`--gold` = `#f08c28`) ব্যবহার করা যাবে না** — মাত্র ২.৩:১। আইকন ও চিপ তাই নিজের সবুজ পটভূমি বহন করে (সাদা লেখা = রঙিন পটভূমি — প্রকল্পের ২ নম্বর নিয়ম)।

## ✨ সেকশন-ব্রেকের আলো-রেখা + লাইট থীমের ক্যালিগ্রাফি (kh-ui · ১২ সেপ্টেম্বর ২০২৬)

### সেকশন-ব্রেক (`.kh-break`)

- **কী:** পাশাপাশি দুটি বড় সেকশনের মাঝে একটি সরু ব্র্যান্ড-গ্র্যাডিয়েন্ট রেখা, মাঝখানে ছোট হীরে, আর তার উপর দিয়ে **বাঁ থেকে ডানে** ভেসে যাওয়া অ্যাম্বার আভা (`khBreakSweep`, ৫.৬ সেকেন্ড)। এক অংশ শেষ হয়ে আরেকটি শুরু হচ্ছে — সেটি যেন চোখে পড়ে।
- **কে বসায়:** `KHUI.mountSectionBreaks()` — `boot()` থেকে `load`-এর পর। মার্কআপে কিছু লিখতে হয় না, **সব পেজেই** আপনাআপনি আসে। বাদ দিতে `<body data-kh-breaks="off">` বা সেকশনে `class="kh-nobreak"`।
- **⚠️ অনুমান করা হয় না — মেপে বাছা হয়।** `khIsBand()` শুধু তাদেরই "ব্যান্ড" ধরে যারা উচ্চতায় ≥১৪০px, চওড়ায় ≥৯০% ও সাধারণ প্রবাহে (`fixed`/`absolute`/`sticky` নয়)। নেভবার, টপবার, গ্লাস নেভ, হোয়াটসঅ্যাপ বাটন ও `[role="dialog"]` স্পষ্টভাবে বাদ। **প্রতিটি পেজের গঠন আলাদা, তাই এই মাপজোখই একমাত্র নিরাপদ পথ।**
- **⚠️ দুবার চালানো হয় (load+৬০ms ও load+১৫০০ms)।** `#khStats`-এর মত ঘর RPC-র উত্তর আসার পর ভরে; প্রথম পাসে উচ্চতা ০ থাকায় বাদ পড়ত, আর রেখাটি ভুল জায়গায় বসত। দ্বিতীয় পাস নিরাপদ — আগের রেখা দেখলে গণনা রিসেট হয়, তাই একই জায়গায় দুবার বসে না।
- **⚠️ body-তে কিছু না মিললে এক ধাপ ভেতরে নামা হয়** — কিছু পেজে (my-dashboard, committee, wallet) পুরো বিষয়বস্তু একটি মোড়কের ভেতরে। **কেবল সাধারণ `block`/`flow-root` মোড়কে** নামা হয়; flex/grid মোড়কে ৩px-এর একটি সন্তান লেআউট ভেঙে দিত। (`dashboard.html`, `user-login.html`, `reset-password.html` — এদের `body` row-flex, কিন্তু সেখানে যোগ্য ব্যান্ড একটির বেশি নেই, তাই কিছুই বসে না।)
- **⚠️ ঘরটি ১২px উঁচু, রেখাটি ৩px** — ব্যাকগ্রাউন্ডে মাঝখানে বসানো। প্রথমে পুরো ঘরটিই ৩px ছিল, তখন `overflow:hidden` মাঝের হীরেটিকে (৭px, ৪৫° ঘোরানো ⇒ ~১০px) কেটে ফেলত, শুধু একটি বিন্দু দেখা যেত। `overflow:hidden` সরানো যায় না — আভা ১৩০% পর্যন্ত যায়, নাহলে পেজে আড়াআড়ি স্ক্রল আসত।
- **⚠️ আভার রঙ অ্যাম্বার (`#ffcf70`), সাদা নয়** — সাদা দিলে লাইট থীমের হালকা পটভূমিতে একেবারেই দেখা যেত না। রেখাটি নিজের রঙ বহন করে, তাই দুই থীমে এক।
- **মোবাইলে থাকে** (ব্যবহারকারীর অনুরোধ) — ১০px উঁচু, রেখা ২px, আভা ৪.২ সেকেন্ডে।
- **ব্যাটারি:** `IntersectionObserver` পর্দার বাইরের রেখার আভা থামিয়ে দেয় (`.kh-break-off` → `animation-play-state: paused`)। একটি পেজে ~১০টি ব্লার-করা স্তর সারাক্ষণ চললে মোবাইলে খরচ হত। `prefers-reduced-motion`-এ আভা পুরোপুরি বন্ধ।

### লাইট থীমের ক্যালিগ্রাফি পটভূমি (`--kh-calli`)

- **কী (সংশোধন: ১৩ সেপ্টেম্বর ২০২৬):** **লগইন পেজের ছবিটিই** — `images/auth-bg.jpg`। ব্যবহারকারীর সিদ্ধান্ত, যাতে সাইটজুড়ে একই ইসলামি ভাষা থাকে। আগে ছিল হাতে আঁকা আরবেস্ক SVG, ২৬০px টাইল।
- **⚠️ ছবিটি টাইল করা যায় না** — এটি একটি সম্পূর্ণ শিল্পকর্ম (১৬৮০×৯৪৪), পুনরাবৃত্তি করলে জোড়া চোখে পড়ত। তাই `center / cover fixed no-repeat` — পুরো পর্দাজুড়ে একটিই ছবি, স্ক্রলেও স্থির।
- **⚠️ ছবিটি বসে কেবল `body`-তে, একবার।** বড় ব্যান্ডগুলোর (`.hero`, `.footer`, `.mv-section`, `.how-section`, …) ঢালাও নিয়মে গ্র্যাডিয়েন্ট এখন **স্বচ্ছ** (alpha `.86`), তাই একই ছবি সবার ভেতর দিয়ে দেখা যায়। আগে প্রতিটি ব্যান্ডে আলাদা নকশা বসত — এতে ব্যান্ডে ব্যান্ডে ছবিটি **মিলে যায়** ও দশটি আলাদা `fixed` ব্যাকগ্রাউন্ড আঁকার খরচও বাঁচে।
- **⚠️ ব্যাকগ্রাউন্ড স্তরে `opacity` দেওয়া যায় না** — তাই ছবির উপরে একটি **সাদা পর্দা** (`--kh-calli-veil`)। `background` shorthand-এ পর্দাটি **প্রথম স্তরে**, ছবি দ্বিতীয়, রঙ শেষে (প্রথম স্তরই সবার উপরে)।
- **⚠️ পর্দার ঘনত্ব `.84` — মেপে বাছা।** ছবিটি ক্যানভাসে একই `cover` ক্রপে এঁকে প্রতিটি পিক্সেল ব্লেন্ড করে দেখা গেছে সবচেয়ে গাঢ় জায়গা দাঁড়ায় `rgb(213,212,216)`। তাতে মূল লেখা **১১.৫:১**, `--muted` **৪.৭:১**; বড় ব্যান্ডের ভেতরে (আরও .86 পর্দা) muted **৬.০:১**।
  `.80` করলে muted ৪.৫:১-এর কিনারায় চলে যেত, `.88` করলে নকশাটাই চোখে পড়ত না।
  **নিয়ম: পটভূমিতে কোনো নকশা বসালে শুধু মূল লেখা নয়, `--muted` লেখাও মেপে দেখতে হবে — সেটিই আগে ভাঙে।**
- **⚠️ মোবাইলে `fixed` নয়** — iOS-এ `background-attachment: fixed` ঠিকমতো চলে না। ≤৭৬৮px-এ `scroll`, `center top / cover`।
- লাইট থীমের `--muted` আগেই গাঢ় করা হয়েছে `#6b6280 → #5f566f` (পুরনো SVG নকশার সময়) — সেটি এখানেও কাজে লাগছে।

## 👣 ভিজিটর কাউন্টার (kh-ui · ১২ সেপ্টেম্বর ২০২৬)

- **কোথায়:** `KHUI.mountVisitorCounter()` — `boot()` থেকে নিজে থেকেই চলে। বসে `#khVisitors` ঘরে, না থাকলে পেজের প্রথম `<footer>`-এর শেষে। বাদ দিতে হলে `<body data-kh-visits="off">`।
  ⚠️ **যে পেজে `<footer>` নেই সেখানে আসে না** — বর্তমানে `index` · `about` · `contact` · `borrower-portal` · `savings-portal`, এই পাঁচটিতে। `donation`, `committee`, `my-dashboard`, `wallet` ইত্যাদিতে ফুটার নেই বলে কাউন্টারও নেই; দরকার হলে ঐ পেজে শুধু `<div id="khVisitors"></div>` বসিয়ে দিলেই চলে। অ্যাডমিন `dashboard.html`-এও ফুটার নেই, তাই ওখানে এমনিতেই আসে না।
- **দুটি সংখ্যা:** আজকের ভিজিটর ও সর্বমোট ভিজিটর। "আজ" মানে **ঢাকার তারিখ** (`now() at time zone 'Asia/Dhaka'`), UTC নয়।
- **⚠️ দুই নিয়ম, একটি সুইচ — `app_settings.visit_mode` (সিদ্ধান্ত: ১২ সেপ্টেম্বর ২০২৬)।**
  • `'views'` — **প্রতিটি পেজ ভিউ একবার গোনা হয়** (এখন এটিই চালু)। শুরুর দিকে সংখ্যা বড় দেখানোর জন্য ব্যবহারকারীর সিদ্ধান্ত। একই ব্যক্তি রিফ্রেশ করলেও বাড়ে।
  • `'unique'` — একজন দর্শক দিনে একবার, আর সর্বমোটে জীবনে একবার। আসল হিসাব; পরে এতে ফেরা হবে।
  • **নিয়ম বদলানো:** সুপার অ্যাডমিন → **জেনারেল সেটিংস → "ভিজিটর কাউন্টার — কী হিসেবে গোনা হবে"** — দুটি রেডিও, সাথে দুই হিসাবের বর্তমান সংখ্যা পাশাপাশি দেখানো থাকে যাতে বেছে নিতে সুবিধা হয়। SQL লাগে না।
    (হাতে করতে চাইলে: `update app_settings set value = '"unique"'::jsonb where key = 'visit_mode';`)
  • **⚠️ দুই হিসাবই সবসময় রাখা হয়** — `views` ও `visitors` আলাদা কলামে। তাই নিয়ম বদলালে পুরনো তথ্য হারায় না, সংখ্যাও পিছিয়ে যায় না; RPC দুটোই ফেরত দেয় (`today_views`/`today_unique`/`total_views`/`total_unique`)।
  • লেবেল দুই নিয়মেই "ভিজিটর" থাকে — বাংলাদেশি সাইটে এটিই প্রচলিত, যদিও `views` মোডে সংখ্যাটি আসলে পেজ ভিউ।
- **গোপনীয়তা:** IP, ইউজার-এজেন্ট বা কোনো ব্যক্তিগত তথ্য রাখা হয় না। শুধু একটি এলোমেলো UUID ব্রাউজারের `localStorage.kh_vid`-এ থাকে। কেউ তা মুছলে বা নতুন ডিভাইসে এলে নতুন দর্শক হিসেবে গোনা হবেন — জেনেশুনেই এই আপস।
- **টেবিল:** `site_visitors` (প্রতি দর্শক এক সারি) · `site_visit_seen (visitor_id, day)` — দিনে একবারের দেয়াল, PK-ই ডুপ্লিকেট ঠেকায় · `site_visit_days (day, views, visitors)` · `site_visit_total (views, visitors)` — এক সারি।
  ⚠️ যোগফল আলাদা টেবিলে রাখা হয়েছে **ইচ্ছে করেই** — নাহলে প্রতিবার `count(*)` করতে হত, সারি বাড়লে ফুটার ধীর হয়ে যেত।
- **RPC:** `record_visit(p_visitor uuid)` → `{visitor, mode, today, total, today_views, today_unique, total_views, total_unique}` (গোনে ও ফেরত দেয়) · `visit_counts()` → একই, শুধু গোনে না। দুটোই SECURITY DEFINER, anon-ও ডাকতে পারে। `today`/`total` সবসময় **চালু মোড অনুযায়ী** আসে, তাই ক্লায়েন্টের কোড বদলাতে হয় না।
  ⚠️ চার টেবিলেই RLS চালু ও **কোনো পলিসি নেই**, সাথে `revoke all ... from anon, authenticated` — অর্থাৎ কাঁচা টেবিল কেউ পড়তেও পারে না, লিখতেও পারে না। যাচাই: লাইভ পেজ থেকে চারটি চেষ্টাতেই `permission denied for table …`।
  ⚠️ `revoke execute ... from public` তারপর `grant` — নমিনির ফাংশনগুলোর মতোই (EXECUTE ডিফল্টে PUBLIC-এর)।
- **সর্বমোট কীভাবে বাড়ে:** `views` মোডে প্রতিবার; `unique` মোডে আজকের দিনটি ঐ দর্শকের **প্রথম দিন** হলে তবেই — তাই ফিরে আসা দর্শক সর্বমোটে দুবার যোগ হন না।
- **যাচাই:** একই দর্শকের তিনটি পেজ ভিউয়ে `views` মোডে ১→২→৩ এসেছে অথচ `today_unique` ১-এই থেকেছে; মোড `unique` করে দেখা গেছে সংখ্যা ১ দেখায় কিন্তু `today_views` ৩-ই রয়ে গেছে — অর্থাৎ নিয়ম বদলালে কোনো তথ্য হারায় না। পরীক্ষার সারি মুছে কাউন্টার শূন্য থেকে শুরু করা হয়েছে।
- **দেখতে:** নিজস্ব গাঢ় গ্র্যাডিয়েন্ট (`#241539 → #3d1a5c → #0d3b2e`) বহন করে, কারণ ফুটারের পটভূমি পেজভেদে আলাদা। উপরে ধীরে ভেসে বেড়ানো রঙিন আভা (`khVisitDrift`), সংখ্যা ০ থেকে গুনে গুনে ওঠে, আর টিমটিম করা সবুজ "লাইভ" ব্যাজ। `prefers-reduced-motion` মানা হয়।
  ⚠️ **প্রিফিক্স `.kh-visit-`** — `.kh-vc-` আগে থেকেই ভেরিফিকেশন-চুজারের দখলে, মিলিয়ে ফেলা যাবে না।
  ⚠️ **লাইট থীমে `index.html`-এর ফুটার একটি ঢালাও নিয়মে ধরা পড়ে** (সেকশন ৯: `html[data-theme="light"] … .footer … div, span { color:#35204f !important }`)। কাউন্টারের লেবেল, "লাইভ" ও আইকন সবই span/div — তাই সেগুলো গাঢ় বেগুনি হয়ে কার্ডের গাঢ় পটভূমিতে **কার্যত অদৃশ্য** (১.০২:১) হয়ে গিয়েছিল। এখন `html[data-theme="light"] .kh-visit …` দিয়ে প্রতিটি রঙ `!important` সহ ফেরানো আছে। **কাউন্টারে নতুন কোনো লেখা যোগ করলে ঐ ব্লকেও তার রঙ ফিরিয়ে দিতে হবে।**
  ⚠️ **আভার ঘনত্ব ইচ্ছে করেই কম** (.৩ ঘরে)। বেশি দিলে কমলা আভার উপর ১২px লেবেলের কনট্রাস্ট ৩.৫:১-এ নেমে যেত — আর আভা নড়ে বলে সমস্যাটা থেকে থেকে ফিরে আসত। এখন সবচেয়ে খারাপ জায়গাতেও ৬.৬:১।
  ⚠️ **`aria-live` দেওয়া হয় না** — গোনার সময় সংখ্যা সেকেন্ডে ~৬০ বার বদলায়, স্ক্রিন রিডার প্রতিবার পড়ে শোনাত। সংখ্যা বসার পর একবারে `aria-label` বসানো হয়।
  ⚠️ ট্যাব পেছনে থাকলে `requestAnimationFrame` চলে না — তাই `document.hidden` হলে সংখ্যা সরাসরি বসানো হয় (নাহলে "০" দেখাত)। RPC-তে ৮ সেকেন্ডের সময়সীমা, আর `visitDone` পতাকা — নাহলে ব্যর্থতার পর আবার ডাকলে দুবার গোনা হত।
- **ব্যর্থ হলে কিছুই দেখায় না** — সুপাবেস না থাকলে বা RPC ব্যর্থ হলে কার্ডটি DOM থেকে সরে যায়, "০ ভিজিটর" দেখিয়ে বিব্রত করে না।
- **অ্যাডমিন টগল (`dashboard.html`):** `loadGeneralSettings()` `app_settings` থেকে `visit_mode` পড়ে রেডিও বসায় ও `visit_counts()` ডেকে চারটি সংখ্যা `#gsVisitNow`-এ দেখায়; `saveGeneralSettings()` বাকি কি-গুলোর সাথে `['visit_mode', visitMode]` পাঠায়। ⚠️ মান **শুধু `'views'` বা `'unique'`** — অন্য কিছু গেলে সার্ভার `'views'` ধরে নেয়। ⚠️ `set_app_setting`-এর `p_value` **jsonb**, তাই সাধারণ স্ট্রিং পাঠালেই `"views"` হয়ে জমা হয় আর `.from('app_settings')` পড়ায় সাধারণ স্ট্রিং হয়েই ফেরে — দুই দিকই মিলে যায় (লাইভে যাচাই করা)।
- **সীমা (জেনে রাখা):** কেউ ইচ্ছে করে বারবার `record_visit` ডাকলে সংখ্যা বাড়ানো সম্ভব (`views` মোডে আরও সহজে)। এটি নিছক একটি ভ্যানিটি কাউন্টার, কোনো আর্থিক হিসাব নয় — তাই রেট-লিমিট বসানো হয়নি। কখনো আসল পরিসংখ্যান দরকার হলে Vercel Analytics বা Plausible ব্যবহার করতে হবে।

## ✍️ ই-সিগনেচার (kh-ui · ১২ সেপ্টেম্বর ২০২৬)

- **কে দিতে পারেন:** যে কেউ, `my-profile.html` → "ই-সিগনেচার" প্যানেল থেকে। **অ্যাডমিন, সুপার অ্যাডমিন ও কমিটির অনুমোদিত সদস্যদের জন্য প্রয়োজন** — তাঁদের স্বাক্ষর না থাকলে নিচে কমলা স্মরণিকা পট্টি (`.kh-sigbar`) ওঠে।
  ⚠️ **স্মরণিকা কোনো কাজ আটকায় না (সিদ্ধান্ত: ১২ সেপ্টেম্বর ২০২৬)** — জরুরি মুহূর্তে স্বাক্ষরের অভাবে অনুমোদন থেমে যাওয়া এর চেয়ে বড় ক্ষতি। "পরে" চাপলে ঐ সেশনে আর দেখায় না (`sessionStorage.kh_sigbar_off`)। বন্ধ করতে `<body data-kh-sig="off">`।
- **দুই পথ:** আঙুলে আঁকা (`pointer` + `touch`, `touch-action:none`) অথবা ছবি/ক্যামেরা। দুটোরই ফল এক — **স্বচ্ছ পটভূমি, কাটা-ছাঁটা, ৬০০×২০০ PNG**।
- **⚠️ পটভূমি মোছা একটিমাত্র উজ্জ্বলতার সীমা দিয়ে হয় না।** মোবাইলে তোলা ছবিতে কাগজের এক পাশে ছায়া থাকে; সাধারণ threshold দিলে ছায়ার দিকটা পুরো কালো হয়ে যেত। তাই `sigAnalyze()`:
  ① ধূসরে রূপান্তর → ② কাগজের উজ্জ্বলতা **এলাকাভিত্তিক** আন্দাজ (ব্লকে ভাগ করে ৯০তম পার্সেন্টাইল — সর্বোচ্চ নিলে চকচকে প্রতিফলন ধরত, গড় নিলে কালিও মিশে যেত) → ③ bilinear মসৃণ করে প্রতি পিক্সেলে বসানো → ④ `diff = কাগজ − পিক্সেল` → ⑤ সবচেয়ে গাঢ় কালিকে ২৫৫ ধরে মাপা (হালকা পেন্সিলও ওঠে) → ⑥ **Otsu** দিয়ে স্বয়ংক্রিয় সীমা।
  `sigRender()` নরম কিনারা দেয় (`lo = thr*0.55`, `hi = thr*1.15`) যাতে খাঁজকাটা না দেখায়, তারপর কালির চারপাশে কেটে `sigFit()` ৬০০×২০০ পাতার মাঝখানে বসায়।
  **যাচাই (ব্রাউজারে, কৃত্রিম কঠিন ছবি দিয়ে):** এক পাশে ভারী ছায়া (`#fff → #6e6a63`) + দানা দেওয়া ছবিতে — কোণা চারটিই সম্পূর্ণ স্বচ্ছ, আর কালি **দুই পাশেই** ধরা পড়েছে (উজ্জ্বল দিকে ২৭৭৮ px, ছায়ার দিকে ২২৯৩ px)। সাধারণ threshold-এ ছায়ার দিকটা পুরো হারিয়ে যেত।
- **⚠️ নীল কলম শনাক্ত করা হয় `diff` বের হওয়ার পরে, শুধু কালির পিক্সেল দেখে।** প্রথমে "গাঢ় পিক্সেল" (`y < 128`) ধরে হিসাব করা হয়েছিল — কিন্তু ছায়ায় পড়া ধূসর কাগজও গাঢ় গোনা হত, ফলে নীল ঝোঁক চাপা পড়ে নীল কলমও কালো হয়ে যেত (পরীক্ষায় ধরা পড়ে)। এখন নীল কলমে `delta = +১০৭` → নীল, কালো কলমে `delta = 0` → কালো। কালি বসে দুটি নির্দিষ্ট রঙে: নীল `#16316b`, নয়তো `#111827` — প্রতি পিক্সেলের কাঁচা রঙ রাখলে দাগ-ছোপ থেকে যেত।
- **ঘনত্বের টান:** ছবি দিলে Otsu-র মান স্লাইডারে বসে; দাগ কম উঠলে বাঁয়ে, দাগ-ছোপ উঠলে ডানে — `sigRender()` সাথে সাথে নতুন করে আঁকে (বিশ্লেষণ একবারই হয়, তাই দ্রুত)।
- **সংরক্ষণ:** `KHUI.saveSignature(blob)` → `signatures` বাকেটে `<uid>/signature-<ts>.png` → `set_my_signature(path)`। **⚠️ পথ অবশ্যই uid দিয়ে শুরু** — স্টোরেজ পলিসি ও RPC **দুই জায়গাতেই** যাচাই হয় (RPC-তে `position(uid in p_url) = 0` হলে বাতিল)। যাচাই: অন্যের uid দিয়ে বসাতে গেলে `signature path must be your own`।
- **গোপনীয়তা:** স্বাক্ষর নকল করা যায়, তাই **প্রাইভেট বাকেট** (NID-এর মতোই) — নীতি `sig_read_own_or_admin` (মালিক বা `is_admin()`), লেখা/মোছা কেবল মালিক, সবই `to authenticated`। দেখানো হয় `KHUI.signatureUrl()`-এর সাময়িক signed URL দিয়ে।
  যাচাই (অতিথি হিসেবে লাইভ পেজ থেকে): তালিকা ফাঁকা, ডাউনলোড আটকেছে, signed URL আটকেছে, আপলোডে `new row violates row-level security policy`, `set_my_signature`-এ `not signed in`।
- **RPC:** `set_my_signature(p_url text)` (null দিলে মুছে যায়) · `my_signature_status()` → `{signed_in, has, url, is_admin, is_member, required}`। কলাম: `user_profiles.signature_url` ও `signature_at`।
- **কমিটির ফর্মে:** `committee-form.html`-এ কাগজে স্বাক্ষরের বিকল্প হিসেবে বসানো আছে। ⚠️ **আমন্ত্রণের লিংকে লগইন ছাড়াই ফর্ম ভরা যায়**, কিন্তু স্বাক্ষর প্রোফাইলে জমা হয় — তাই লগইন না থাকলে বাটনটি নিষ্ক্রিয় থাকে ও বলা হয় পরে লগইন করে দেওয়া যাবে; **আবেদন আটকায় না**।
- **⚠️ কালির ঘর সবসময় সাদা** — আঁকার প্যাড ও প্রাকদর্শন দুটোই। কালি গাঢ়, ডার্ক থীমে গাঢ় ঘরে বসালে মিলিয়ে যেত।
- **পরে যেখানে কাজে লাগবে:** অনুমোদনের কাগজ, সনদ, কমিটির নথি — `user_profiles.signature_url` থেকে নিয়ে বসানো যাবে। ⚠️ ভাউচারের মেইলে বসাতে হলে মনে রাখতে হবে বাকেটটি প্রাইভেট, তাই Edge Function-কে service_role দিয়ে পড়তে হবে।

## 🪪 প্রিমিয়াম প্রোফাইল কার্ড + অনলাইন উপস্থিতি (১২ সেপ্টেম্বর ২০২৬)

- **কে কী দেখেন:** সদস্য **কেবল নিজের** কার্ড (`my-dashboard.html` → "আমার পরিচয়পত্র")। অ্যাডমিন/সুপার অ্যাডমিন সবার — dashboard → **"সদস্যের পরিচয়পত্র"** প্যানেল, ক্যাটাগরি পিল (সব / আমানতকারী / দানকারী / ঋণগ্রহীতা) ও নাম-মোবাইল-কোড দিয়ে খোঁজ।
- **RPC:** `profile_card(p_user uuid default null)` — null হলে নিজের; অন্যের চাইলে `is_admin()` না হলে `শুধু নিজের কার্ড দেখা যাবে`। `admin_profile_cards(category, search, limit, offset)` ও `online_users()` — দুটোই `admin only`।
  ⚠️ ভেতরের হেল্পার **`_card_for(uuid)` কোনো অনুমতি যাচাই করে না** — তাই `anon`/`authenticated` কাউকে EXECUTE দেওয়া হয়নি, কেবল `service_role`। নতুন কোথাও ব্যবহার করলে আগে অনুমতি যাচাই করতে হবে।
  যাচাই: সদস্য সেজে — নিজের কার্ড ✓, অন্যের কার্ড আটকেছে, তালিকা ও অনলাইন তালিকা `admin only`; অ্যাডমিন সেজে — সব ✓, ছাঁকনিতে সব ৪ / আমানতকারী ৪ / দানকারী ৩ / ঋণগ্রহীতা ০।
- **⚠️ ক্যাটাগরি ঠিক হয় আসল রেকর্ড দেখে, `is_depositor` পতাকা দেখে নয়** — পতাকা পুরনো বা ভুল থাকলেও তালিকা ঠিক থাকে। হিসাব থাকলে আমানতকারী, নিশ্চিত দান থাকলে দানকারী, ঋণ থাকলে ঋণগ্রহীতা।
- **হিসাব:** আমানত (হিসাব সংখ্যা, জমা, উত্তোলন, অপেক্ষমাণ, স্থিতি — শুধু `approved` ধরা হয়, স্টেটমেন্টের মতোই) · দান (সংখ্যা, সর্বমোট — শুধু `confirmed`) · ঋণ (সংখ্যা, মূল, পরিশোধিত, বকেয়া)।
  যাচাই: চারজনের প্রতিটি সংখ্যা কাঁচা SQL-এর সাথে হুবহু মিলেছে।
- **অনলাইন:** `user_presence(user_id, last_seen)` + `touch_presence()`। `KHUI.startPresence()` প্রতি মিনিটে স্পন্দন পাঠায়; **২ মিনিটের** মধ্যে সাড়া পেলে অনলাইন।
  ⚠️ **ট্যাব পেছনে থাকলে স্পন্দন পাঠানো হয় না** (`document.hidden`) — নাহলে খুলে রাখা ট্যাব চিরকাল "অনলাইন" দেখাত। সামনে ফিরলেই একবার পাঠায়।
  অ্যাডমিনের "কারা অনলাইনে" বাটন `online_users()` ডাকে।
- **নকশা (`.kh-pc`):** ব্যবহারকারীর দেওয়া "Premium Profile Card" নমুনা হুবহু — ৩২px গোলাই, `0 25px 55px rgba(68,76,130,.14)` ছায়া, বাঁয়ে `skew(12deg,18deg) scale(1.5)` করা গ্র্যাডিয়েন্ট ব্লব, সাদা রিং-ঘেরা গোল ছবি, সবুজ পিল, নাম, চিপ, শেষে তীরসহ লিংক, একদম ডানে গোল বাটনের খুঁটি।
  **বদল দুটি (ব্যবহারকারীর সিদ্ধান্ত):** ① রঙ Aurora-তে — `#7048e8 → #d6336c → #f08c28`; ② ডানের সোশ্যাল বাটনের বদলে **কাজের বাটন** — স্টেটমেন্ট, ফোন, হোয়াটসঅ্যাপ, ই-মেইল (তথ্য না থাকলে ধূসর ও নিষ্ক্রিয়)। **নতুন অংশ:** আমানত · দান · ঋণ বকেয়া — তিনটি ঘর।
- **⚠️ `.kh-pc-body` অবশ্যই অস্বচ্ছ সাদা ও ব্লবের উপরে।** প্রথমে স্বচ্ছ রাখা হয়েছিল — ব্রাউজারে বসিয়ে দেখা গেল তির্যক গ্র্যাডিয়েন্ট নাম, কোড ও পেশার লেখার নিচ দিয়ে চলে যাচ্ছে, গাঢ় গোলাপির উপর গাঢ় লেখা পড়াই যায় না। এখন বাঁ কিনারা `clip-path: polygon(46px 0, …)` দিয়ে কাত — নমুনার তির্যক ভাগটি এভাবেই আসে, অথচ সব লেখা সাদার উপর।
  খাড়া সাজে (≤৬২০px) কাত কিনারা উপরে সরে যায় (`polygon(0 26px, …)`), নাহলে ছবির নিচটা কেটে যেত।
- **কার্ডের ভেতর সবসময় সাদা** — নমুনাটাই সাদা কাগজের, আর গাঢ় করলে গ্র্যাডিয়েন্টের সাথে লেখা মিলিয়ে যেত। সব রঙ মেপে বসানো: লেবেল ৪.৯:১, কোড ৪.৭:১, সংখ্যা ৫.০–৫.৪:১, লিংক ৬.৮:১।
- **ব্যবহার:** `KHUI.mountProfileCard(host, uid)` (uid ছাড়া নিজের) · `KHUI.profileCardHTML(row)` (তালিকার জন্য) · `KHUI.profileCardDetail(row, cat)` — ভাউচারের পাতায় পূর্ণ হিসাব; `cat` দিলে বড় অঙ্ক ও সংশ্লিষ্ট সারিগুলো সেই ক্যাটাগরির হয় · `KHUI.pcHighlight(cardEl, cat)` · `KHUI.PC_CATS`।

### 🟠 অফলাইন ব্যাজ কমলা + ক্যাটাগরি নির্বাচন (১২ সেপ্টেম্বর ২০২৬)

- **অফলাইন = কমলা, অনলাইন = সবুজ।** আগে দুটোই সবুজ ঘরানার ছিল বলে দূর থেকে আলাদা করা যেত না। এখন `.kh-pc-pill` — পটভূমি `#fff3e6`, লেখা `#a04a00` (৫.৫:১), বিন্দু `#e8590c` (৩.৩:১); `.kh-on` আগের মতোই সবুজ ও স্পন্দিত।
- **ক্যাটাগরি ছাঁকনি আগে থেকেই সার্ভারে কাজ করত** (`admin_profile_cards`-এর `case v_cat …`)। "সব" আর "আমানতকারী" একই দেখাত কারণ **চারজন সদস্যের চারজনই আমানতকারী** (দানকারী ৩, ঋণগ্রহীতা ০) — কোড ভাঙেনি। বিভ্রান্তি এড়াতে `#pcCount` এখন চালু ক্যাটাগরির নামও লেখে।
  **নিয়ম:** "ছাঁকনি কাজ করছে না" শুনলে আগে ডাটাবেজে গুনে দেখতে হবে ঐ ক্যাটাগরিতে আসলে কারা পড়েন — আগের সঞ্চয় অনুমোদনের মতোই।
- **⚠️ নির্বাচন শুধু দেখার — ডাটাবেজে কিছু সংরক্ষণ হয় না (ব্যবহারকারীর সিদ্ধান্ত: ১২ সেপ্টেম্বর ২০২৬)।** কোনো নতুন টেবিল/RPC নেই, সদস্যের নিজের `my-dashboard` বদলায় না।
  • কার্ডে ক্লিক → কার্ডটি নির্বাচিত (`.kh-pc-sel` রিং + কোণে "✓ নির্বাচিত"), হাইলাইট হয় **চালু পিলের** ক্যাটাগরি।
  • কার্ডের ভেতরের **আমানত / দান / ঋণ ঘরে** ক্লিক → সেই ক্যাটাগরির জন্য নির্বাচন — "সব" পিলেও কাজ করে। তাই **একই ইউজারকে একাধিক ক্যাটাগরির জন্য** বেছে নেওয়া যায় (একবারে একটি দেখানো হয়; একই ঘরে আবার ক্লিক করলে নির্বাচন ওঠে)।
  • নির্বাচিত ক্যাটাগরিটি "বিস্তারিত হিসাব"-এও যায় — বড় অঙ্ক ও ঐ ক্যাটাগরির সারিগুলো `.kh-vch-hl` দিয়ে জোর পায় (`KHUI.voucher`-এর `rows` এখন `[লেবেল, মান, হাইলাইট?]`)।
  • ক্যাটাগরি পিল বদলালে নির্বাচন মুছে যায়; `#pcSelNote`-এ কাকে কোন ক্যাটাগরির জন্য বাছা হয়েছে তা লেখা থাকে (**`textContent` — নাম কখনো `innerHTML`-এ নয়**)।
- **⚠️ `data-kh-cat` ঘরে থাকে, `title` থাকে না।** টুলটিপ (`"… হিসেবে নির্বাচন করুন"`) বসায় `dashboard.html` — `kh-pc-pick` ক্লাস দেওয়ার সময়। সদস্যের নিজের কার্ডে বাছাবাছি নেই, তাই সেখানে টুলটিপও থাকে না।
- **রঙ মেপে বসানো:** হাইলাইট ঘরের পটভূমি `#f3ecff → #fdeaf3`, লেবেল `#4a3a80` (৮.৩:১), উপশিরোনাম `#6a5f86` (৫.১:১); ভাউচারের হাইলাইট সারিতে লেবেল `#4a4458` (৮.১:১)।
- **ক্যাশ-বাস্টিং** — এখন **`?v=30`**, সব ১৮টি HTML-এ (৩৬টি রেফারেন্স, গুনে মিলিয়ে দেখা)। ⚠️ `loan-application.html` আগে `?v=14`-এ আটকে ছিল; সংস্করণ বাড়ানোর সময় **সব** ফাইল গুনে মিলিয়ে দেখতে হবে, শুধু আগের সংখ্যাটি খুঁজে নয়। ⚠️ ঐ ফাইলের `</html>`-এর পরে ~১ KB ফাঁকা জায়গায় একটি NUL বাইট আছে, তাই ripgrep ওটিকে "binary" বলে বাদ দিতে পারে — আলাদা করে দেখতে হবে।

## 📊 হোম পেজের লাইভ ড্যাশবোর্ড + দুই তহবিল (১২ সেপ্টেম্বর ২০২৬)

- **কোথায় (সংশোধন: ১২ সেপ্টেম্বর ২০২৬):** `index.html`-এ ফাঁকা `<section id="khStats">`; `kh-ui.js`-এর `boot()` থেকে `KHUI.mountPublicStats()` নিজে থেকেই ভরে দেয়। বাদ দিতে `<body data-kh-stats="off">`। **অঙ্ক আনতে না পারলে ঘরটি নিজেই DOM থেকে সরে যায়** — দাতাকে "৳০" দেখিয়ে বিভ্রান্ত করা হয় না (ভিজিটর কাউন্টারের মতোই)।
  ⚠️ **প্রথমে হিরোর ঠিক নিচে বসানো হয়েছিল; ব্যবহারকারীর সিদ্ধান্তে সরানো হয়েছে।** হোম পেজের ক্রম এখন —
  **হিরো → হাদিসের স্ক্রল (`.quran-strip`) → আমাদের সেবাসমূহ (`#products`, দুটি ফটোকার্ড) → 📊 আজ পর্যন্ত আমাদের হিসাব (`#khStats`) → আমাদের কার্যক্রম (`#programs`)**।
  ফুটারের "Live dashboard" লিংকের `id="dashboard"` সেকশনটির ঠিক আগের `<span>`-এ — সেকশন সরালে ঐ `<span>`-ও সাথে নিতে হবে, নাহলে লিংকটি ভুল জায়গায় নামবে। যাচাই: লাইভ পেজে DOM ক্রম মিলিয়ে দেখা হয়েছে ও ছবি তুলে দেখা হয়েছে।
  ⚠️ সেকশনটির নিজের কোনো পটভূমি নেই (বডির সাদা)। উপরে `.pc-section` (`#f7f2fb`) ও নিচে `.section-alt` (`#faf8fc`) — তিনটিই হালকা, তাই জোড়া চোখে পড়ে না। কার্ডগুলো নিজের গ্র্যাডিয়েন্ট বহন করে বলে পটভূমি বদলালেও কনট্রাস্ট নষ্ট হয় না, কিন্তু **শিরোনাম দুটি (`.kh-stats-h2` / `.kh-stats-sub`) পটভূমির উপর নির্ভরশীল** — গাঢ় কোনো সেকশনের পাশে সরালে ঐ দুটির রঙ আবার মেপে নিতে হবে।
- **⚠️ নকল সংখ্যার পুরনো "Live impact dashboard" বাদ।** `index.html`-এ `.impact-section` ছিল হার্ডকোড করা — ১২৭ উপকারভোগী, ৳১৮L, ৯৯% পরিশোধের হার, ৮৪ জন দাতা — সাথে "Full Dashboard" বাটন যা দর্শককে **অ্যাডমিন** `dashboard.html`-এ পাঠাত। পুরো সেকশন ও তার `COUNTER ANIMATION` স্ক্রিপ্ট মুছে ফেলা হয়েছে; `id="dashboard"` (ফুটারের "Live dashboard" লিংক) এখন নতুন সেকশনের আগের `<span>`-এ। **নিয়ম: দাতব্য সাইটে কখনো বানানো সংখ্যা রাখা যাবে না।** (CSS ও i18n-এর অব্যবহৃত `impact-*` কি-গুলো রেখে দেওয়া হয়েছে — ক্ষতি নেই।)
- **ছয়টি কার্ড:** সর্বমোট দান · অপারেশন ফান্ড · রিভলভিং ফান্ড · মোট সঞ্চয় জমা · ঋণ বিতরণ · ঋণ আদায়। কার্ডে ক্লিক করলে **শুধু সেই বিষয়ের** বিস্তারিত পর্দা (`KHUI.statsDetail(topic)`) — হেডলাইন কার্ড, ১২ মাসের বার চার্ট ও ভাগ-ভিত্তিক টেবিল।
- **⚠️ দুই তহবিল দানের শতকরা অংশ থেকে (ব্যবহারকারীর সিদ্ধান্ত)।** প্রতিটি **নিশ্চিত** দানের ডিফল্ট ৫% অপারেশন ফান্ডে ও আরও ৫% রিভলভিং ফান্ডে — অর্থাৎ দানের মোট ১০% আলাদা হয়। হিসাবটি **সংরক্ষণ করা হয় না, প্রতিবার কষা হয়** (`round(মোট দান × হার / 100, 2)`), তাই হার বদলালে পুরনো কোনো তথ্য হারায় না।
  হার বদলানো: সুপার অ্যাডমিন → **জেনারেল সেটিংস → "তহবিলের হার"** (`app_settings.fund_rules` = `{operation_pct, revolving_pct}`); ০–১০০-এর বাইরে গেলে ক্লায়েন্ট ও সার্ভার দুই জায়গাতেই ৫ ধরে নেয়।
- **টেবিল `fund_ledger`** (নতুন টেবিল নয় — পুরনোটিই বাড়ানো হয়েছে): নতুন কলাম `fund` (`operation`/`revolving`), আর `type` এখন `expense` · `adjustment` · `income`। **আয় = শতকরা অংশ + `income` এন্ট্রি; ব্যয় = `expense` + `adjustment`; স্থিতি = আয় − ব্যয়।**
  ⚠️ **দুটি পরস্পরবিরোধী চেক কনস্ট্রেইন্ট একসাথে ছিল** — পুরনো `fund_ledger_type_check` (`donation_in/loan_out/repayment_in/expense/reserve/transfer`) আর নতুন `fund_ledger_type_chk`; দুটোর ছেদ কেবল `'expense'`, তাই `adjustment` লিখতে গেলে নীরবে আটকে যেত (পরীক্ষায় ধরা পড়ে)। এখন একটিই চেক, পুরনো মানসহ। একইভাবে `category` চেকে `office` ও `salary` যোগ করতে হয়েছে — নাহলে ফর্মের নতুন দুটি বিভাগ বাতিল হত।
  **নিয়ম: টেবিলে নতুন মান যোগ করার আগে `pg_constraint` দেখে নিতে হবে কোন চেক আগে থেকেই আছে।**
- **⚠️ কাঁচা `insert` নয়।** `authenticated`-এর INSERT/UPDATE/DELETE **প্রত্যাহার করা হয়েছে**; সব লেখা `record_fund_entry(fund, type, date, amount, description, category, ref, notes)` দিয়ে (SECURITY DEFINER, `is_admin()` যাচাই, অঙ্ক ও বিবরণ যাচাই)। তালিকা `admin_fund_entries(fund, limit)` — `admin only`।
- **RPC (anon-ও ডাকতে পারে):** `public_stats()` → দান/অপারেশন/রিভলভিং/সঞ্চয়/ঋণের সব যোগফল · `public_stats_detail(topic)` — topic: `donation` · `operation` · `revolving` · `savings` · `loan`; ফেরত দেয় `{title, subtitle, cards[], rows[], series{points[]}, as_of}`।
  ⚠️ **কোনো ব্যক্তির নাম, মোবাইল বা খরচের বিবরণ প্রকাশ্য RPC-তে যায় না** — কেবল ভাগ-ভিত্তিক যোগফল (মাধ্যম, স্কীম, বিভাগ, অবস্থা) ও মাসভিত্তিক ধারা। খরচের **বিবরণ** কেবল অ্যাডমিন প্যানেলে।
  ⚠️ "আজ" ও মাস সবসময় **ঢাকার তারিখ** (`at time zone 'Asia/Dhaka'`)।
  যাচাই (ভূমিকা পাল্টে): অতিথি → `public_stats` ✓, `public_stats_detail` ✓, কাঁচা `fund_ledger` **permission denied**, `admin_fund_entries` **admin only**; সাধারণ সদস্য → `record_fund_entry` **admin only**, কাঁচা insert **permission denied**; অ্যাডমিন → সবই ✓, ঋণাত্মক অঙ্ক ও ভুল তহবিল আটকেছে। অঙ্ক কাঁচা SQL-এর সাথে হুবহু মিলেছে (৫% + হাতে লেখা আয়)।
- **অ্যাডমিন প্যানেল:** সাইডবারে **"তহবিল ও খরচ"** (আগে "খরচ এন্ট্রি") — উপরে দুই তহবিলের অবস্থা, তারপর তহবিল+ধরন বেছে এন্ট্রি, নিচে ছাঁকনিসহ তালিকা। **নিয়ম মনে রাখা: নাম চার জায়গায় — সাইডবার, `PANEL_TITLES`, প্যানেল-শিরোনাম, পারমিশন-লেবেল।**
  ⚠️ ড্যাশবোর্ডের "মোট খরচ" কার্ড ও কার্যক্রম লগে `.eq('type','expense')` ছিল — `adjustment`/`income` কখনো গোনা হত না। এখন `.in('type', […])`। **(এটি তৃতীয়বার — `type` দিয়ে ছাঁকার আগে ডাটাবেজে গুনে দেখতে হবে।)**
- **⚠️ `.filter-pill`-এর কোনো CSS ছিল না** `dashboard.html`-এ (শুধু `savings-portal.html`-এ ছিল) — তাই গাঢ় অ্যাডমিন পর্দায় সঞ্চয়-অনুমোদন, পরিচয়পত্র, কমিটি ও নতুন তহবিল — সব প্যানেলের পিল ব্রাউজারের ডিফল্ট ধূসর বাটন দেখাত। এখন `.fpill`-এর পাশেই `.filter-pills`/`.filter-pill` যোগ করা হয়েছে।
- **দেখতে:** প্রতিটি কার্ড **নিজের গ্র্যাডিয়েন্ট বহন করে ও লেখা সাদা**, তাই দুই থীমেই এক। ব্রাউজারে মেপে দেখা কনট্রাস্ট — গোলাপি ৫.৪, সবুজ ৫.৯, বেগুনি ৭.৪, কমলা ৪.৯, নীল ৬.৯, লাল ৫.৫:১; ভেতরের ছোট ঘরে ৭.২–৯.৯:১।
  ⚠️ `.kh-st-sub` প্রথমে `rgba(255,255,255,.9)` ছিল — কমলা কার্ডে ৪.৩৩:১ নেমে যেত (ব্রাউজারে মেপে ধরা পড়ে), এখন পুরো সাদা।
  ⚠️ গ্রিড `minmax(320px, 1fr)` — ছয়টি কার্ড ৩+৩ সাজে; ২৬৮px দিলে ৪+২ হয়ে দ্বিতীয় সারিতে বড় ফাঁক পড়ত।
  ⚠️ বিস্তারিত পর্দার `z-index: 10020` — ক্যারোসেল লাইটবক্স (১০০০০), সিগনেচার পট্টি (১০০০৫), হোয়াটসঅ্যাপ (৯৯৯৯), গ্লাস নেভ (৯০০০), চ্যাট (৯৪০০) — সবার উপরে।
  ⚠️ `vEsc(KHUI.bn(x))` — **আগে বাংলা অঙ্ক, তারপর এসকেপ**। উল্টো করলে `&#39;`-এর ৩৯-ও বাংলা হয়ে `&#৩৯;` হয়ে যেত।
- **সীমা:** সব অঙ্ক প্রতিবার নতুন করে কষা হয় (কোনো ক্যাশ নেই)। সারি অনেক বাড়লে `public_stats()` ধীর হবে — তখন materialized view বা ৫ মিনিটের ক্যাশ লাগবে।

## 🔑 পাসওয়ার্ড রিসেট

- সদস্য: user-login → "পাসওয়ার্ড ভুলে গেছেন?" · অ্যাডমিন: admin-login → একই লিংক। ই-মেইল ঠিকানা আবশ্যক (মোবাইল alias-এ হয় না)।
- `resetPasswordForEmail(email, {redirectTo: /reset-password.html})` — দুই পথ: **৬ সংখ্যার কোড** (`verifyOtp type:'recovery'`, টেমপ্লেটে `{{ .Token }}` থাকলে) অথবা **ই-মেইলের লিংক**।
- `reset-password.html` নিজেই hash/query থেকে টোকেন নিয়ে `setSession()`/`exchangeCodeForSession()` করে (কারণ `_supabase.js`-এ `detectSessionInUrl:false`), তারপর `updateUser({password})`। মেয়াদোত্তীর্ণ লিংক ও সেশনহীন অবস্থাতেও বাংলা বার্তা দেখায়।

## 🗄️ গুরুত্বপূর্ণ DB ফাংশন

`create_user_profile`, `get_my_profile`, `update_my_profile` (প্রোফাইল না থাকলে নিজেই তৈরি করে — `p_mobile` লাগে),
`get_my_overview`, `touch_my_login`, `record_donation`, `record_loan_payment`, `get_my_loan_schedule`,
`create_depositor`, `set_my_savings_scheme`, `get_savings_overview`,
`set_app_setting` (super_admin only), `generate_due_reminders`,
`adjust_account_balance`, `get_balance_summary`, `verify_borrower*`, `approve_identity_claim`।
সবগুলোতে `SECURITY DEFINER` + `SET search_path = public` + `auth.uid()` যাচাই।
স্টোরেজ বাকেট: `avatars` (পাবলিক) ও `nid-docs` (প্রাইভেট — শুধু নিজে ও অ্যাডমিন)।

## 🤖 হাসানা সহায়িকা — চ্যাটবট (১৪ সেপ্টেম্বর ২০২৬)

**সিদ্ধান্তগুলো (ব্যবহারকারীর):** ইঞ্জিন **Google Gemini** (খরচের কারণে ১৪ সেপ্টেম্বর Claude থেকে বদলানো) · হোয়াটসঅ্যাপ বাটন ও চ্যাটবট **দুটি আলাদা বাটন** · এভাটার এখন **কণ্ঠস্বর + ছোট অ্যানিমেশন** (পরে সত্যিকারের ভিডিও এভাটার) · জ্ঞানভান্ডারে **গঠনতন্ত্র v3 ও কমিটির কাঠামো v3** — দুটোই বসানো হয়েছে।

### ✅ চালু আছে (১৪ সেপ্টেম্বর ২০২৬)

`GEMINI_API_KEY` বসানো হয়েছে, বট লাইভে কাজ করছে। কি-টি থাকে **Supabase Dashboard → Project Settings → Edge Functions → Secrets**-এ; **কখনো চ্যাটে বা কোডে লেখা যাবে না**। কি সরিয়ে নিলে বট ভদ্রভাবে বলে "এখনো সম্পূর্ণভাবে চালু হয়নি" — কিছু ভাঙে না।

**⚠️⚠️ মডেলের নাম পুরনো হলে ৪০৪ — চালুর দিনেই ধরা পড়েছে।** প্রথম কলে Google উত্তর দিয়েছিল: *"models/gemini-2.5-flash is no longer available to new users… use models/gemini-3.6-flash"*। বাইরে থেকে দেখাচ্ছিল কেবল "দুঃখিত, উত্তর তৈরি করতে পারছি না" — আসল কারণ জানা গেছে **Edge Function-এর লগ পড়ে**। এখন `chat_rules.gemini_model = 'gemini-3.6-flash'`।
**নিয়ম: বট চুপ হয়ে গেলে সবার আগে `function_logs`-এ `kh-chat model error` খুঁজতে হবে** — মডেলের নাম বদলানো, কোটা শেষ, কি অচল — তিনটিই ওখানে স্পষ্ট লেখা থাকে। মডেলের নাম `app_settings`-এ, তাই বদলাতে **রিডিপ্লয় লাগে না**।

**⚠️ বাংলায় টোকেন সীমা বড় লাগে।** `maxOutputTokens: 900` দিয়ে উত্তর **মাঝপথে কেটে যাচ্ছিল** (লাইভে ধরা পড়ে) — বাংলা লেখা ইংরেজির চেয়ে অনেক বেশি টোকেন খায়, আর Gemini 3-এর ভাবনার টোকেনও এই সীমাতেই গোনা হয়। এখন ডিফল্ট **৩,০০০** (`chat_rules.max_tokens`)। তবু কাটা পড়লে `finishReason ≠ STOP` ধরে উত্তরের শেষে "(উত্তরটি এখানেই শেষ হয়েছে…)" যোগ হয় — **কাটা উত্তর কখনো নিঃশব্দে দেওয়া হয় না**।

⚠️ **অর্গানাইজেশনাল (Workspace) অ্যাকাউন্টে কি নাও পাওয়া যেতে পারে।** Workspace অ্যাডমিন AI Studio বন্ধ করে রাখলে, অথবা প্রতিষ্ঠানের Cloud org প্রজেক্ট/কি তৈরিতে বাধা দিলে "permission denied" আসে। তখন সহজ পথ — **ব্যক্তিগত Gmail দিয়ে** AI Studio-তে ঢুকে কি নেওয়া। ফাউন্ডেশনের নিজস্ব বিলিং/কোটা চাইলে Google Cloud-এ আলাদা প্রজেক্ট খুলে **Vertex AI** ব্যবহার করতে হবে (তখন কোড বদলাতে হবে — Vertex-এর এন্ডপয়েন্ট ও OAuth আলাদা)।

⚠️⚠️ **ফ্রি টিয়ারে Google প্রম্পট মডেল উন্নয়নে ব্যবহার করতে পারে।** তাই ব্যক্তিগত আর্থিক তথ্য AI-তে পাঠানো বন্ধ করার সুইচ রাখা হয়েছে: `app_settings.chat_rules.share_personal = false` দিলে লগইন করা সদস্যের `profile_card()`/`my_savings_summary()` প্রম্পটে **বসেই না** (বট তখন ড্যাশবোর্ড দেখতে বলে)। পেইড টিয়ার বা Vertex AI-তে এই সীমাবদ্ধতা নেই।

### গঠন

- **Edge Function `kh-chat`** — `verify_jwt: false` (প্রকল্পের নিয়ম; ভেতরেই অনুমতি যাচাই)।
  • **ইঞ্জিন বাছাই নিজে থেকে:** `GEMINI_API_KEY` থাকলে Gemini (`generativelanguage.googleapis.com/v1beta/models/<model>:generateContent`, হেডার `x-goog-api-key`), নইলে `ANTHROPIC_API_KEY` থাকলে Claude, দুটোই না থাকলে ভদ্র বার্তা। **দুটো কোডপথই রাখা আছে** — কি বদলালেই ইঞ্জিন বদলায়, রিডিপ্লয় লাগে না।
  • মডেল: `chat_rules.gemini_model` (ডিফল্ট `gemini-2.5-flash`) ও `chat_rules.model` (Claude-এর জন্য)।
  • ⚠️ Gemini-তে বার্তার ভূমিকা `user`/**`model`** (`assistant` নয়), আর সিস্টেম প্রম্পট যায় `systemInstruction`-এ — Anthropic-এর `system` ফিল্ডের মতো নয়।
  • উত্তরে `engine` ও `kb` (কোন অধ্যায় ব্যবহৃত হলো) ফেরত আসে — সমস্যা ধরার জন্য।
- **🧪 শুকনো রান (dry run)** — `{"q":"…","dry":true}` পাঠালে মডেল ডাকা হয় **না**, কেবল ফেরত আসে কোন অধ্যায় বাছা হলো, প্রম্পট কত অক্ষরের, ব্যক্তিগত ব্লক বসল কি না। কোটা তবু গোনা হয় (অপব্যবহার ঠেকাতে)। **জ্ঞানভান্ডার বদলালে এটি দিয়ে যাচাই করতে হবে।**
- **⚠️⚠️ গোপনীয়তার সীমারেখা — সবচেয়ে গুরুত্বপূর্ণ অংশ।** ব্যক্তিগত তথ্য **কখনো service_role দিয়ে আনা হয় না**। লগইন থাকলে ব্রাউজার তার সেশন টোকেন হেডারে পাঠায়, আর Edge Function **ঐ টোকেন দিয়েই** সুপাবেস ক্লায়েন্ট বানিয়ে `profile_card()`, `my_savings_summary()` ও `my_activity()` ডাকে — অর্থাৎ `auth.uid()`/RLS-ই সীমা টানে। **তিনটি ফাংশনের একটিতেও কারো uid দেওয়ার প্যারামিটার নেই**, তাই একজনের তথ্য আরেকজনের কাছে যাওয়ার কোনো কোডপথই নেই। লগইন না থাকলে ব্যক্তিগত ব্লকটি সিস্টেম প্রম্পটে বসেই না — কেবল জ্ঞানভান্ডার ও `public_stats()`।

### 📊 `my_activity()` — নিজের সব লেনদেনের বিস্তারিত (১৪ সেপ্টেম্বর ২০২৬)

**⚠️ যে সমস্যাটি ধরা পড়েছিল (ব্যবহারকারীর পর্যবেক্ষণ):** "এই মাসে আমি কতবার দান করেছি?" জিজ্ঞেস করলে বট বলত *"সর্বমোট ১৩ বার … তবে কেবল এই চলতি মাসের আলাদা হিসাবটি আমার কাছে নেই"*। কারণ প্রম্পটে যেত কেবল `profile_card()`-এর **আজন্ম যোগফল** — কোনো তারিখ, কোনো মাসভিত্তিক ভাগ, কোনো লেনদেনের সারি ছিল না।

**সমাধান — `my_activity(p_months int default 24, p_rows int default 40)`** (SECURITY DEFINER, `search_path=public`, শুধু `authenticated`+`service_role`)। ফেরত দেয় একটি jsonb:

- `as_of` — ঢাকার আজকের তারিখ, চলতি ও গত মাসের নাম (`today_label`, `this_month_label` — বাংলা অঙ্কসহ)
- `donations` — `confirmed_total` · `pending_total` · `this_month` · `first/last_donation_on` · **`by_month`** (মাসভিত্তিক সংখ্যা ও অঙ্ক) · `recent` (তারিখ, অঙ্ক, মাধ্যম, ভাউচার নং, অবস্থা)
- `savings` — প্রতি হিসাবের জমা/উত্তোলন/স্থিতি · `this_month` · `total_balance` · `by_month` · `recent`
- `loans` — প্রতিটি ঋণের মূল, পরিশোধিত, বকেয়া, পরবর্তী কিস্তির তারিখ, বিলম্বিত কিস্তির সংখ্যা · `paid_this_month` · `repayments_recent`

**⚠️⚠️ সব যোগফল ও গণনা SQL-ই করে দেয় — মডেলকে কখনো হিসাব কষতে দেওয়া হয় না।** সিস্টেম প্রম্পটে স্পষ্ট লেখা: মাসভিত্তিক প্রশ্নে `this_month`/`by_month` থেকে সংখ্যা নিতে হবে, নিজে যোগ-বিয়োগ করা বা `recent`-এর সারি গুনে সংখ্যা বলা নিষিদ্ধ। **নিয়ম: আর্থিক প্রশ্নে LLM-কে কখনো পাটিগণিত করতে দেওয়া যাবে না — সংখ্যা তৈরি হবে ডাটাবেজে, মডেল কেবল পড়ে বলবে।**

- **⚠️ "নেই" আর "শূন্য" আলাদা।** `by_month`-এ যে মাস নেই সে মাসে লেনদেন হয়নি — প্রম্পটে বলা আছে তখন "শূন্য" বলতে হবে, "জানা নেই" নয়। যাচাই: "গত মাসে কত জমা দিয়েছি?" → "আগস্ট ২০২৬-এ ৳০", সাথে চলতি মাসের অঙ্কও।
- **আজকের তারিখ প্রম্পটে বসে** (`Asia/Dhaka`) — নাহলে "এই মাস" মানে মডেলের প্রশিক্ষণের সময় ধরে নিত।
- **`bn_month_label(date)`** — নতুন immutable হেল্পার, "সেপ্টেম্বর ২০২৬" (বাংলা অঙ্কে সাল) দেয়। নতুন কোথাও মাসের নাম লাগলে এটিই ব্যবহার করতে হবে।
- **মাপ:** `chat_rules.activity_months` (২৪) ও `activity_rows` (৪০) দিয়ে নিয়ন্ত্রিত — সারি বাড়লে প্রম্পটও ভারী হয়।
- **`my_savings_summary()` রাখা হয়েছে** — তাতে বকেয়া কিস্তির হিসাব আছে যা `my_activity`-তে নেই; দুটোই পাঠানো হয়।

**যাচাই — লাইভে, সত্যিকারের লগইন সেশনে (uid `b8cff1c2…`):**

| প্রশ্ন | উত্তর | SQL-এর সাথে মিল |
|---|---|---|
| এই মাসে কতবার দান করেছি? | "১৩ বার, ৳১,৭০,৯৫০" | ✅ হুবহু |
| সর্বশেষ তিনটি দান? | ১২ সেপ্টে ৳৫০,০০০ (ব্যাংক) · ১১ সেপ্টে ৳১,৩০০ (বিকাশ) · ১০ সেপ্টে ৳১৭,০০০ (বিকাশ) | ✅ হুবহু |
| সঞ্চয় হিসাব কয়টি, স্থিতি কত? | ৩টি, ৳২৭,৮৫০ (০০০১ ৳২৭,৩৫০ · ০০০৭ ৳৫০০ · ০০০৮ ৳০) | ✅ যোগফল মেলে |
| গত মাসে কত জমা? | "আগস্ট ২০২৬-এ ৳০" | ✅ সত্যিই শূন্য |

**গোপনীয়তা যাচাই:** ভূমিকা পাল্টে — Mitali সেজে ডাকলে **কেবল তাঁরই** ৯টি দান/৳২,২২,০০০ আসে; `anon` ডাকলে `permission denied for function my_activity`। লাইভে — অতিথি একই প্রশ্ন করলে "লগইন করুন", আর লগইন থাকা অবস্থায় **অন্য সদস্যের নাম ধরে** তথ্য চাইলে বট অস্বীকার করে (তাঁর তথ্য প্রম্পটে আদৌ যায়নি — এটিই আসল সুরক্ষা, মডেলের ভদ্রতা নয়)।
- **জ্ঞানভান্ডার `chat_knowledge`** — অ্যাডমিন প্যানেল → **"চ্যাটবটের জ্ঞানভান্ডার"** (সুপার অ্যাডমিন) থেকে সম্পাদনাযোগ্য, রিডিপ্লয় লাগে না। এখন **২৯টি নথি**:
  • `sort_order < ১০০` — **মূল নথি, সবসময় প্রম্পটে যায়** (৫টি, ~৭,৭০০ অক্ষর): সংজ্ঞা ও ইসলামি ভিত্তি (১০) · অর্থায়নের উৎস ও আখুওয়াত মডেল (২০) · ওয়েবসাইটের সেবা (৩০) · প্রতিষ্ঠান ও যোগাযোগ (৪০) · **কমিটির কাঠামো — সারসংক্ষেপ ও মূল ব্যক্তিত্ব (৪৫)**।
  • `sort_order ≥ ১০০` — লম্বা দলিল, প্রশ্ন অনুযায়ী বাছা হয়: **গঠনতন্ত্র v3** `১০০` মুখবন্ধ+সংশোধনীর সারসংক্ষেপ+সূচিপত্র, `১০১…১১৯` = অধ্যায় ১–১৯ (দ্বিভাষিক, হুবহু, ~৭৫,০০০ অক্ষর); **কমিটির কাঠামো v3** `১২১…১২৪` = চারটি কমিটির পূর্ণ তালিকা ও দায়িত্ব (~১৩,৫০০ অক্ষর)।
  • **⚠️ "সভাপতি কে?" ধরনের প্রশ্ন সবচেয়ে বেশি আসে**, তাই কমিটির **সারসংক্ষেপ ও মূল ব্যক্তিত্ব** অংশটি ইচ্ছে করেই মূল নথিতে (৪৫) রাখা — প্রতিবারই প্রম্পটে যায়, বাছাইয়ের উপর নির্ভর করতে হয় না। বিস্তারিত দায়িত্ব-তালিকা বাছাই-নির্ভর।
  • **⚠️ নামের বানান জ্ঞানভান্ডারেই লেখা আছে** (৪৫ নম্বর নথির শেষে English = বাংলা তালিকা)। নাহলে মডেল নিজে থেকে উচ্চারণ করত ও একই উত্তরে "আমীমুল" আর "আমিনুল" দুরকম লিখত (পরীক্ষায় ধরা পড়ে)। **নতুন কারো নাম যোগ হলে ঐ তালিকাতেও বসাতে হবে।**
  • **⚠️ `[Name]` মানে পদটি খালি** — সিস্টেম প্রম্পটে স্পষ্ট বলা আছে কোনো নাম বানানো যাবে না। যাচাই: "কোষাধ্যক্ষ কে?" → "পদটি খালি রয়েছে"।
  • **⚠️ পুরো গঠনতন্ত্র প্রতিবার পাঠানো হয় না** — প্রতি প্রশ্নে কেবল প্রাসঙ্গিক অধ্যায়গুলো যায় (`chat_rules.kb_budget`, ডিফল্ট ১৬,০০০ অক্ষর); **অধ্যায়ের শিরোনামের তালিকা সবসময়ই যায়**, তাই না-পাঠানো অধ্যায়ের কথাও বট জানে ও "ঐ অধ্যায়ে আছে" বলতে পারে।
  ⚠️ **`.docx` পড়ার কৌশল (বাশ স্যান্ডবক্স বন্ধ থাকায়)** — ৮ সেপ্টেম্বরের উইন্ডোজ আপডেটের পর `extract-text` চালানো যায় না। সমাধান: লাইভ পেজে JSZip ঢুকিয়ে, ব্রাউজারের ফাইল-ইনপুটে `.docx` আপলোড করে `word/document.xml` থেকে লেখা তোলা হয়েছে, তারপর একটি **সাময়িক** `security definer` ফাংশন দিয়ে সরাসরি ডাটাবেজে পাঠিয়ে SQL-এ অধ্যায় ভাগ করা হয়েছে (কাজ শেষে ফাংশন ও স্টেজিং টেবিল **ড্রপ করা হয়েছে**)। ভবিষ্যতে বড় ডকুমেন্ট পড়াতে হলে এই পথটিই সবচেয়ে সহজ।
- **কোটা `chat_usage`** — দিনে অতিথি ২৫, সদস্য ৬০, আর **GLOBAL ১২০০**। ⚠️ GLOBAL ছাদটি ইচ্ছে করেই: এন্ডপয়েন্ট প্রকাশ্য। Gemini ফ্রি টিয়ারেও দিনে অনুরোধের সীমা আছে, তাই ছাদটি রাখাই ভালো। সব সংখ্যা `app_settings.chat_rules`-এ বদলানো যায় — `{enabled, gemini_model, model, daily_guest, daily_user, daily_global, kb_budget, max_tokens, activity_months, activity_rows, share_personal}`; `enabled:false` দিলে বট ভদ্রভাবে বন্ধ হয়।
- **ক্লায়েন্ট** — `KHUI.mountAssistant()`, `boot()` থেকে নিজেই চলে। বন্ধ করতে `<body data-kh-bot="off">` (অ্যাডমিন পেজ দুটিতে দেওয়া আছে)। বাটন `.kh-bot-launch` (৯৩০১) হোয়াটসঅ্যাপ চ্যাটের (৯৩০০) **১৮px উপরে**; প্যানেল ৯৪০১।
- **এভাটার** — ব্রাউজারের নিজস্ব `speechSynthesis` দিয়ে কথা বলে। **কোনো সেবা, খরচ বা API লাগে না।** মিউট বাটন `localStorage.kh_bot_mute`-এ মনে রাখে। ছবির বিস্তারিত নিচের "🧕 এভাটার" অংশে, কণ্ঠের বিস্তারিত "🔁 অডিওর বিনিময়ে অডিও" অংশে।
- **⚠️ উত্তর সবসময় `textContent`-এ বসে**, কখনো `innerHTML`-এ নয় — মডেলের লেখা সরাসরি DOM-এ ঢোকানো XSS-এর পথ।

### 🧕 এভাটার — ব্যবহারকারীর দেওয়া ছবি (১৫ সেপ্টেম্বর ২০২৬)

**⚠️⚠️ প্রথম ছবিটি বাতিল হয়েছিল — জলছাপ।** ব্যবহারকারী প্রথমে যে ছবিটি দেন সেটি ছিল **Dreamstime-এর জলছাপযুক্ত প্রিভিউ কপি** — বাটনে জলছাপের লেখা দেখা যেত ও লাইসেন্সের ঝুঁকি থাকত। জানানোর পর তিনি **জলছাপবিহীন একটি পরিষ্কার ছবি** দেন; সেটিই এখন বসানো (মাঝে সাময়িকভাবে একটি মৌলিক SVG আঁকা হয়েছিল, এখন আর ব্যবহৃত নয়)।
**নিয়ম: সাইটে কোনো ছবি বসানোর আগে জলছাপ/লাইসেন্স দেখে নিতে হবে। স্টক প্রিভিউ কখনো নয়।**

- **ছবিটি কোথায়:** Supabase স্টোরেজের **পাবলিক বাকেট `site-assets`**, পথ `bot/hasana-avatar.jpg` (২২৪px, **১০ KB**) ও `bot/hasana-avatar@2x.jpg` (৪৪৮px, **২৮ KB**)। CSS-এ `image-set()` দিয়ে রেটিনায় ২× আসে, ফলব্যাক হিসেবে আগে সাধারণ `url()` লেখা আছে।
- **⚠️ মূল ফাইলটি ছিল ১,৯৬৫ KB** — প্রতিটি পেজে বসানোর জন্য অসম্ভব ভারী। তাই ব্রাউজারেই ক্যানভাসে মাথা-কাঁধ বরাবর বর্গাকারে কাটা হয়েছে (মূলের **x ৯%, y ১% থেকে ৬৪%** — চারটি ক্রপ পাশাপাশি রেখে চোখে দেখে বাছা) ও JPEG-এ নামানো হয়েছে। **ক্রপ বদলাতে হলে ঐ তিনটি সংখ্যা বদলালেই হবে।**
- **⚠️ ফাইলটি প্রকল্পের `images/` ফোল্ডারে নেই — কেন।** বাশ স্যান্ডবক্স (৮ সেপ্টেম্বরের উইন্ডোজ আপডেটের পর) বন্ধ, তাই বাইনারি ফাইল কপি করা যায় না; আর base64 টুলের ভেতর দিয়েও আনা যায় না (`BLOCKED: Base64 encoded data`)। তাই ছবিটি **পেজ থেকেই সরাসরি** স্টোরেজে আপলোড করা হয়েছে — কোনো ধাপ ব্যবহারকারীকে করতে হয়নি।
  **ভবিষ্যতে স্থানীয় করতে চাইলে:** ফাইলটি `images/`-এ রেখে `kh-ui.css`-এর `.kh-av`-এর দুটি URL আপেক্ষিক পথে বদলে দিলেই হবে; `push-to-github.bat` পুরো `images\` ফোল্ডার কপি করে, তাই স্ক্রিপ্টে কিছু যোগ করতে হবে না।
- **⚠️ বাকেটে লেখার অনুমতি নেই।** আপলোডের জন্য `site_assets_tmp_write`/`_tmp_update` নামে দুটি সাময়িক পলিসি বসিয়ে, কাজ শেষে **ড্রপ করা হয়েছে**। এখন কেবল `select` (পাবলিক) আছে। যাচাই: anon আপলোডে `new row violates row-level security policy`।
- **কোথায় বসে:** `kh-ui.js`-এর `khAvatarHTML()` — কেবল ঘর ও ব্যাজের মার্কআপ; **ছবিটি CSS-এ** (`.kh-av`-এর `background-image`)। দুই জায়গায়: লঞ্চ বাটন ও প্যানেলের মাথা।
- **⚠️ ছবি বলে ঠোঁট নড়ানো যায় না** — তাই "কথা বলছে" বোঝানো হয় **দুইভাবে একসাথে**: ① `khAvNod` — মাথা-কাঁধের নরম দোলা (translateY + সামান্য rotate + scale), ② `.kh-av-wave` — কোণে তিন দণ্ডের শব্দতরঙ্গ ব্যাজ। একটিমাত্র ইঙ্গিত যথেষ্ট স্পষ্ট হত না।
- **শোনার সময়** (`.kh-listening`) — এভাটারের চারপাশে **লাল স্পন্দন** (`khAvListen`), অর্থাৎ মাইক চালু আছে তা এক নজরে বোঝা যায়।
- **⚠️ `.kh-bot-face`-এ `overflow: hidden` দেওয়া যাবে না** — দিলে শব্দতরঙ্গের ব্যাজটি কেটে যায়। ছবিটি `.kh-av`-এর নিজের `border-radius: 50%` দিয়েই গোল হয়।
- **⚠️ `.kh-av`-এর উপর `background` shorthand লেখা যাবে না** — একবার `.kh-bot-launch .kh-av { background: #fff4ea }` লেখা ছিল, সেটি `background-image` (অর্থাৎ পুরো ছবিটাই) মুছে দিত। রঙ দিতে হলে `background-color`।
- **⚠️ `.kh-speaking` বাটন ও প্যানেল — দুটোতেই বসে** (`talking(on)` হেল্পার), কারণ বাটনের এভাটারও কথা বলে।
- **লঞ্চ বাটন:** `62px`, `padding: 3px` — ব্র্যান্ড গ্র্যাডিয়েন্ট একটি **রিং** হিসেবে দেখা যায়, ভেতরে ছবির নিজস্ব ক্রিম পটভূমি। তাই **দুই থীমেই হুবহু এক**।
- **যাচাই (লাইভ পেজে নতুন কোড বসিয়ে):** তিন অবস্থাই বড় করে দেখা হয়েছে (সাধারণ · কথা বলছে · শুনছে); `image-set` প্রয়োগ হয়েছে (computed style মিলিয়ে দেখা); লাইট থীমে বাটনটি সবুজ হোয়াটসঅ্যাপ বাটনের উপরে আলাদা হয়ে থাকে, ফাঁক ১২px — **কোনো ওভারল্যাপ নেই**।

### 🎙️ কণ্ঠে কথা বলা — দুই মোড (১৫ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর সিদ্ধান্ত:** মাইক বাটন **ও** হ্যান্ডস-ফ্রি — দুটোই, টগল দিয়ে।

- **প্রযুক্তি:** Web Speech API (`SpeechRecognition` / `webkitSpeechRecognition`), ভাষা `bn-BD` (`window.KH_VOICE_LANG` দিয়ে বদলানো যায়)। **সম্পূর্ণ ব্রাউজারের ভেতরে — কোনো সার্ভার, খরচ বা API কি লাগে না**, Gemini-র কোটাও খরচ হয় না।
- **⚠️ ব্রাউজার না চিনলে বাটন দুটি বসানোই হয় না** (`hidden` থাকে, `if (SR)` হলে তবেই খোলে) — Firefox-এ নিষ্ক্রিয় বাটন দেখিয়ে বিভ্রান্ত করা হয় না।
- **মাইক বাটন** (`.kh-bot-mic`, লেখার ঘরের বাঁয়ে): চাপলে শোনা শুরু, বলতে বলতেই লেখা ইনপুটে বসে (`interimResults`), **থামলেই নিজে থেকে পাঠিয়ে দেয়**। শোনার সময় বাটন লাল ও স্পন্দিত, নিচের নোট বদলে "🎙️ শুনছি…" হয়।
- **হ্যান্ডস-ফ্রি টগল** (`.kh-bot-hf`, মাথায় মিউটের পাশে): চালু থাকলে বট উত্তর **বলা শেষ করার পর নিজে থেকেই আবার শোনা শুরু** করে — টানা কথোপকথন। `localStorage.kh_bot_hf`-এ মনে রাখে। চালু করলেই সাথে সাথে শোনা শুরু হয় (ওটি ব্যবহারকারীর ক্লিক, তাই মাইকের অনুমতি চাওয়াও সেখানেই স্বাভাবিক)।
- **⚠️ কণ্ঠে করা প্রশ্নের উত্তর কেবল কণ্ঠেই যায়** — বিস্তারিত নিচের "🔁 অডিওর বিনিময়ে অডিও" অংশে।
- **⚠️⚠️ বট কথা বলার সময় কখনো শোনা হয় না।** নাহলে মাইক নিজের কণ্ঠস্বরই ধরত এবং প্রশ্ন→উত্তর→প্রশ্ন অনন্ত লুপ তৈরি হত। তাই শোনা শুরু হয় **কেবল `afterSpeak()` থেকে**, অর্থাৎ `speechSynthesis`-এর `onend`-এর পর; আর `ask()`-এর একদম শুরুতে `stopListen()`।
- **⚠️ `ask()` দুবার ডাকা ঠেকানো** — `r.onend`-এ লেখা থাকলে একবারই `ask()` হয়, তারপর `heard` খালি করা হয়। যাচাইয়ে পূর্ণ চক্রে `askCount: 1`।
- **অনুমতি না পেলে** (`not-allowed`): হ্যান্ডস-ফ্রি নিজে থেকে বন্ধ হয় ও চ্যাটে বাংলা নির্দেশনা আসে — "ঠিকানা বারের পাশের 🔒 আইকনে গিয়ে মাইক্রোফোন Allow করুন"। কিছু শুনতে না পেলে (`no-speech`) আলাদা ভদ্র বার্তা।
- **প্যানেল বন্ধ = মাইকও বন্ধ** — `close()`-এ `stopListen()`; `startListen()` প্যানেল খোলা না থাকলে চলে না।
- **যাচাই (লাইভ পেজে নতুন কোড বসিয়ে):**
  • কনট্রাস্ট, **ডার্ক ও লাইট দুই থীমেই হুবহু এক** — মাইক ৭.৯৯:১, শোনার সময় ৪.৮৩:১, হ্যান্ডস-ফ্রি (চালু/বন্ধ) ৯.১৯:১, নোট ৫.৬৬:১, "শুনছি" নোট ৬.৪৭:১ — সবই AA পাস।
  • ৩৭৫×৮১২: বট বাটন `y 600–656`, হোয়াটসঅ্যাপ `y 674–728` — **১৮px ফাঁক, কোনো ওভারল্যাপ নেই**, আড়াআড়ি স্ক্রল নেই।
  • অবস্থা-যন্ত্রটি নকল `SpeechRecognition` দিয়ে চালিয়ে দেখা হয়েছে: মাইক → শোনা → এক প্রশ্ন → উত্তর → বলার সময় শোনা বন্ধ → বলা শেষে **আবার শোনা শুরু**; প্যানেল বন্ধ করলে সব থেমে যায়।
  ⚠️ **পরীক্ষার ফাঁদ:** ব্রাউজার প্যানেলটি লুকানো থাকলে (`document.hidden`) Chrome `setTimeout` থ্রটল করে — নেস্টেড ১০ms টাইমার সেকেন্ডখানেক পরে চলে। প্রথম রানে তাই মনে হয়েছিল হ্যান্ডস-ফ্রি আবার শোনে না; লম্বা অপেক্ষা দিয়ে চালালে ঠিকই চলে। **এটি বাগ নয়, পরীক্ষার artifact** (নেভবারের সময়ও একই ফাঁদে পড়া হয়েছিল)।

### 🔁 অডিওর বিনিময়ে অডিও, লেখার বিনিময়ে লেখা (১৫ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর নির্দেশ (হুবহু):** *"অডিওর বিনিময়ে অডিও লেখা নয় আর লেখার বিনিময়ে লেখা অডিও নয়।"* সাথে: **"বাই ডিফল্ট সব ক্ষেত্রে সফট ফিমেল ভয়েস"**।

- **`ask(mode)` — একটিই ফাংশন, দুই পথ।** `mode` আসে কে ডাকল তার উপর:
  • `'voice'` — কেবল `rec.onend` থেকে (কণ্ঠে বলা শেষ হলে)
  • `'text'` — পাঠান বাটন, Enter, ও পরামর্শের চিপ
  **⚠️ নতুন কোনো জায়গা থেকে `ask()` ডাকলে mode দিতেই হবে**, নাহলে লেখার প্রশ্নেও কণ্ঠ চলে যেতে পারে।
- **কণ্ঠে প্রশ্ন →** পর্দায় বসে কেবল **প্রশ্নটি** (মাইক ঠিক শুনেছে কি না বোঝার জন্য — ব্যবহারকারীর পছন্দ), উত্তরের বুদবুদ **আসে না**; উত্তর কেবল কণ্ঠে। উত্তরের জায়গায় এক লাইনের ফিকে সূচক `.kh-bot-vrep` — *"কণ্ঠে উত্তর দেওয়া হয়েছে — লেখায় দেখুন"*; চাপলে ঐ উত্তরটিই সাধারণ বুদবুদ হয়ে বসে।
- **লিখে প্রশ্ন →** শুধু বুদবুদ, **`speak()` ডাকাই হয় না**।
- **⚠️ কণ্ঠ না চললে উত্তর হারাতে দেওয়া হয় না।** মিউট থাকলে, `speechSynthesis` না থাকলে, বা ২ সেকেন্ডেও কণ্ঠ শুরু না হলে (`guard` টাইমার) উত্তরটি নিজে থেকেই লেখায় বসে যায়। মিউট এখন কার্যত "নীরব মোড" — কণ্ঠের প্রশ্নেরও উত্তর লেখায় আসে।
- **ত্রুটির বার্তা সবসময় লেখায়** — ওটি উত্তর নয়, তাই নিয়মের বাইরে।

#### ⚠️⚠️ `ttsClean()` — পড়ার আগে লেখা পরিষ্কার করা বাধ্যতামূলক

ব্যবহারকারীর পর্যবেক্ষণ: *"নানা ধরনের ক্যারেক্টারকে পরতে গিয়ে অদ্ভুদ সব উচ্চারণ করে।"* মডেলের উত্তরে মার্কডাউন থাকে (`**`, `*`, `` ` ``, `#`, `-` বুলেট), ইমোজি থাকে, লিংক থাকে — TTS প্রতিটি চিহ্নকেও উচ্চারণ করার চেষ্টা করে। `ttsClean()` ক্রম মেনে সরায়:

কোড ব্লক বাদ → `[লেখা](লিংক)` → শুধু লেখা → URL ও ই-মেইল → "লিংক"/"ই-মেইল ঠিকানা" → ইমোজি → লাইনের শুরুর বুলেট → `৳`→"টাকা", `%`→"শতাংশ", `&`→"এবং" → বাকি চিহ্ন (`*_\`~#>|[](){}<>^=+\/•·→…`) → ফাঁকা → পুনরাবৃত্ত যতিচিহ্ন → স্পেস মেলানো।

**নিয়ম: কণ্ঠে কিছু পড়ানোর নতুন কোনো কোড লিখলে কাঁচা লেখা সরাসরি `SpeechSynthesisUtterance`-এ দেওয়া যাবে না — `ttsClean()` দিয়ে নিতে হবে।**

#### ⚠️ লম্বা উত্তর টুকরো করে বলা (`ttsChunks`)

ক্রোম এক টানে লম্বা লেখা বললে মাঝপথে কেটে যায়। তাই বাক্যের শেষে (`।.!?`) ভেঙে **সর্বোচ্চ ১৮০ অক্ষরের** টুকরো বানিয়ে পরপর বলা হয়। `speakSeq` নামের একটি গণনা রাখা হয় — নতুন কথা শুরু হলে, মাইক চালু হলে, মিউট চাপলে বা প্যানেল বন্ধ হলে সেটি বাড়ে ও **বাকি টুকরোগুলো নিজে থেকেই থেমে যায়** (নাহলে পুরনো উত্তরের লেজ বাজতে থাকত)।
পুরনো কোডে `String(text).slice(0, 700)` ছিল — লম্বা উত্তর **কেটে** যেত; সেটি বাদ।

#### 🎙️ কণ্ঠ বাছাই — `pickVoice()`

স্কোর দিয়ে বাছা হয়, প্রথমটি নেওয়া হয় না: বাংলা **+১০০** · হিন্দি +৪০ · en-IN +২৬ · অন্য ইংরেজি +১০ · বাকি −৪০; নামে নারীকণ্ঠের ইঙ্গিত **+৫০**, পুরুষকণ্ঠের −৩০; Google +৬।
**⚠️ ভাষা আগে, লিঙ্গ পরে (ব্যবহারকারীর সিদ্ধান্ত):** বাংলা নারীকণ্ঠ না থাকলে **বাংলা পুরুষকণ্ঠ** নেওয়া হয়, ইংরেজি নারীকণ্ঠ নয় — ভুল ভাষায় বাংলা পড়া শোনাই যায় না।
নরম শোনাতে `rate 0.95`, `pitch 1.12`।
হাতে বদলাতে: `KHUI.botVoices()` তালিকা দেখায়, `KHUI.botVoice('নাম')` বেছে দেয় (`localStorage.kh_bot_voice`), `window.KH_TTS_VOICE`-ও কাজ করে।
⚠️ `getVoices()` প্রথমবার ফাঁকা আসতে পারে, তাই ফল **ক্যাশ করা হয় না** — প্রতিবার বাছা হয়।

**যাচাই (লাইভ পেজে আসল নতুন `kh-ui.js` আপলোড করে চালিয়ে, নকল `SpeechSynthesis`/`SpeechRecognition` দিয়ে):**

| পরীক্ষা | ফল |
|---|---|
| লিখে প্রশ্ন | বুদবুদ ২টি (প্রশ্ন+উত্তর), **কণ্ঠ ০ বার** ✅ |
| কণ্ঠে প্রশ্ন | কেবল প্রশ্নের বুদবুদ, **উত্তরের বুদবুদ ০**, সূচক ১টি ✅ |
| পরিষ্কার হওয়া লেখা | `**সালাম!** … ৳১,৭০,৯৫০ … https://… ✅` → `সালাম! … টাকা ১,৭০,৯৫০ … লিংক …` ✅ |
| কণ্ঠ | `Microsoft Nabanita (bn-BD)` — নারী, rate .95, pitch 1.12 ✅ |
| বাংলা নারীকণ্ঠ সরালে | `Bashkar (bn-IN)` — বাংলা পুরুষ ✅ (Zira নয়) |
| বাংলাই না থাকলে | `Zira (en-US)` ✅ |
| মিউট + কণ্ঠে প্রশ্ন | কণ্ঠ ০, **উত্তর লেখায়** ✅ |
| ১,২৭৮ অক্ষরের উত্তর | ১৪ টুকরো, জোড়া লাগালে **হুবহু মূল লেখা** ✅ |
| "লেখায় দেখুন" চাপা | সূচক সরে উত্তরের বুদবুদ বসে ✅ |
| হ্যান্ডস-ফ্রি | টগলেই শোনা, বলা শেষে **আবার শোনা** ✅, উত্তরের বুদবুদ ০ ✅ |
| কনট্রাস্ট (`.kh-bot-vrep`) | লেখা **৭.৪৮:১**, আইকন **৫.০৭:১** — দুই থীমে হুবহু এক ✅ |

⚠️ **আবারও সেই ফাঁদ:** লুকানো ট্যাবে Chrome `setTimeout` সেকেন্ডে একবারে নামিয়ে দেয়, তাই নকল synthesis টাইমার দিয়ে লিখলে ১৪ টুকরোর মধ্যে ২–৩টি গোনা পড়ত — মনে হচ্ছিল উত্তর কেটে যাচ্ছে। নকলটিকে সিঙ্ক্রোনাস করলে ১৪/১৪ আসে। **এটি বাগ নয়, পরীক্ষার artifact** (তৃতীয়বার এই ফাঁদে পড়া হলো)।

### ⚠️ যে ফাঁদটি ধরা পড়েছে — `revoke ... from public` যথেষ্ট নয়

`chat_quota()` বানিয়ে `revoke execute … from public` করার পরেও **anon সেটি ডাকতে পারছিল** (পরীক্ষায় ধরা পড়ে)। কারণ Supabase-এর ডিফল্ট প্রিভিলেজ `anon` ও `authenticated` রোলকে **নাম ধরে** EXECUTE দেয় — PUBLIC থেকে revoke করলে ঐ আলাদা গ্র্যান্টগুলো থেকেই যায়। তার উপর **`create or replace function` করলে ডিফল্ট গ্র্যান্ট আবার ফিরে আসে**, তাই রিপ্লেসের পর আবার revoke করতে হয়।
**নিয়ম:** `revoke execute on function … from public, anon, authenticated;` — তিনটিই নাম ধরে লিখতে হবে, আর প্রতিবার ফাংশন রিপ্লেস করার পর আবার চালাতে হবে।
(ঝুঁকিটি বাস্তব ছিল: anon সরাসরি `chat_quota` ডেকে GLOBAL কাউন্টার ফুলিয়ে দিনের জন্য সবার চ্যাট বন্ধ করে দিতে পারত।)

### যাচাই (ভূমিকা পাল্টে, ডাটাবেজে)

অতিথি → `chat_knowledge` **permission denied**, `chat_usage` **permission denied**, `chat_quota()` **permission denied**, `admin_kb_list()` **permission denied**; সাধারণ সদস্য → `chat_quota()` **permission denied**, `admin_kb_list()` **super admin only**; service_role → `chat_quota()` ✅ (Edge Function এই পথেই চলে)।
Edge Function লাইভে ডেকে দেখা হয়েছে — CORS পাস, কোটা চলে, জ্ঞানভান্ডার পড়ে, আর কি না থাকায় ভদ্র বার্তা ফেরত দেয়।
লেআউট (৩৭৫×৮১২): বট বাটন হোয়াটসঅ্যাপের ১৮px উপরে, **কোনো ওভারল্যাপ নেই**, প্যানেল ভিউপোর্টের ভেতরে, গ্লাস নেভের সাথেও সংঘর্ষ নেই, আড়াআড়ি স্ক্রল নেই।

### ⚠️⚠️ বাংলা শব্দ মেলানোর দুটি ফাঁদ (১৪ সেপ্টেম্বর ২০২৬ — পরীক্ষায় ধরা পড়েছে)

অধ্যায় বাছাইয়ের প্রথম সংস্করণ **প্রায় কিছুই খুঁজে পেত না** — ১০টি প্রশ্নের ৬টিতেই ফলব্যাক সূচিপত্র আসত, আর "ঋণ পরিশোধ" প্রশ্নে ভুল করে **অধ্যায় ৩** আসত। দুটি কারণ:

1. **`\p{L}` বাংলা কার/ফলা (মাত্রা) ধরে না** — সেগুলো ইউনিকোডে Mark (`\p{M}`)। তাই `q.replace(/[^\p{L}\p{N}]+/gu, " ")` লিখলে **"অনুদান" হয়ে যায় "অনদান"**, "সঞ্চয়" হয়ে যায় "সঞচয়" — মূল লেখার সাথে আর কোনো শব্দই মেলে না। শুধু যেসব খণ্ড আকস্মিকভাবে সাবস্ট্রিং হিসেবে মিলত, সেগুলোই স্কোর পেত — তাই ভুল অধ্যায় আসত। **নিয়ম: বাংলা লেখা পরিষ্কার করার যেকোনো regex-এ `\p{M}` অবশ্যই রাখতে হবে।**
2. **বিভক্তি শব্দের সাথে জোড়া থাকে** — "সঞ্চয়ের" ≠ "সঞ্চয়", "কমিটির" ≠ "কমিটি"। তাই `stem()` শেষের বিভক্তি (গুলোকে/দের/য়ের/ের/তে/কে/টি/র/ে/ই) কেটে দ্বিতীয়বার কম ওজনে মেলায়।

স্কোর: শিরোনামে পূর্ণ শব্দ ×৬, মূল লেখায় ×২, stem শিরোনামে ×৩ ও লেখায় ×১; ন্যূনতম ৬ না পেলে নেওয়া হয় না। "ধারা ১৪.২" / "অধ্যায় ৭" / "article 7.5" স্পষ্ট করে বললে ঐ অধ্যায় **জোর করে** নেওয়া হয় (+১০০০)।

**যাচাই (সংশোধনের পর, লাইভ এন্ডপয়েন্টে ১০টি প্রশ্নের dry run):** ঋণ→৭ · সঞ্চয়ের স্কীম→৮ · ধারা ১৪.২→১৪ · কমিটি→৪ · বিদেশী অনুদান→৯ · সদস্যপদ→৩ · ভাউচার→১২ · নিরীক্ষা→১০ · সংশোধন→১৭ · "hello"→সূচিপত্র — **১০/১০-এ প্রত্যাশিত অধ্যায়টি এসেছে**। প্রম্পটের মাপ ১১,৫৭০–২৪,২২৮ অক্ষর (পুরো জ্ঞানভান্ডার পাঠালে হত ~৮০,০০০)।

### ⚠️ স্থায়ী নিয়ম — জ্ঞানভান্ডার সবসময় সাইটের সাথে মিলিয়ে রাখতে হবে (ব্যবহারকারীর নির্দেশ: ১৪ সেপ্টেম্বর ২০২৬)

> "ভবিষ্যতে যত ধরনের পরিবর্তন হবে বা গঠনতন্ত্র পরিবর্তন হবে সাথে সাথে বট এর মেমোরিতেও পরিবর্তন করে দিবে।"

**যখনই নিচের কিছু বদলাবে, একই কাজের অংশ হিসেবে `chat_knowledge`-ও হালনাগাদ করতে হবে — আলাদা করে বলার অপেক্ষা করা যাবে না:**

- **গঠনতন্ত্র সংশোধন (v4, v5 …)** → সংশ্লিষ্ট অধ্যায়ের সারিটি বদলাতে হবে; অধ্যায় যোগ/বাদ হলে সারিও যোগ/বাদ, আর শিরোনামে সংস্করণ নম্বর (`গঠনতন্ত্র v4 · অধ্যায় …`) বদলাতে হবে। পুরনো সংস্করণের সারি **রেখে দেওয়া যাবে না** — বট দুটো নিয়ম একসাথে পেলে ভুল বলবে।
- **নতুন সেবা, পেজ বা ফিচার** (যেমন নতুন আমানত স্কীম, নতুন পোর্টাল, মোবাইল অ্যাপ) → "৩. ওয়েবসাইটে কী কী সেবা আছে" নথিটি বদলাতে হবে।
- **যোগাযোগের তথ্য, হোয়াটসঅ্যাপ নম্বর, কার্যালয়** → "৪. প্রতিষ্ঠান ও যোগাযোগ"।
- **নিয়ম/হার বদল** (কিস্তির নিয়ম, রেয়াতকাল, তহবিলের হার, ঋণসীমা) → গঠনতন্ত্রের সংশ্লিষ্ট অধ্যায় **এবং** মূল নথি — দুটোতেই।

**পদ্ধতি:** সুপার অ্যাডমিন → ড্যাশবোর্ড → "চ্যাটবটের জ্ঞানভান্ডার" থেকে হাতে, অথবা `admin_kb_save(id, title, body, sort_order, is_active)` দিয়ে। বদলের পর **অন্তত দুটি প্রশ্নে dry run** চালিয়ে দেখতে হবে ঠিক অধ্যায়টি বাছা হচ্ছে কি না।
**নিয়ম: জ্ঞানভান্ডার হালনাগাদ না করে কোনো নিয়ম-পরিবর্তনের কাজ "শেষ" বলা যাবে না** — বট তখন পুরনো নিয়ম বলতে থাকবে, আর সেটা আর্থিক প্রতিষ্ঠানে সবচেয়ে ক্ষতিকর ভুল।

### ✅ লাইভ যাচাই — সত্যিকারের Gemini উত্তর (১৪ সেপ্টেম্বর ২০২৬)

অতিথি হিসেবে লাইভ সাইট থেকে, `dry` ছাড়াই:

| প্রশ্ন | যে অংশ বাছা হলো | উত্তর ঠিক? |
|---|---|---|
| সভাপতি কে, প্রতিষ্ঠাতা কে? | কমিটি ১ + ২ | ✅ শফিকুজ্জামান / তৌহিদুজ্জামান, সাথে সহ-প্রতিষ্ঠাতাও |
| ঋণ যাচাই কমিটিতে কারা? | অধ্যায় ৪ + কমিটি ১, ৩, ৪ | ✅ ২ জনের নাম, বাকি ৩ পদ "পূরণ করা হয়নি" |
| ঋণ পরিশোধ করতে না পারলে? | অধ্যায় ৭ + কমিটি ৩ | ✅ জরিমানা নেই, মেয়াদ বাড়ে, মওকুফ সম্ভব — **ধারা ৭.১ ও ৭.৫ উদ্ধৃত** |
| আমার সঞ্চয়ের স্থিতি কত? | অধ্যায় ৬ + ৮ | ✅ "লগইন করা নেই" — **গোপনীয়তার সীমা কাজ করছে** |
| সহ-সভাপতি ও কোষাধ্যক্ষ কে? | কমিটি ১ | ✅ আমীমুল এহসান; কোষাধ্যক্ষ "পদটি খালি" |

চারটি উত্তরের একটিও কাটা পড়েনি (`cut: false`), সবগুলোই বাংলায়, আর কোনো নাম/সংখ্যা বানানো হয়নি।

### এখনো বাকি

- চাইলে পরে: কথোপকথনের লগ রাখা (এখন কেবল গণনা রাখা হয়, প্রশ্নের লেখা নয় — গোপনীয়তার কারণে), আর সত্যিকারের ভিডিও এভাটার
- কমিটির পদ পূরণ হলে `chat_knowledge`-এর ৪৫ ও ১২১–১২৪ নম্বর নথি হালনাগাদ করতে হবে (উপরের স্থায়ী নিয়ম)

## 📱 ভবিষ্যৎ কাজ — মোবাইল অ্যাপ (সিদ্ধান্ত: ১৪ সেপ্টেম্বর ২০২৬)

> **⏳ এখন শুরু করা যাবে না।** এটি কেবল **স্মরণে রাখার জন্য** লিখে রাখা হলো। **ওয়েবসাইটের উন্নয়ন সম্পূর্ণ শেষ হওয়ার পরেই** এই কাজ ধরতে হবে। ব্যবহারকারী আলাদা করে "এখন অ্যাপ বানাও" না বললে এই অংশ ধরে কোনো কাজ শুরু করা যাবে না।

**ব্যবহারকারীর নির্দেশনা (হুবহু ভাব):** সাইট ডেভেলপমেন্ট শেষ হলে একটি **মোবাইল অ্যাপ** তৈরি করতে হবে, যেখানে **ওয়েব অ্যাপ/সাইটের সকল সুবিধা** থাকবে — **ব্যাংকের মোবাইল অ্যাপের মতো** করে। **আন্তর্জাতিক মানের** হতে হবে।

### কাজ শুরুর আগে যা যা ঠিক করতে হবে (তখন জিজ্ঞেস করতে হবে)

- **প্ল্যাটফর্ম ও প্রযুক্তি** — Android ও iOS দুটোই, নাকি প্রথমে Android? React Native / Flutter / নাকি বিদ্যমান সাইটকেই PWA + wrapper? (ব্যাকএন্ড যেহেতু Supabase, React Native + `supabase-js` সবচেয়ে সরল পথ; কিন্তু এটি **ব্যবহারকারীর সিদ্ধান্ত**, ধরে নেওয়া যাবে না।)
- **প্রকাশ** — Google Play / App Store অ্যাকাউন্ট, ডেভেলপার ফি, ফাউন্ডেশনের নামে না ব্যক্তির নামে।
- **অগ্রাধিকার** — প্রথম সংস্করণে কোন কোন সেবা থাকবে (সব একসাথে, নাকি ধাপে ধাপে)।

### অ্যাপে যা যা থাকতে হবে (ওয়েবের বর্তমান সব সুবিধা)

সদস্যের দিক — লগইন/সাইন-আপ · আমার ড্যাশবোর্ড · প্রোফাইল ও NID/ছবি · **একাধিক আমানত হিসাব** (জমা, উত্তোলন, ইতিহাস, স্টেটমেন্ট, বকেয়া, নমিনি) · ঋণ আবেদন ও কিস্তি · দান (+ আয়কর প্রত্যয়নপত্র) · ডিজিটাল ভাউচার (ছবি/PDF) · কমিটির সম্মতিপত্র · ই-সিগনেচার · পরিচয়পত্র · ওয়ালেট/ব্যালেন্স।
অ্যাডমিনের দিক — পুরো `dashboard.html`-এর কাজগুলো (অনুমোদন, এন্ট্রি, তহবিল ও খরচ, রিমাইন্ডার, কমিটি, ব্যবহারকারী, সেটিংস)। **অ্যাডমিন অংশ আলাদা অ্যাপ হবে না আলাদা ট্যাব — সেটিও তখন ঠিক করতে হবে।**

### "ব্যাংকের অ্যাপের মতো" বলতে যা বোঝানো হয়েছে

- চালু হওয়ার পর্দা → **বায়োমেট্রিক/PIN লক** (প্রতিবার পাসওয়ার্ড নয়), নিষ্ক্রিয় থাকলে নিজে থেকে লক
- হোমে **স্থিতি ও সাম্প্রতিক লেনদেন** এক নজরে, নিচে ট্যাব-বার
- লেনদেনের পর **তাৎক্ষণিক রশিদ**, শেয়ার/ডাউনলোড
- **পুশ নোটিফিকেশন** — কিস্তির তারিখ, অনুমোদন, রশিদ (এখন যা ই-মেইলে যায়)
- অফলাইনে অন্তত **স্থিতি ও শেষ স্টেটমেন্ট** দেখা
- বাংলা-প্রথম, বড় ট্যাপ-এলাকা, ধীর নেটওয়ার্কে কাজ করে

### আন্তর্জাতিক মান — যা মানতেই হবে

- **নিরাপত্তা:** টোকেন কেবল Keychain/Keystore-এ, স্ক্রিনশট-প্রতিরোধ আর্থিক পর্দায়, রুট/জেলব্রেক শনাক্তকরণ, certificate pinning, স্বয়ংক্রিয় সেশন টাইমআউট। **RLS ও SECURITY DEFINER RPC-ই থাকবে একমাত্র লেখার পথ** — অ্যাপ থেকেও কাঁচা `insert` নয় (ওয়েবের নিয়মগুলো হুবহু প্রযোজ্য)।
- **অ্যাকসেসিবিলিটি:** WCAG-সমতুল্য — কনট্রাস্ট ≥৪.৫:১ (এই ফাইলের ৩ নম্বর থীম-নিয়ম অ্যাপেও চলবে), স্ক্রিন রিডার লেবেল, ফন্ট বড় করলে লেআউট না ভাঙা।
- **দোকানের নীতি:** Play/App Store-এর আর্থিক অ্যাপের শর্ত, গোপনীয়তা-নীতি পাতা, ডেটা-সেফটি ফর্ম, অ্যাকাউন্ট মুছে ফেলার পথ (দুটো স্টোরেই বাধ্যতামূলক)।
- **গুণমান:** ক্র্যাশ রিপোর্টিং, সংস্করণ/আপডেট ব্যবস্থা, ডার্ক ও লাইট দুই থীম, বাংলা+ইংরেজি।

### ⚠️ যা মাথায় রাখতে হবে

- **ব্যাকএন্ড নতুন করে বানানোর দরকার নেই** — Supabase-এর টেবিল, RLS ও RPC সবই অ্যাপ থেকে একইভাবে ব্যবহার হবে। নতুন কোনো সুবিধার জন্য RPC লাগলে সেটিও একই নিয়মে (`SECURITY DEFINER` + `search_path` + `auth.uid()` যাচাই)।
- **ভাউচারের ছবি/PDF** — ওয়েবে html2canvas + jsPDF দিয়ে হয়; অ্যাপে ঐ কৌশল চলবে না, নেটিভ রেন্ডারিং বা সার্ভার-সাইড বিকল্প ভাবতে হবে (বাংলা shaping-এর সমস্যাটি আবার আসবে — "📎 হুবহু ভাউচার" অংশ দেখুন)।
- **পুশ নোটিফিকেশন** এলে SMS-এর প্রশ্নটিরও নিষ্পত্তি হতে পারে (এখন গেটওয়ে নেই — "📱 SMS এখনো নেই" দেখুন)।

## 🎞️ ভিডিও থেকে হুবহু নকল করা দুটি UI (২০ সেপ্টেম্বর ২০২৬)

ব্যবহারকারী দুটি স্ক্রিন-রেকর্ডিং দিয়ে বলেছেন **"হুবহু"** নকল চাই।
ফল দুটি স্বতন্ত্র পেজ — `subscribe-animation.html` ও `card-payment.html`।
দুটোই **একা চলে**: কোনো ছবি নেই, কোনো লাইব্রেরি নেই, সুপাবেসও লাগে না।

### ⚠️⚠️ ভিডিও পড়ার কৌশল — `<video>` এই পরিবেশে চলে না

বাশ স্যান্ডবক্স বন্ধ (৮ সেপ্টেম্বরের উইন্ডোজ আপডেট), তাই ffmpeg নেই।
আর ব্রাউজারে `<video>` এলিমেন্টটিও আটকে থাকে — `readyState` ০-তেই
পড়ে থাকে, `play()`-এর প্রতিশ্রুতি কখনো মেটে না (মিডিয়া পাইপলাইন
এই স্বয়ংক্রিয় পরিবেশে অচল)। **সমাধান — WebCodecs:**

1. পেজে `mp4box.all.min.js` (jsDelivr) ঢোকানো
2. ফাইল-ইনপুটে ভিডিও আপলোড → `FileReader` → `mp4.appendBuffer`
3. `avcC` বাক্স থেকে `description` বের করে `VideoDecoder.configure`
4. প্রতিটি sample `EncodedVideoChunk` করে `decode`, ফ্রেম ক্যানভাসে এঁকে
   `toDataURL` — চাইলে সরাসরি ক্রপ করে, যাতে স্মৃতি না ভরে

**⚠️ `MP4Box.DataStream` নেই** — ঐ বিল্ডে গ্লোবাল `DataStream`।
না মেলালে `description` `undefined` যায় ও `configure` ছুড়ে দেয়।
(`MP4Box.DataStream = DataStream;` বসিয়ে নিলেই চলে।)

দুটি ভিডিওই ৭২০×১২৮০, ~২৯.২৩ fps — Subscription ১৫২ ফ্রেম (৫.২s),
Card Payment ১৬১ ফ্রেম (৫.৫s)।

### ✉️ `subscribe-animation.html` — কাগজের উড়োজাহাজ

**⚠️ ভিডিওর ভেতরের কোড-প্যানেল পড়া গেছে** (ফ্রেম ৪× বড় করে) —
তাই ক্লাসনাম ও রঙ অনুমান নয়, **আসল**: `plume__seat · plume__btn ·
plume__face · plume__words · plume__plane · plume__facet`;
`--field #12151c · --accent #2bffc8 · --accent-off #232c36 ·
--on-ink #04150f · --paper-lit #2bffc8 · --paper-dark #12937a`।
কোড-প্যানেলগুলো পুরো ভিডিওতে **স্থির** (পিক্সেল-ডিফ করে দেখা)।

**জ্যামিতি — যেটা বুঝতে সময় লেগেছে।** `HALF[]`-এর `r`/`t` জোড়ায়
`a` হলো **উল্লম্ব ভাঁজরেখা থেকে দূরত্ব** (০ = রেখা, ১ = বাইরের
কিনারা, মাপ W/2) আর `b` হলো উপর-নিচ (+.5 উপরে)। তাই `r` = বাটনের
**অর্ধেক আয়তক্ষেত্র**, আর `t` = নিচে খাঁজসহ **অন্তর্লিখিত ত্রিভুজ**।
দুই অর্ধেক উল্লম্ব রেখার চারপাশে ঘুরে (`dih`, শেষে ৭২°) — তাতেই
তাঁবুটি দাঁড়ায়। প্রতি অর্ধেকে দুটি ত্রিভুজ, প্রতিটির রঙ নিজের
normal থেকে (`--paper-lit` ↔ `--paper-dark`)।

- **⚠️ CSS-এর `rotateY` দিয়ে তাঁবুকে ঘোরানো যায় না** — SVG সমতল,
  ৯০°-তে মিলিয়ে যায়। তাই বিন্দুগুলো JS-এ নিজেই ঘোরানো হয়
  (`rot3`) ও perspective ভাগ করে আঁকা হয় (`project`, CAM 300)।
  শেষ অবস্থান `ax 50° · ay 30° · az 62°` — ফ্রেম ১১৩–১১৬ মিলিয়ে বাছা।
- **⚠️ গভীরতা অনুযায়ী ত্রিভুজ সাজানো হয়** (`quads.sort`), নাহলে
  ঘোরার সময় পেছনের মুখ সামনে এসে পড়ে।
- সময় ভিডিও থেকে গুনে: ভাঁজ ২৮০ms · পাশ ফেরা ২৪০ms · স্থির ৩৮০ms
  · উড়ান ২৮০ms (মোট ~১.১৮s, ভিডিওতে ফ্রেম ৯৮→১৩২)।
- **⚠️ সফল হওয়ার পর বাটন ফিরে আসে না** — `is-done`-এ লুকানো, নাহলে
  "Subscribe" ও "✓ Done" একে অপরের উপরে বসে (পরীক্ষায় ধরা পড়ে)।

### 💳 `card-payment.html` — Glassy Payment Checkout

ভিডিওর কোড-প্যানেলটি **কাজে লাগেনি** — ওতে সম্পর্কহীন একটি
`StatsCard` কম্পোনেন্ট দেখানো হয়। তাই রঙ সরাসরি **পিক্সেল মেপে**
নেওয়া: PAY বাটন `#fec40a → #fba208 → #ff8a08`, চিপ `#f7ab24`,
লোগো `#efb627`, কার্ড `#191725 → #14151a`, পেজ `#08060b`।

- **তিন অবস্থা:** ফর্ম → `Processing Secure Transaction…` (কার্ড
  মাঝখানে, সোনালি আভা, ঘূর্ণি) → `Payment Successful!` (কার্ড সবুজ,
  টিক আঁকা হয়)। কার্ড উল্টে CVV দেখা যায়; CVV ঘরে গেলে নিজে থেকেই ওল্টে।
- **⚠️ `grid-template-columns: 0fr 1fr` নয় — `1fr 0fr`।** HTML-এ
  কার্ড আগে, ফর্ম পরে; উল্টো দিলে কার্ডের ঘরটাই মুছে যেত।
- **⚠️ `.cols > * { min-width: 0 }` আবশ্যক** — নাহলে গুটিয়ে যাওয়া
  কলামটি ভেতরের লেখার min-content প্রস্থ ধরে রাখে, `0fr` শূন্যে নামে না।
- **⚠️⚠️ `.pane`-এ `clip-path` আছে, আর clip-path সব সন্তানকেও কাটে।**
  দুটি ফল: ① কোণের বন্ধনী দুটি `.pane`-এর **বাইরে** (`.wrap`-এ)
  রাখতে হয়েছে, নাহলে একেবারেই দেখা যেত না; ② ফলাফলের লেখা পাতের
  নিচের কিনারা ছাড়ালে অদৃশ্য হয়ে যায় — তাই `.stage { min-height: 476px }`।
- পটভূমির সোনালি ফিতা পুরোটাই CSS (ঘোরানো ও ঝাপসা করা উপবৃত্ত)।
  **⚠️ ফিতা মোটা ও বেশি ঝাপসা করলে পুরো পর্দা কমলা হয়ে যায় ও
  কাচের পাতটি আর কাচ মনে হয় না** — সরু ও স্পষ্ট রাখতে হবে, আর
  পাতের নিজের পটভূমি গাঢ় (`rgba(14,12,18,.72)`)।
- **⚠️ কোনো কার্ডের তথ্য কোথাও পাঠানো হয় না** — পুরোটাই দেখার জন্য।
  সত্যিকারের পেমেন্টে যুক্ত করতে হলে গেটওয়ে ও সার্ভার-সাইড যাচাই
  লাগবে; এই পেজে কখনো আসল কার্ড নম্বর নেওয়া যাবে না।

### ⚠️ যাচাইয়ের পাঠ — লুকানো ট্যাবে স্ক্রিনশট মিথ্যা বলে

**চতুর্থবার এই ফাঁদে পড়া হলো।** লুকানো ট্যাবে `requestAnimationFrame`
ও CSS ট্রানজিশন থেমে থাকে, তাই স্ক্রিনশটে অ্যানিমেশন অর্ধেক পথে
আটকে দেখায় — মনে হয় লেআউট ভেঙে গেছে। **নিয়ম: অবস্থা বদলের যাচাই
স্ক্রিনশটে নয়, `getBoundingClientRect()` ও `getComputedStyle()` মেপে
করতে হবে** (দরকার হলে `*{transition:none!important}` বসিয়ে)।
এভাবে মেপেই নিশ্চিত হওয়া গেছে: busy-তে `cols = 1052px 0px`,
ফর্মের প্রস্থ ০, কার্ড ঠিক মাঝখানে (কেন্দ্র ৯৬০ = পাতের কেন্দ্র ৯৬০)।

## 💳 দানের কার্ড পেমেন্ট — পরিমাণ, কার্ডের ধরন ও ক্যালিগ্রাফি (২১ সেপ্টেম্বর ২০২৬)

ব্যবহারকারীর তিনটি পর্যবেক্ষণ, তিনটিই `card-payment.html`-এ যোগ করা হয়েছে।

### 💰 দানের পরিমাণ (ধাপ ① Amount)

- পাঁচটি দ্রুত-চিপ (৳৫০০ · ৳১,০০০ · ৳২,৫০০ · ৳৫,০০০ · ৳১০,০০০) + হাতে লেখার ঘর। সীমা `MIN = 10`, `MAX = 500000`।
- **একটিই উৎস, তিন জায়গায় লেখা** — `paintAmount()` মোট, PAY বাটন ও সফলতার পর্দা, তিনটিই একসাথে বসায়। **নিয়ম: অঙ্ক নতুন কোথাও দেখাতে হলে ঐ ফাংশনেই যোগ করতে হবে**, আলাদা করে লিখলে তিনটির একটি পিছিয়ে থাকবে।
- **⚠️ ক্লাসের নাম সংঘর্ষ — `.chip` নেওয়া যাবে না।** প্রথমে পরিমাণের চিপগুলোর নাম `.chip` দেওয়া হয়েছিল, কিন্তু ঐ নামটি আগে থেকেই **ক্রেডিট কার্ডের সোনালি চিপ** (৪২×৩১px গ্র্যাডিয়েন্ট)। ফলে পরিমাণের চিপ কেটে যেত **আর কার্ডের সোনালি চিপটিও ধূসর হয়ে যেত**। এখন `.amt-chip` / `.amt-chips`।

### 💳 কার্ডের ধরন নিজে থেকে চেনা

- `BRANDS[]` — আটটি: **AMEX · DINERS · JCB · MAESTRO · UNIONPAY · DISCOVER · MASTERCARD · VISA**। প্রতিটির `re` (IIN প্রিফিক্স), `len` (বৈধ দৈর্ঘ্য), `gaps` (কোথায় ফাঁক) ও `cvv` (৩ না ৪ ঘর)।
- **⚠️ ক্রমটি গুরুত্বপূর্ণ** — নির্দিষ্ট প্রিফিক্স আগে, চওড়া প্রিফিক্স পরে। `VISA` (`^4`) সবার শেষে, নাহলে `4026`-জাতীয় Electron কার্ড ভুল ঘরে পড়ত; `JCB` (`^35…`) অবশ্যই `DINERS` (`^36|^38|^39`)-এর পরে এবং AMEX (`^3[47]`)-এর পরে।
- **ফাঁক ও CVV ধরনভেদে বদলায়** — AMEX `4-6-5` (`3714 496353 98431`) ও CVV ৪ ঘর; বাকিরা `4-4-4-4` ও ৩ ঘর। `spaced()` `gaps` থেকে গোষ্ঠী বানায়, হার্ডকোড নয়।
- `luhnOk()` চেকসাম — কেবল **পূর্ণ দৈর্ঘ্যে** যাচাই হয়, নাহলে টাইপ করার মাঝপথে বৈধ কার্ডও ভুল দেখাত।
- লোগো দুই জায়গায় — ফর্মের ঘরের ভিতরে ও কার্ডের গায়ে; `paintMark()` `mark--<id>` ক্লাস বদলায়, ছবি নয় (সব CSS-এ আঁকা)।
- যাচাই: আটটি ব্র্যান্ডের নমুনা নম্বরে আটটিই সঠিক ধরা পড়েছে, ফাঁক ও CVV-র দৈর্ঘ্যও মিলেছে, আর একটিও Luhn-এ ভুলভাবে আটকায়নি।

### ⚠️⚠️ নতুন UI কেবল `card-payment.html`-এ ছিল — `donation.html` টেরই পায়নি (সংশোধন: ২১ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর প্রশ্ন:** *"দানের ক্ষেত্রে কার্ড সিলেক্ট করলে কার্ড পেমেন্ট এর নতুন ফিচার আসছে না। এটি কোথায় এফেক্ট হয়েছে?"* — **কোথাও এফেক্ট হয়নি।** `card-payment.html` একটি স্বতন্ত্র পেজ, আর সাইটের **কোনো জায়গা থেকে লিংকই করা ছিল না** (পুরো রিপোতে খুঁজে একটিও `card-payment.html` লিংক পাওয়া যায়নি)। `donation.html` তার নিজের পুরনো `#cardSection`-ই দেখাত — তিনটি সাদামাটা ঘর ও `formatCard()`, যেটি সবসময় ৪-৪-৪-৪ ফাঁক ও ১৬ অঙ্কের সীমা ধরত (তাই AMEX-এর ১৫ অঙ্ক ও ৪-ঘরের CVV অসম্ভব ছিল)।

**নিয়ম: নতুন কোনো UI বানানোর পর "কোথা থেকে ব্যবহারকারী এটি পাবেন" — সেই পথটি একই কাজে যোগ না করলে কাজ শেষ নয়।** স্বতন্ত্র ডেমো পেজ বানানো আর সেটি সাইটে যুক্ত করা দুটি আলাদা কাজ।

**সিদ্ধান্ত (ব্যবহারকারী): দানের পেজেই বসিয়ে দাও** — আলাদা পেজে পাঠানো হয়নি, কারণ তাতে দান দুই পেজে ভাগ হয়ে যেত ও দুই জায়গায় অঙ্ক মেলাতে হত।

- `donation.html`-এর `#cardSection` এখন ৩D কার্ড (সামনে/পিঠ, CVV ঘরে গেলে নিজে থেকেই ওল্টে) + আটটি ব্র্যান্ড শনাক্তকরণ + Cardholder name ঘর (আগে ছিলই না, অথচ কার্ডে নাম দেখাতে হয়)।
- **⚠️ অঙ্ক এখানে আবার চাওয়া হয় না** — পেজের উপরের দানের পরিমাণের ঘরটিই একমাত্র উৎস। `card-payment.html`-এর `.amt-chip` অংশটি **পোর্ট করা হয়নি**, নাহলে দুটি সংখ্যা আলাদা হয়ে যেত।
- **⚠️ সব ক্লাসে `cp-` প্রিফিক্স** — মূল পেজে নাম ছিল `.chip`/`.face`/`.pan`/`.mark`; `donation.html`-এ `.card`, `.card-body`, `.card-head` আগে থেকেই আছে, আর `.chip` নিয়ে একবার সংঘর্ষ হয়েছিল। প্রিফিক্স দিয়ে সেই ঝুঁকি একেবারে বন্ধ।
- **⚠️ `CP_BRANDS` তালিকাটি এখন দুই ফাইলে** (`donation.html` ও `card-payment.html`) — একটি বদলালে অন্যটিও বদলাতে হবে। (shared kit-এ নেওয়া যেত, কিন্তু `card-payment.html` ইচ্ছে করেই একা চলে — কোনো লাইব্রেরি বা kh-ui ছাড়া।)
- **⚠️ লম্বা নামে ব্র্যান্ডের চিহ্ন কার্ডের বাইরে চলে যেত** — `.cp-foot > span:first-child`-এ `min-width:0` ও নামে `text-overflow:ellipsis` দিতে হয়েছে। (**তৃতীয়বার এই `min-width` ফাঁদ** — flex/grid আইটেম ডিফল্টে min-content প্রস্থ ধরে রাখে।) যাচাই: ৩৪০px কার্ডে ৪২ অক্ষরের নাম দিয়ে দেখা গেছে নাম `…` হয়ে কাটে, চিহ্নটি ডান কিনারা থেকে ১৯px ভেতরে থাকে (প্যাডিং ১৮px), আর কার্ডের প্যাডিং-সীমা কোনো উপাদান ছাড়ায় না।
- **ফর্মের কার্ড দুই থীমেই সাদা** আর ৩D কার্ড **নিজের গাঢ় গ্র্যাডিয়েন্ট বহন করে** — তাই ভেতরের সব মান থীম-নিরপেক্ষ। যাচাই (দুই থীমে হুবহু এক): নম্বর ১৩.৪২:১ · PLATINUM ৭.২৭:১ · লেবেল ৬.০৮:১ · নাম/তারিখ ১৫.০৫:১ · CVV বাক্স ১৮.১:১ · VISA চিহ্ন ১৪.২:১ · উল্টানোর বাটন ১১.৩১:১ · নিচের নোট ৪.৮৩ (ডার্ক) / ৬.৮৯:১ (লাইট) · ত্রুটির বার্তা ৪.৮৩:১।
- **যাচাই (লাইভ পেজে নতুন কোড বসিয়ে):** ১০টি নমুনা নম্বরে **১০/১০ ব্র্যান্ড সঠিক** (Visa · Visa Electron `4026` · Mastercard · ২-সিরিজ `2223` · AMEX · Discover · Diners · JCB · UnionPay · Maestro), ফাঁক ও CVV-র দৈর্ঘ্যও মিলেছে (`3714 496353 98431` → CVV ৪ ঘর; `3622 720627 1667` → ১৪ অঙ্ক), একটিও Luhn-এ ভুলভাবে আটকায়নি; ভুল চেকসাম (`…1112`) দিলে লাল বেড় + বাংলা বার্তা দেখা যায়; নাম/তারিখ/CVV তিনটিই কার্ডের গায়ে প্রতিফলিত হয়; উল্টানোর বাটনে লেখা বদলায়। **`#cardSection`-এর ভেতরের একটিও উপাদান নিজের ঘর ছাড়ায় না, আড়াআড়ি স্ক্রল ০।**
- **⚠️ `kh-ui.css`/`kh-ui.js` বদলায়নি, তাই `?v=` বাড়ানোর দরকার নেই** — পুরো পরিবর্তনটি `donation.html`-এর ভেতরে, আর HTML ফাইলটি ব্রাউজার ক্যাশ করে না।
- **⚠️ এখনো আসল পেমেন্ট হয় না।** `donation.html`-এর কার্ডের ঘরগুলো কোথাও পাঠানো হয় না; `record_donation`-এ কেবল `'Debit/Credit Card'` নামটি জমা হয় (আগেও তাই ছিল)। তাই ঘরের নিচে স্পষ্ট লেখা আছে "কার্ডের তথ্য এই পর্দাতেই থাকে — কোথাও পাঠানো হয় না"। সত্যিকারের পেমেন্টের জন্য গেটওয়ে (SSLCommerz / aamarPay / Stripe) ও সার্ভার-সাইড যাচাই লাগবে — **সেটি এখনো সিদ্ধান্ত হয়নি**, আর তার আগে এই ঘরে আসল কার্ড নম্বর নেওয়া যাবে না।

### 🕌 কালো পটভূমির বদলে লগইনের ক্যালিগ্রাফি

- পেজের পটভূমি এখন `images/auth-bg.jpg` (`.silk`, `fixed`) + গাঢ় পর্দা (`.veil`) + দুটি নরম আভা (`.glow`); কার্ডের গায়েও একই ছবি `opacity: .11`-এ (`.face::before`)।
- **⚠️ `mix-blend-mode: luminosity` ব্যবহার করা যাবে না** — চেষ্টা করা হয়েছিল, কিন্তু ওটি কার্ডের **সোনালি চিপ ও PLATINUM লেখার রঙ শুষে নিত**। এখন শুধু `opacity` + `z-index`, আর ভেতরের সব স্তরে `position: relative; z-index: 2`।

## ✉️ ফুটারের সাবস্ক্রিপশন (kh-ui · ২১ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর সিদ্ধান্ত:** *"সাবসক্রিপশন তো ফুটারেই আছে। সেখানেই থাকলে ভালো লাগবে।"*

- `KHUI.mountSubscribe()` — `boot()` থেকে নিজেই চলে, ফুটারের পুরনো `.ft-nl` ঘরটিকেই (বা `#khSubscribe`) বদলে দেয়। **কোনো HTML-এ হাত দিতে হয় না**, ফুটার আছে এমন সব পেজেই একসাথে আসে। বাদ দিতে `<body data-kh-sub="off">`।
- জ্যামিতি ও সময়রেখা `subscribe-animation.html` থেকে **হুবহু** পোর্ট করা (`HALF` → `PLUME_HALF`, `DIH 72°`, `AX 50° · AY 30° · AZ 62°`, `CAM 300`, FOLD ২৮০ · TURN ২৪০ · HOLD ৩৮০ · FLY ২৮০ ms)। ⚠️ ঐ পেজের জ্যামিতি বদলালে এখানেও বদলাতে হবে — দুই জায়গায় কপি আছে।
- **স্কিনটি সোনালি (`.plume--gold`), ভিডিওর নিয়ন সবুজ নয়** — ফুটারের প্লাম পটভূমিতে নিয়ন সবুজ ব্র্যান্ডের বাইরে পড়ত। হুবহু নিয়ন সংস্করণটি `subscribe-animation.html`-এই আছে।
- CSS: `kh-ui.css` **সেকশন ২৪**। পিলটি নিজের গাঢ় `--field: #241539` বহন করে, তাই ভেতরের সব লেখা **দুই থীমে হুবহু এক**।

### ⚠️ তিনটি ফাঁদ — লাইভে মেপে ধরা পড়েছে

1. **`ft-nl` ক্লাসটি সরাতেই হবে।** `index.html`-এর ইনলাইন `<style>`-এ `.ft-nl input` ও `.ft-nl button` আছে — ওজন (০,১,১), আর `.plume__input` (০,১,০)। ফলে ক্লাসটি রেখে দিলে পুরনো সোনালি বাটন-চেহারাই ফিরে আসত। তাই `mountSubscribe()` প্রথমেই `classList.remove('ft-nl')` করে।
2. **`enhanceEmails()`-এর আগে চলতে হবে, আর ইনপুটে `data-kh-skip="1"` দিতে হবে।** নাহলে ফুটারের ই-মেইল ঘরটি প্রিমিয়াম ই-মেইল UI পেয়ে যায় — টিক চিহ্নটি `position:absolute` হয়ে Subscribe বাটনের উপর পড়ে, আর "দ্রুত বাছাই: gmail / outlook / yahoo" চিপগুলো ফুটার ভরে ফেলে। লাইভে পরীক্ষা করে ধরা পড়েছে (`.ft-nl`-এ `kh-email-host` ক্লাস বসে গিয়েছিল)।
3. **⚠️⚠️ লাইট থীমে ফুটার ফ্যাকাশে হয়ে যায় — নোটের রঙ তাই উল্টো।** মেপে দেখা গেছে `.footer`-এর পটভূমি ডার্কে `#160d26`, কিন্তু লাইটে `rgba(247,241,251,.86)` সাদার উপর, অর্থাৎ কার্যত **`#f8f3fc`**। নোটটি পিলের **বাইরে** বসে, তাই প্রথমে দুই থীমেই সাদা রাখায় লাইটে সেটি **১.৫৬:১** হয়ে কার্যত অদৃশ্য হত। এখন ডার্কে `rgba(255,255,255,.62)` (৭.৫৭:১) ও লাইটে `#4a4458` (৮.৫১:১); ত্রুটির রঙ ডার্কে `#ffb4a2` (১০.৯৯:১), লাইটে `#b3261e` (৫.৯৮:১)।
   **নিয়ম: কোনো লেখা পিলের/কার্ডের বাইরে বসলে ধরে নেওয়া যাবে না ফুটার গাঢ়ই থাকবে — মেপে দেখতে হবে।**
   ⚠️ নোটের নির্বাচকের ওজনও বাড়াতে হয়েছে — সেকশন ৯-এর ঢালাও নিয়মটি (০,২,২) `<p>`-ও ধরে, তাই ক্লাসটি **দুবার** লেখা (`.plume__note.plume__note` → (০,৩,১))। `!important` একা যথেষ্ট নয়।

### যাচাই (লাইভ পেজে নতুন CSS ও মার্কআপ বসিয়ে, computed style মেপে)

| কী | ডার্ক | লাইট |
|---|---|---|
| ই-মেইল ইনপুট | ১৪.২১:১ | ১৪.২১:১ |
| Subscribe (নিষ্ক্রিয়) | ৯.২১:১ | ৯.২১:১ |
| Subscribe (সোনালি, কালি লেখা) | ৮.০৬:১ | ৮.০৬:১ |
| "✓ হয়ে গেছে" | ৮.০৯:১ | ৮.০৯:১ |
| নোট | ৭.৫৭:১ | ৮.৫১:১ |
| নোট (ত্রুটি) | ১০.৯৯:১ | ৫.৯৮:১ |

চারটি অবস্থাই মেপে দেখা হয়েছে (idle · armed · retry · done): `is-done`-এ `btn opacity 0` ও "হয়ে গেছে" `visible` — **লেখা দুটি একে অপরের উপর পড়ে না**। ৩৭৫px-এ পিল ৩৪৩×৪৪px, বাঁয়ে ১৬ ডানে ৩৫৯, **আড়াআড়ি স্ক্রল ০**। লাইট থীমে প্রতিটি `!important` ফেরত সত্যিই প্রয়োগ হচ্ছে তা computed style থেকে মিলিয়ে দেখা হয়েছে (পিল `#241539`-ই থাকে, ঢালাও নিয়ম তাকে ছোঁয় না)।

**ক্যাশ-বাস্টিং — এখন `?v=31`**, সব ১৮টি HTML-এ (৩৬টি রেফারেন্স, গুনে মিলিয়ে দেখা)। ⚠️ `loan-application.html`-এর `</html>`-এর পরের NUL বাইটের কারণে ripgrep ঐ ফাইলের দ্বিতীয় রেফারেন্সটিকে "binary" বলে বাদ দেয় — আলাদা করে গুনে দেখতে হয়েছে।

## 💸 টাকা দেওয়ার মাধ্যম — এক কম্পোনেন্ট, সব জায়গায় (২১ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর তিনটি সিদ্ধান্ত (AskUserQuestion-এ):**
① **রেকর্ড রাখা, গেটওয়ে পরে** — টাকা সাইটে কাটা হয় না; সদস্য বাইরে পাঠান, এখানে মাধ্যম + TrxID লেখেন, অ্যাডমিন মিলিয়ে অনুমোদন করেন।
② **৩D কার্ডটি ফাউন্ডেশনের তথ্য দেখাবে** — দাতার কার্ড নম্বর কখনো নেওয়া হবে না।
③ চারটি গুচ্ছই — **MFS** (বিকাশ · নগদ · রকেট · উপায়) · **বাংলা কিউআর** · **ব্যাংক ট্রান্সফার · কার্ড** · **কার্যালয়ে নগদ · এজেন্ট ব্যাংকিং**।

### আগে কী ছিল (সমীক্ষা করে পাওয়া)

| কোথায় | অবস্থা |
|---|---|
| `donation.html` | ৬টি হার্ডকোড করা `.pay-btn` + ৩টি সেকশন; ব্যাংকের হিসাব **পেজেই লেখা** ("1234567890" — placeholder); "International" কখনো কাজ করত না; কার্ড/ব্যাংকে `p_transaction_ref` **সবসময় null** যেত |
| `savings-portal.html` জমা | **কোনো মাধ্যমই ছিল না** — শুধু "bKash/Nagad TrxID" ঘর, আর নম্বরটি পেজে হার্ডকোড |
| `borrower-portal.html` কিস্তি | সাদামাটা `<select>` (৫টি বিকল্প) |
| `dashboard.html` | ক্যাশ-দানে method হার্ডকোড `'cash'`, সঞ্চয় এন্ট্রিতে method নেই |

### এখন

- **`KHUI.payPicker(host, opts)`** — `kh-ui.js`; টাইল → ৩D কার্ড → রেফারেন্সের ঘর। হ্যান্ডেল দেয় `.validate()` · `.value()` · `.method()` · `.reset()` · `.onChange()`। CSS: `kh-ui.css` **সেকশন ২৫**, প্রিফিক্স `.kh-pay-`।
- **`KHUI.PAY_METHODS`** — ৯টি মাধ্যমের নাম/আইকন/রেফারেন্সের লেবেল **একমাত্র উৎস**। ⚠️ `donation.html`-এর `methodMap` ও `borrower-portal.html`-এর `METHOD` — দুটো হার্ডকোড তালিকাই মুছে এখান থেকে নেওয়া হয়; নাহলে নতুন মাধ্যমে ভাউচারে কাঁচা id ("bqr") দেখা যেত।
- **ফাউন্ডেশনের তথ্য `app_settings.pay_methods`-এ**, ক্লায়েন্ট পায় `public_pay_methods()` RPC দিয়ে — যা **কেবল চালু** মাধ্যম ফেরত দেয়। **কোনো নম্বর কোডে নেই।**

### 🟥 নয়টিই চালু — ডেমো তথ্য + লাল সতর্কবার্তা (২১ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর নির্দেশ:** *"ব্যাংক · বাংলা QR · কার্ড · এজেন্ট বন্ধ রাখা যাবে না। এখন ডেমো আকারে বসিয়ে দাও। পরবর্তীতে আমি সঠিক টি দিয়ে রিপ্লেস করিয়ে নিব। তুমি আপাতত কোন ফাংশন বন্ধ রাখবে না।"*

- **নয়টি মাধ্যমই `enabled: true`।** বিকাশ ও নগদে কোডে সত্যিই থাকা নম্বর (০১৭১১৫১৫৯৫২); বাকি **সাতটিতে ডেমো তথ্য** — রকেট · উপায় · বাংলা QR · ব্যাংক · কার্ড · কার্যালয়ে নগদ · এজেন্ট।
- **⚠️⚠️ প্রতিটি ডেমো সারিতে `demo: true`, আর কম্পোনেন্ট সেটি দেখলে কার্ডের মাথায় লাল সতর্কবার্তা বসায়** — *"এটি ডেমো তথ্য — এই নম্বর/হিসাবে টাকা পাঠাবেন না।"* (`.kh-pay-demo`, ১১.৬৯:১) — আর **টাইলের কোণে ছোট লাল বিন্দু** (`.kh-pay-dot`), যাতে তালিকা দেখেই বোঝা যায় কোনগুলোর তথ্য এখনো আসল নয়।
  **কারণটি আর্থিক, নিছক সাজসজ্জা নয়:** ভুয়া হিসাব নম্বরে কেউ সত্যিই টাকা পাঠিয়ে দিলে সেটি আর ফেরানো যাবে না। **নিয়ম: আর্থিক তথ্যের কোনো ডেমো/placeholder মান পর্দায় দেখানো হলে তার পাশে স্পষ্ট সতর্কবার্তা থাকতেই হবে** (`donation.html`-এর পুরনো "1234567890" ঠিক এই সতর্কবার্তা ছাড়াই বসে ছিল)।
- **আসল তথ্য বসানোর পদ্ধতি:** `app_settings.pay_methods.<id>`-এ মান বদলে **`demo` কি-টি মুছে দিতে বা `false` করতে হবে** — তখনই ব্যাজ ও বিন্দু নিজে থেকে চলে যায়। কোড বদলাতে হয় না।
- **⚠️ বাংলা QR-এ স্ক্যানযোগ্য নকল QR আঁকা হয় না।** `image` ফাঁকা থাকলে একটি **স্পষ্ট প্লেসহোল্ডার** বসে (`.kh-pay-qr--ph` — ড্যাশ-বর্ডার, "ডেমো QR" লেখা, ৭.৮:১)। নকল কিন্তু স্ক্যানযোগ্য কোড বানালে কেউ সেটি স্ক্যান করে বিভ্রান্ত হতেন — তাই ইচ্ছে করেই আঁকা হয়নি।
  **↳ সংশোধন (২২ সেপ্টেম্বর ২০২৬): এখন একটি ডেমো QR বসানো আছে** — নিচের "🔳 ডেমো বাংলা QR" অংশ দেখুন। নিয়মটি বদলায়নি, কেবল সীমাটি স্পষ্ট হয়েছে: **স্ক্যানযোগ্য হওয়া নিষিদ্ধ নয়, পেমেন্ট-ফরম্যাট হওয়া নিষিদ্ধ।**
- **`savings_transactions.payment_method`** নতুন কলাম; `deposit_to_savings`-এ `p_method` — অজানা মাধ্যম সার্ভারই আটকায়, নাম lowercase-এ normalise হয়। ⚠️ পুরনো ৪-প্যারামিটার সংস্করণ **ড্রপ করা হয়েছে** (নাহলে PostgREST ওটিই ডাকত ও মাধ্যম নীরবে হারাত — `create_depositor`-এ ঠিক এটিই হয়েছিল)।

### 🎨 ব্র্যান্ডের আসল রঙ — ব্যান্ড + গাঢ় দেহ (সংশোধন: ২১ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর নির্দেশ:** *"এমএফএস এর অরিজিনাল কালারগুলো ব্যবহার করবে।"*

তিন ধাপে এখানে পৌঁছানো হয়েছে, আর **মাঝের ধাপটি ভুল ছিল** —

1. **প্রথমে** আসল ব্র্যান্ড রঙে সাদা লেখা বসানো হয়েছিল → নগদ **২.৬২:১** · উপায় **২.৫৯:১** · বিকাশ **৩.২৪:১** (AA ফেল)।
2. **তারপর** প্রতিটি রঙ হিসাব করে গাঢ় করা হয়েছিল (নগদ ×.৭৬ ইত্যাদি) → কনট্রাস্ট পাস, **কিন্তু ব্র্যান্ড আর চেনা যেত না** — আর ব্যবহারকারী আসল রঙই চেয়েছিলেন।
3. **এখন তৃতীয় পথ, দুটোই মেলে:** আসল রঙ থাকে কার্ডের মাথার **ব্যান্ডে** (`.kh-pay-band`, `--band`), আর সব তথ্য বসে নিচের **গাঢ় দেহে** (`#241f22 → #151214` — বিকাশ ও নগদ দুই ব্র্যান্ডেরই নিজের charcoal `#231f20` ঘরানা)। ফলে দেহের লেখা **১০.৩ – ১৭.৮:১**, আর ব্র্যান্ড রঙ চোখের সামনেই থাকে।

**ব্যান্ডের লেখার রঙ ব্র্যান্ডভেদে আলাদা — মেপে বাছা:**

| মাধ্যম | ব্যান্ডের রঙ (আসল) | লেখা | কনট্রাস্ট |
|---|---|---|---|
| বিকাশ | `#e2136e → #9e1638` | সাদা | ৪.৬১:১ |
| নগদ | `#f6921e → #ec1c24` | **কালি `#231f20`** | ৭.০২:১ |
| রকেট | `#8c3494 → #5f1f66` | সাদা | ৬.৯২:১ |
| উপায় | `#f58220 → #6f2c91` | **কালি** | ৬.২৮:১ |

- **⚠️ নগদের লাল `#ec1c24`-এ সাদা মাত্র ৪.৪১:১ আর কালি ৩.৬৯:১ — দুটোই ফেল।** তাই ব্যান্ডে তার **কমলা** প্রান্ত (`#f6921e`) ব্যবহার করা হয় ও লেখা বসে কালিতে; লাল থাকে গ্র্যাডিয়েন্টের যে প্রান্তে কোনো লেখা নেই। একই যুক্তি উপায়ের কমলায়।
- **উৎস যাচাই করা:** বিকাশ `#e2136e · #d12053 · #9e1638` + charcoal `#231f20`; নগদ `#ec1c24 · #f6921e` + `#231f20` — দুটোই logotyp.us-এর ব্র্যান্ড পাতা থেকে। ⚠️ **রকেট ও উপায়ের হেক্স কোনো নির্ভরযোগ্য সূত্রে পাওয়া যায়নি** — প্রচলিত মান বসানো (`#8c3494`, `#6f2c91`/`#f58220`); **আসল ব্র্যান্ড গাইড পেলে এই দুটি বদলাতে হবে।**
- **⚠️ বাংলা QR-এর সবুজ অবাধে গাঢ় করা যায়** — ওটি কোনো ব্র্যান্ড নয়, একটি জাতীয় স্কিম। `#16916a`-এ সাদা ছিল ৩.৯৭:১, এখন `#0d7052` (৬.০৮:১)।

**⚠️⚠️ নতুন একটি ফাঁদ ধরা পড়েছে — `opacity` মাপে ধরা পড়ে না।** ব্যাজে `opacity: .82` দেওয়া ছিল; সেটি লেখাকে ব্যান্ডের রঙের দিকে ফিকে করে ও কনট্রাস্ট নামায়, **অথচ `getComputedStyle().color` পুরো রঙই ফেরত দেয়** — তাই মেপেও ভুল (বেশি) মান পাওয়া যায়। এখন `opacity: 1`।
**নিয়ম: কনট্রাস্ট মাপার আগে দেখে নিতে হবে উপাদানে বা তার পূর্বপুরুষে `opacity` আছে কি না — থাকলে মাপটি মিথ্যা।**

### ⚠️ বাকি যে ফাঁদগুলো মেপে ধরা পড়েছে
2. **কপি বাটনের পটভূমি কালচে, সাদা নয়।** সাদা আভা দিলে পিলটি কার্ডের চেয়ে হালকা হয়ে যায় ও সাদা লেখা দুর্বলতম হয় (নগদে ৩.১২:১)। `rgba(0,0,0,.26)` দিলে সাদা লেখা সবসময় কার্ডের চেয়ে ভালো — আলাদা করে মাপার দরকারই থাকে না (এখন ৯.৩–১২.৪:১)।
3. **কম্পোনেন্টটি নিজের হালকা সারফেস বহন করে** (`#fbf9fd`)। চার জায়গায় বসে যার দুটির পটভূমি সাদা, একটির গাঢ় (অ্যাডমিন) — হোস্টের রঙের উপর ছাড়লে প্রতিটি জায়গায় আলাদা করে মাপতে হত। ভাউচার/প্রোফাইল কার্ডের মতোই **দুই থীমে ও চার জায়গায় এক**।
4. **সেকশন ৯-এর ঢালাও লাইট-থীম নিয়ম** কার্ডের ভেতরের সাদা লেখাও ধরে — তাই `.kh-pay .kh-pay-card …` দিয়ে ওজন বাড়িয়ে (০,৩,১) ফেরানো হয়েছে। **নতুন কোনো লেখা যোগ করলে ঐ ব্লকেও রঙ দিতে হবে।**
5. **রেফারেন্সে বাংলা অঙ্ক/অক্ষর ঢুকতে দেওয়া হয় না** — `[^A-Za-z0-9-/]` ছেঁটে uppercase, সর্বোচ্চ ৪০। নাহলে অ্যাডমিন TrxID মিলিয়ে দেখতে পারতেন না।

### ⚠️⚠️ দাতার কার্ড নম্বর নেওয়া বন্ধ

`donation.html`-এর কার্ড-ঘরগুলো (নম্বর · নাম · Expiry · CVV) **সরানো হয়েছে**। কারণ: গেটওয়ে নেই, তাই তথ্য কোথাও যেত না — অথচ দাতা ভাবতেন পেমেন্ট হয়ে গেছে; আর গেটওয়ে থাকলেও নিজের পেজে কার্ড নম্বর নেওয়া PCI-DSS-এর সবচেয়ে কঠিন স্তরে ফেলে।
⚠️ **ব্র্যান্ড শনাক্তকরণের কোড (`CP_BRANDS`, `cpInit()`, ইনপুট যাচাই) মুছে ফেলা হয়নি** — `cpInit()` `#cardNum` না পেলে চুপচাপ ফিরে যায়, আর `#cardSection` `hidden`। গেটওয়ে এলে শুধু ঘরগুলো ফিরিয়ে আনলেই চলবে। হুবহু নকশাটি `card-payment.html`-এও আছে।

### ইনপুট যাচাই (ব্যবহারকারীর অনুরোধ)

`donation.html`-এর কার্ড-ঘরে `guard(el, okChar, strip)` — **তিনটি পথ** বন্ধ: `beforeinput` (টাইপ) · `paste` (পরিষ্কার করে বসায়, পুরো পেস্ট বাতিল করে না) · `input` (ড্র্যাগ-ড্রপ, অটোফিল, ভয়েস)। কার্সর যেন না লাফায় তাই অবস্থান হিসাব করে রাখা হয়।
⚠️ **নামের নিয়মে `\p{M}` রাখতেই হবে** — নাহলে "তৌহিদুজ্জামান" হয়ে যেত "তহদজজামান" (চ্যাটবটের শব্দ মেলানোর সময়ও এই ফাঁদেই পড়া হয়েছিল)। যাচাই: বাংলা নাম অটুট · `Md. Abdur Rahman-Khan` ও `D'Souza` বৈধ · `Touhid123` → `Touhid` · বাংলা অঙ্ক `৪১১১` বাদ · কার্ড নম্বরে অক্ষর ঢোকে না।

### যাচাই (লাইভ পেজে নতুন কোড বসিয়ে, মেপে)

- **৯টি কার্ডের প্রতিটি মান AA পাস**, সবচেয়ে খারাপ **৪.৬১:১** — আর সেটি বিকাশের **নিজের** রঙ-জোড়া (`#e2136e` + সাদা)। ব্যান্ডের নাম ৪.৬১–৭.৩৫:১ · দেহের লেবেল ১০.৩১:১ · মান ১৬.২৩:১ · কপি বাটন ১৭.৭৭:১ · নোট ১৩.৩৩:১ · ডেমো সতর্কবার্তা ১১.৬৯:১। টাইল ১৫.০:১ · শিরোনাম ১৪.৪:১ · হিন্ট ৬.৮:১ · তারকা ৫.৬:১।
- মাধ্যম বদলালে কার্ডের ক্লাস, রেফারেন্সের লেবেল ("TrxID" → "রেফারেন্স নম্বর" → "রশিদ নম্বর" → "অনুমোদন নম্বর") ও `aria-checked` — তিনটিই বদলায় (ঠিক একটি চেক থাকে)।
- রেফারেন্স পরিষ্কার: `trx 9f2k-7x/1a০৫ঘ!@#` → `TRX9F2K-7X/1A`; ৪০ অক্ষরে থামে।
- ফাঁকা রেখে জমা দিলে লাল বেড় + মাধ্যম-অনুযায়ী বাংলা বার্তা; মান দিলে `{method, reference}`।
- **একটিও উপাদান নিজের ঘর ছাড়ায় না, আড়াআড়ি স্ক্রল ০**; ৯টি টাইল ৩ সারিতে।
- ডাটাবেজে: অজানা মাধ্যম (`paypal`) আটকেছে · `'BKash'` → `bkash` হয়ে কলামে বসেছে · মাধ্যম ছাড়াও পুরনো মতোই চলে (backwards compatible) · `public_pay_methods()` অতিথির কাছে **কেবল ৩টি চালু** মাধ্যম দেয়, বন্ধগুলোর অসম্পূর্ণ তথ্য বাইরে যায় না। পরীক্ষার সারি রোলব্যাক/মুছে ফেলা হয়েছে (`leftover = 0`)।

**ক্যাশ-বাস্টিং — এখন `?v=32`**, সব ১৮টি HTML-এ (৩৬টি রেফারেন্স, গুনে মিলিয়ে দেখা)।

### 🔜 এখনো বাকি (এই কাজেরই অংশ)

1. ~~অ্যাডমিন সেটিংস UI~~ — **হয়ে গেছে (২২ সেপ্টেম্বর ২০২৬)**, নিচের "⚙️ টাকা নেওয়ার মাধ্যম — অ্যাডমিন সেটিংস UI" অংশ দেখুন। আর SQL লাগে না।
2. ~~`dashboard.html`-এর অ্যাডমিন এন্ট্রি~~ — **হয়ে গেছে (২২ সেপ্টেম্বর ২০২৬)**। ⚠️ `payPicker` বসানো হয়নি — বদলে তিনটি select `KHUI.PAY_METHODS` থেকে ভরে দেওয়া হয়েছে। কারণ অ্যাডমিনকে ফাউন্ডেশনের নিজের হিসাব-কার্ড ও গেটওয়ে-বাটন দেখানোর কোনো মানে নেই; দরকার ছিল শুধু **হার্ডকোড তালিকাটি সরানো**। বিস্তারিত নিচে।
3. ~~`chat_knowledge` হালনাগাদ~~ — **হয়ে গেছে (২২ সেপ্টেম্বর ২০২৬)**, নিচের গেটওয়ে অংশ দেখুন।
4. ফাউন্ডেশনের **আসল** তথ্য — ব্যাংক হিসাব (নাম · নম্বর · ব্যাংক · শাখা · রাউটিং), বাংলা QR-এর ছবি, রকেট ও উপায়ের নম্বর, কার্যালয়ের ঠিকানা ও এজেন্ট আউটলেট। ব্যবহারকারী দেবেন; **অনুমানে বসানো যাবে না**। বসানোর সাথে সাথেই ঐ মাধ্যমের `demo` পতাকা তুলে দিতে হবে।
5. **রকেট ও উপায়ের আসল ব্র্যান্ড হেক্স** — এখনকার মান প্রচলিত, যাচাই করা নয়।

## 🌐 অনলাইন পেমেন্ট গেটওয়ে — SSLCommerz (২২ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর নির্দেশ:** *"নির্বাচিত মাধ্যমের পোর্টালে গিয়ে সরাসরি টাকা পরিশোধ বা জমা দেয়া যাবে… পেমেন্ট মাধ্যম নির্বাচন করলে সিস্টেম তাকে সংশ্লিষ্ট পেমেন্ট গেটওয়েতে নিয়ে যাবে। তবে বাংলা কিউআর হাইলাইটেড রাখা যেতে পারে… বাংলা কিউআরটি সবক্ষেত্রেই অপেন রাখা প্রয়োজন।"*

**সিদ্ধান্তগুলো (AskUserQuestion-এ):** গেটওয়ে **SSLCommerz**, এখন **স্যান্ডবক্সে** (মার্চেন্ট অ্যাকাউন্ট নেই) · হিসাব নম্বর **বাটনের আড়ালে, QR সবসময় খোলা** · সফল হলে **নিজে থেকেই নিশ্চিত** · চ্যানেল **গেটওয়ে যা দেয় তাই** · **ম্যানুয়াল পথও থাকবে** · QR ছবি ব্যবহারকারী দেবেন।

### ⚠️⚠️ যে দুটি সীমা মেনে নিতে হয়েছে

1. **দাতার কার্ড নম্বর কখনো আমাদের পাতায় নেওয়া যাবে না।** নিজের পাতায় কার্ড নম্বর নিলে PCI-DSS-এর **SAQ-D** স্তরে পড়তে হয় (বার্ষিক অডিট, পেনিট্রেশন টেস্ট) — একটি দাতব্য ফাউন্ডেশনের পক্ষে বহন করা অসম্ভব, আর ফাঁস হলে দায়ও ফাউন্ডেশনের। তাই ৩D কার্ডটি এখন **গেটওয়েতে যাওয়ার** কার্ড; নম্বর/Expiry/CVV টাইপ হয় গেটওয়ের পাতায়। `donation.html`-এর কার্ড-ঘরগুলো আগেই সরানো হয়েছে।
2. **মার্চেন্ট অ্যাকাউন্ট ছাড়া আসল টাকা আসে না।** তাই স্যান্ডবক্সে পুরো পথটি বানানো ও পরীক্ষা করা হয়েছে; পর্দায় স্পষ্ট অ্যাম্বার ব্যাজ থাকে ("পরীক্ষামূলক — আসল টাকা কাটা হয় না")। **নিয়ম: স্যান্ডবক্স অবস্থায় দাতাকে কখনো "দান সম্পন্ন" বলে বিশ্বাস করানো যাবে না।**

### গঠন — চারটি স্তর

| স্তর | কী করে |
|---|---|
| `payment_sessions` টেবিল | প্রতিটি লেনদেনের রেকর্ড — tran_id, purpose, amount, method, payload, status, val_id, callback |
| `start_payment()` RPC | **অঙ্ক চূড়ান্ত করে** ও সেশন খোলে (anon-ও পারে) |
| `pay-start` Edge Function | গেটওয়েতে সেশন বানিয়ে **রিডাইরেক্ট URL** দেয় |
| `pay-callback` Edge Function | গেটওয়ের চারটি পথ (`success`/`fail`/`cancel`/`ipn`) ধরে, **যাচাই করে**, `settle_payment()` ডাকে |

- **`start_payment(purpose, method, amount, payload)`** — ⚠️⚠️ **অঙ্ক কখনো ব্রাউজারের কথায় হয় না।** দানে ৳১০–৳৫,০০,০০০ সীমা; সঞ্চয়ে **লক করা কিস্তি** (কেবল "যখন খুশি" স্কীমে টাইপ করা অঙ্ক); কিস্তিতে **বকেয়া** (`amount_due − amount_paid`)। মালিকানাও এখানেই যাচাই হয় — অন্যের হিসাবে জমা বা অন্যের কিস্তি পরিশোধ শুরুই করা যায় না। `payload`-এর বাড়তি সব ঘর **ফেলে দেওয়া হয়**, কেবল দরকারিগুলো রাখা হয়।
- **`settle_payment(...)`** — service_role only, **idempotent** (একই tran_id দুবার এলে দ্বিতীয়বার কিছুই হয় না)। ⚠️ ব্যবসায়িক কাজটি **বিদ্যমান RPC-ই করে** (`record_donation` · `deposit_to_savings` · `record_loan_payment`), তাই কিস্তির নিয়ম, ভাউচারের ট্রিগার, মেইলের কিক — সব আগের মতোই চলে, নতুন করে কিছু লেখা হয়নি।
  ⚠️ সেশনের মালিক সেজে বসা হয় `set_config('request.jwt.claims', …, true)` দিয়ে (লেনদেন-স্থানীয়)। **`set_config('role', …)` চালানো যায় না** — Postgres SECURITY DEFINER ফাংশনের ভেতরে সেটি সরাসরি আটকে দেয় (`cannot set parameter "role" within security-definer function`)। `auth.uid()` কেবল claims দেখে, তাই ঐটুকুই যথেষ্ট।
- **`payment_status(tran_id)`** — ফেরার পেজ এটিই পড়ে। কোনো ব্যক্তিগত তথ্য ফেরত দেয় না; `tran_id` নিজেই এলোমেলো গোপন স্ট্রিং।
- **`public_pay_config()`** — মাধ্যম + গেটওয়ের অবস্থা একবারে। ⚠️ পুরনো `public_pay_methods()` **অপরিবর্তিত** রাখা হয়েছে যেন পুরনো কোড না ভাঙে।

### ⚠️⚠️ যাচাই ছাড়া টাকা কখনো হিসাবে বসে না

ডকুমেন্টেশনে **`verify_sign`-এর হ্যাশ অ্যালগরিদম লেখাই নেই** (কোন ক্রমে কী জোড়া লাগে, store password-এর md5 কোথায় বসে — কিছুই নেই)। তাই সেটি অনুমান করে ব্যবহার করা হয়নি। বদলে ডকের **নির্দেশিত পথ**: `val_id` নিয়ে সার্ভার থেকে **Order Validation API**-তে জিজ্ঞাসা, তারপর চারটি শর্ত —
① `status` ∈ {`VALID`, `VALIDATED`} · ② `tran_id` মেলে · ③ মুদ্রা মেলে · ④ **অঙ্ক নিজের ডাটাবেজের সাথে মেলে** (`< ০.০১` পার্থক্য)।
একটিও না মিললে সেশন `failed` হয় ও কারণ লেখা থাকে। **নিয়ম: গেটওয়ের পোস্ট করা অঙ্ক বা অবস্থা কখনো বিশ্বাস করা যাবে না** — ব্রাউজার ঐ POST বদলে দিতে পারে।
⚠️ যাচাই API-তে পৌঁছানোই না গেলে অবস্থা **অপরিবর্তিত** রাখা হয় (`failed` করা হয় না) — IPN আবার এলে বা অ্যাডমিন মিলিয়ে দেখলে উদ্ধার হয়।

### ⚠️⚠️ চ্যানেল বাছাই — শ্রেণি ধরে অনুমান করা নিষিদ্ধ

SSLCommerz-এর ডকুমেন্টেশন ২০১৯ সালের; ওতে **নগদ, রকেট ও বাংলা QR-এর কি-ওয়ার্ড নেই**। তাই কি হার্ডকোড করা হয়নি — প্রতিটি সেশনের **লাইভ উত্তরের `desc[]` তালিকা** থেকে মেলানো হয়। লাইভ স্যান্ডবক্সে পাওয়া গেছে: `bkash` ✓ · `nagad` ✓ · `dbblmobilebanking` (= রকেট) ✓ · `upay` ✓ · কার্ড ✓ · ইন্টারনেট ব্যাংকিং ✓ · **বাংলা QR নেই**।

**⚠️ এখানেই একটি বিপজ্জনক ফাঁদ ধরা পড়েছিল।** প্রথম সংস্করণে শ্রেণি (`type`) ধরে ফলব্যাক ছিল — বাংলা QR তালিকায় না থাকায় `mobilebanking` শ্রেণির **প্রথমটি (বিকাশ)** বেছে নিত, অর্থাৎ **QR চেপে দর্শক বিকাশে চলে যেতেন ও ভুল মাধ্যমে টাকা পাঠাতেন**। `gwtype` পুরোপুরি বাদ দেওয়া হয়েছে। এখন দুটি স্পষ্ট ঘর:
- `gw` — সোজা যাওয়ার চ্যানেল কি (লাইভ তালিকার সাথে **হুবহু** মেলানো হয়)
- `multi` — না মিললে গেটওয়ের পাতায় কেবল এই মাধ্যমগুলোই দেখাবে (`multi_card_name`)

**নিয়ম: পেমেন্ট চ্যানেল কখনো "কাছাকাছি" মিলিয়ে বাছা যাবে না — হুবহু কি না মিললে গেটওয়ের নিজের বাছাই-পাতায় পাঠাতে হবে।**

**যাচাই (৮টি মাধ্যমে লাইভ `pay-start` ডেকে):** বিকাশ → `bkash` সোজা · নগদ → `nagad` সোজা · রকেট → `dbblmobilebanking` সোজা · উপায় → `upay` সোজা · কার্ড → গেটওয়ের কার্ড-পাতা · ব্যাংক → ইন্টারনেট ব্যাংকিং পাতা · **বাংলা QR → গেটওয়ের পূর্ণ পাতা (বিকাশ নয়)** · কার্যালয়ে নগদ → বাংলায় স্পষ্ট বার্তা দিয়ে আটকায়।
⚠️ EasyCheckOut পাতায় `multi_card_name` দিলেও তিনটি ট্যাব (CARDS/MOBILE BANKING/NET BANKING) দেখা যায় — গেটওয়ের নিজের আচরণ, আমাদের কিছু করার নেই।

### ✅ শেষ-থেকে-শেষ যাচাই — স্যান্ডবক্সে সত্যিকারের একটি পেমেন্ট

৳১৫০ দান, কার্ড মাধ্যম, পুরো পথ ব্রাউজারে চালিয়ে:
গেটওয়ের পাতা খুলেছে → NEXUS চ্যানেল → OTP `111111` → "Success" →
`pay-callback` ডাকা হয়েছে → Validation API `status: VALID`, `risk_title: Safe` →
অঙ্ক ও মুদ্রা মিলেছে → **দান তৈরি `KH-DON-260922-65471`**, `payment_method = 'card'`, `status = confirmed` →
`val_id`, `bank_tran_id`, `card_type` (`NEXUS-Dutch Bangla`), `store_amount` ৳১৪৬.২৫ সব সংরক্ষিত →
ব্রাউজার ঠিক জায়গায় ফিরেছে: `payment-return.html?tran=…&r=success` →
**রশিদ নিজে থেকেই ই-মেইলে গেছে** (`voucher_mail_log` → `sent`, দানের ~১ মিনিট ৪৪ সেকেন্ড পর)।
পরীক্ষার সব সারি (দান, দাতার যোগফল, সেশন, মেইল লগ) **মুছে ফেলা হয়েছে** — ফাউন্ডেশনের হিসাব অটুট।

**নিরাপত্তা যাচাই (ভূমিকা পাল্টে):** ৳৫ দান **আটকেছে** · অজানা মাধ্যম (`paypal`) **আটকেছে** · লগইন ছাড়া সঞ্চয়ে জমা **আটকেছে** ("আগে লগইন করুন") · একই tran_id দুবার settle করলে দ্বিতীয়বার `already: true`, **দ্বিতীয় দান তৈরি হয়নি** · নয়টি মাধ্যমের প্রতিটির id ঠিকভাবে কলামে বসেছে।

### ⚠️⚠️ আরেকটি পুরনো ফাঁদ ধরা পড়ল — দুই জায়গায় সাদা-তালিকা

`donations.payment_method`-এ CHECK ছিল কেবল `bkash/nagad/bank/cash/card/other`, **আর `record_donation`-এর ভেতরেও** একই রকম একটি তালিকা। ফলে ২১ সেপ্টেম্বরে যোগ করা **রকেট · উপায় · বাংলা QR · এজেন্ট** দিয়ে দান করলে সেটি নীরবে `'other'` হয়ে যেত — অর্থাৎ **কোন মাধ্যমে টাকা এসেছে তা হারিয়ে যেত**। দুটোই ঠিক করা হয়েছে (নয়টি id + `other`, সাথে `lower()`)।
**নিয়ম (এটি পঞ্চমবার): নতুন মান যোগ করার আগে `pg_constraint` **এবং** সংশ্লিষ্ট ফাংশনের ভেতরের তালিকা — দুটোই দেখে নিতে হবে।**

### নতুন পেজ — `payment-return.html`

গেটওয়ে থেকে ফিরে এখানেই আসা হয়। ⚠️ **এই পেজ কোনো সিদ্ধান্ত নেয় না** — `payment_status()` পড়ে দেখায়; URL-এর `r=success` একটি ইঙ্গিত, প্রমাণ নয়।
⚠️ IPN সাধারণত দর্শকের ফেরার **আগেই** পৌঁছায়, কিন্তু সবসময় নয় — তাই ১.৫ সেকেন্ড পর পর ১৪ বার (~২১ সেকেন্ড) দেখা হয়। তারপরও অমীমাংসিত থাকলে **"ব্যর্থ" কখনো লেখা হয় না**, বলা হয় "নিশ্চিতকরণ এখনো আসেনি, টাকা কেটে থাকলে নিজে থেকেই বসে যাবে"। **টাকা কেটে গিয়ে "ব্যর্থ" দেখানো সবচেয়ে খারাপ ভুল।**
⚠️ `success_url` **Edge Function-এ** যায়, স্ট্যাটিক HTML-এ নয় — গেটওয়ে POST করে, আর Vercel-এর স্ট্যাটিক পাতা POST নিতে পারে না। ফাংশন যাচাই করে তারপর ৩০২ দিয়ে এই পেজে পাঠায়।

### `KHUI.payPicker()` — এখন গেটওয়ে-লঞ্চার

নতুন opts: `purpose` · `amount` (সংখ্যা বা ফাংশন) · `payload` · `beforePay` (false দিলে গেটওয়েতে যাওয়া আটকায়)। নতুন হ্যান্ডেল: `.pay()` · `.online()` · `.refresh()`।
- **বাংলা QR নির্বাচনের বাইরে, নিজের ঘরে** (`.kh-pay-qrbox`) — কোন মাধ্যম বাছা হয়েছে তা যাই হোক, সবসময় খোলা (ব্যবহারকারীর শর্ত)। ঘরটি **নিজের সাদা পটভূমি বহন করে**, কারণ QR সাদা ছাড়া স্ক্যান হয় না — আর তাতেই দুই থীমে মাপ এক থাকে।
- **হিসাব নম্বর `<details>`-এর আড়ালে** (`.kh-pay-acc` — "প্রতিষ্ঠানের হিসাব বিবরণী")।
  ⚠️ সারিগুলো আগে গাঢ় কার্ডে ছিল (সাদা লেখা); এখন হালকা ঘরে, তাই রঙ উল্টে দিতে হয়েছে। বিশেষত **কপি বাটনের `rgba(0,0,0,.26)` সাদা ঘরে ধূসর হয়ে যায় ও সাদা লেখা ১.৬:১-এ নামে** — তাই ওখানে ভরাট `#3b3450` (১১.৬৯:১)।
- পরিশোধের বাটন নিজের সবুজ গ্র্যাডিয়েন্ট বহন করে (`#0f7a43 → #0a5c33`, সাদা লেখা **৫.৪০–৮.১০:১**), তাই দুই থীমে এক। স্যান্ডবক্স ব্যাজ `#ffe0a3` on `#4a3410` = **৯.১৮:১**; ত্রুটির লেখা `#ffb4a2` ≈ ১০.৫:১।
- CSS: `kh-ui.css` **সেকশন ২৫**-এর শেষে; লাইট থীমের ঢালাও নিয়ম (সেকশন ৯) থেকে বাঁচাতে নতুন লেখাগুলোর রঙও ওখানে ফেরানো আছে। **নতুন কোনো লেখা যোগ করলে ঐ ব্লকেও দিতে হবে।**

### ⏱️ রশিদ ~২ মিনিট পরে যায় — কেন, আর কীভাবে দ্রুত করা যাবে

ভাউচারের মেইল ৯০ সেকেন্ড অপেক্ষা করে **ব্রাউজারের হুবহু কপির** জন্য (`pending_voucher_mails`-এর grace)। কিন্তু গেটওয়ে-পেমেন্টে দর্শক তখন ফেরার পেজে — **হুবহু কপি কখনোই আসবে না**। তাই ৯০ সেকেন্ড + ২ মিনিটের cron = ~২ মিনিট দেরি হয়, আর সাধারণ HTML রশিদ যায় (ছবি/PDF ছাড়া)।
**ভবিষ্যতে দ্রুত করতে:** `pending_voucher_mails`-এর "তৈরি" শর্তে যোগ করতে হবে *"অথবা `payment_sessions`-এ এই `transaction_ref`-টি `paid`"* — তখন গেটওয়ে-পেমেন্টের রশিদ সাথে সাথেই যাবে, আর দৌড়ের ফাঁদও ফিরে আসবে না (কেবল নিশ্চিতভাবে গেটওয়ে-নিষ্পন্ন রেকর্ডই ছাড়া পায়)।

### সেটিংস ও গোপন কি

- `app_settings.pay_gateway` = `{provider, mode, enabled, manual_enabled, site_url}` — `mode` `'sandbox'`→`'live'` করলেই লাইভ, কোড বদলাতে হয় না।
- **⚠️⚠️ `SSLC_STORE_ID` ও `SSLC_STORE_PASSWD` কেবল Supabase Dashboard → Project Settings → Edge Functions → Secrets-এ** — **কখনো কোডে বা চ্যাটে নয়**। কি না দিলে স্যান্ডবক্সে ডকুমেন্টেশনের প্রকাশ্য টেস্ট স্টোর (`testbox`) ব্যবহার হয়, তাই মার্চেন্ট অ্যাকাউন্ট ছাড়াই পুরো পথ পরীক্ষা করা যায়।
- **⚠️ দুটি ফাংশনেই `verify_jwt: false`** (প্রকল্পের নিয়ম) — অতিথিও দান করেন, আর গেটওয়ের সার্ভার-থেকে-সার্ভার IPN-এ কোনো JWT থাকে না। ডিপ্লয়ের পর ফেরত JSON-এ মিলিয়ে দেখা হয়েছে।
- `chat_knowledge`-এর "৩. ওয়েবসাইটে কী কী সেবা আছে" নথিতে পুরো ব্যবস্থাটি লেখা হয়েছে (**স্থায়ী নিয়ম**)। যাচাই: দুটি dry run-এ ঠিক অধ্যায় এসেছে, আর সত্যিকারের প্রশ্নে বট বলেছে নগদে সোজা যাওয়া যায় **এবং** "আমাদের ওয়েবসাইটে কখনো কার্ডের নম্বর দিতে হয় না"।

**ক্যাশ-বাস্টিং — এখন `?v=33`**, সব ১৯টি HTML-এ। ⚠️ `loan-application.html`-এর NUL বাইটের কারণে ripgrep ঐ ফাইলটি বাদ দেয় — আলাদা করে বদলাতে হয়েছে (গুনে মিলিয়ে দেখা: দৃশ্যমান ১৮ ফাইলে ৩৬ + ঐ ফাইলে ২)।

## 🔳 ডেমো বাংলা QR (২২ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর নির্দেশ:** *"তুমি নিজে থেকে একটি ডেমো বাংলা কিউ আর বসিয়ে দাও। যেটা স্ক্যান করলে কোন নির্দিষ্ট একাউন্ট এ যাবে না। শুধু সিস্টেম এর কার্যকারিতা পরীক্ষা করার জন্য।"*

**⚠️⚠️ যা ইচ্ছে করেই বানানো হয়নি — কাঠামোগতভাবে বৈধ EMVCo / বাংলা-QR।** ওটা বানালে বিকাশ/নগদ/ব্যাংকের অ্যাপ সেটিকে **আসল পেমেন্ট QR হিসেবে গ্রহণ করে** ফেলত এবং ভেতরে লেখা মার্চেন্ট নম্বরে টাকা পাঠানোর চেষ্টা করত — সেই নম্বর হয় অস্তিত্বহীন, নয়তো **অন্য কারো**। কেউ সত্যিই টাকা পাঠিয়ে দিলে তা আর ফেরানো যেত না।

**যা বানানো হয়েছে:** একটি সাধারণ **টেক্সট QR**। স্ক্যান করলে কেবল এই লেখাটি দেখায় —
`KORJE HASANA FOUNDATION - DEMO QR (TEST ONLY). No bank or MFS account is linked to this code. Do not send money. https://korje-hasana-website.vercel.app`
কোনো পেমেন্ট শুরু হয় না, কারণ এটি পেমেন্ট-ফরম্যাটই নয় (EMV হলে `000201` দিয়ে শুরু হত — যাচাই করে দেখা হয়েছে, হয় না)।

- **কোথায়:** পাবলিক বাকেট `site-assets`, পথ `pay/bqr-demo.png` (৫৬০×৬৪৮, ৩০ KB)। সেটিংসে `app_settings.pay_methods.bqr.image`।
- **ছবির গায়েই সতর্কবার্তা** — লাল ড্যাশ-বর্ডার + `DEMO QR - NOT A PAYMENT CODE` + `No account linked. System testing only.`। ⚠️ ক্যাপশন **ইংরেজিতে**, কারণ ক্যানভাসে বাংলা যুক্তাক্ষর ফন্টভেদে ভেঙে আসতে পারে; বাংলা লেখাটি কম্পোনেন্টের HTML-এ আছে। ছবিটি একা কোথাও শেয়ার হলেও যেন "ডেমো" বোঝা যায় — এজন্যই ছবির ভেতরে বসানো।
- **`demo: true` রাখা হয়েছে** — তাই কার্ডের লাল সতর্কবার্তা ও টাইলের লাল বিন্দু আগের মতোই থাকে। **আসল বাংলা QR এলে শুধু `image` বদলে `demo` পতাকা তুলে দিলেই হবে**, কোড ছুঁতে হবে না।
- **⚠️ কীভাবে বানানো হলো (বাশ স্যান্ডবক্স বন্ধ):** লাইভ পেজের ব্রাউজারেই QR আঁকা → ক্যানভাসে সীমানা ও ক্যাপশন → `toBlob` → সরাসরি স্টোরেজে আপলোড। এভাটারের মতোই **দুটি সাময়িক স্টোরেজ পলিসি** (`site_assets_tmp_qr_write`/`_update`, কেবল `pay/` ফোল্ডারে) বসিয়ে, কাজ শেষে **ড্রপ করা হয়েছে**। যাচাই: এখন অতিথি আপলোডে `new row violates row-level security policy`।
  ⚠️ `donation.html`-এ আগে থেকেই **qrcodejs** (কনস্ট্রাক্টর API) লোড হয়, তাই npm-এর `QRCode.toCanvas` পাওয়া যায় না — `if (!window.QRCode)` শর্তে নতুন লাইব্রেরি আসেই না। বিদ্যমানটিই ব্যবহার করতে হয়।
- **যাচাই:** আপলোড করা ছবিটি আবার নামিয়ে `jsQR` দিয়ে ডিকোড করে দেখা গেছে লেখাটি **হুবহু** ফিরে আসে, আর `^000201` মেলে না (অর্থাৎ পেমেন্ট QR নয়)। অতিথি `public_pay_config()` থেকে ছবির ঠিকানা পান, `demo` পতাকাও পান।

## 💳 কাচের কার্ড-পর্দা ফিরে এসেছে — `KHUI.payStage()` (২২ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর প্রশ্ন:** *"এত কস্ট করে যে কার্ড পেমেন্ট এর স্ক্রীন তৈরী করা হলো সেটা কোথায় ?"*

**⚠️⚠️ এটি আমার ভুল ছিল, আর ঠিক উল্টো দিক থেকে একই নিয়ম ভাঙা।** PCI-DSS-এর কারণে `donation.html` থেকে কার্ডের ঘরগুলো সরানো হয়েছিল — কিন্তু **কাচের পর্দাটিকে নতুন কোনো ঘর দেওয়া হয়নি**, ফলে ২০ সেপ্টেম্বরে হুবহু নকল করা ডিজাইনটি কার্যত হারিয়ে গিয়েছিল। ২১ সেপ্টেম্বরের নিয়মটি ছিল *"নতুন UI বানানোর পর কোথা থেকে পাওয়া যাবে সেই পথটিও যোগ করতে হবে"* — এবার **পুরনো UI-এর পথ কেটে দিয়ে নতুন ঘর না দেওয়ায়** একই ক্ষতি হলো।
**নিয়ম: কোনো UI-এর প্রবেশপথ সরালে সেটিকে কোথায় বসানো হবে তা একই কাজে ঠিক করতে হবে — নাহলে কাজটি সরানো নয়, মুছে ফেলা।**

**সিদ্ধান্ত (AskUserQuestion-এ):** পর্দাটি **দুই অবস্থাতেই** আসবে (প্রসেসিং + সফল) · কার্ডের চেহারা **যেমন বানানো হয়েছিল ঠিক তেমন** · `card-payment.html` **রেখে দাও, কিন্তু কোথাও লিংক দিয়ো না**।

### কোথায় বসেছে

| অবস্থা | কে দেখায় |
|---|---|
| `busy` — "Processing Secure Transaction…" | `payPicker.pay()` — গেটওয়েতে রওনা হওয়ার মুহূর্তে |
| `busy` — "যাচাই চলছে" | `payment-return.html` — ফিরে আসার পর, নিশ্চিতকরণের অপেক্ষায় |
| `done` — সবুজ কার্ড + টিক | `payment-return.html` — `payment_status()` `paid` বললে |
| `bad` — লাল টিক | `payment-return.html` — `failed`/`cancelled` |

- **`KHUI.payStage(opts)`** — `kh-ui.js`; পূর্ণ পর্দার ওভারলে, ফেরত দেয় `{el, set(o), close()}`। `set()` দিয়ে একই পর্দা `busy → done` বদলে যায়, নতুন করে খোলা হয় না।
- **কার্ডের গায়ে কার্ড নম্বরের জায়গায় বসে লেনদেন নম্বর** (`cpGroup()` চার-চার করে ভাগ করে, ঠিক কার্ড নম্বরের মতো) — দাতার কোনো তথ্য কার্ডে নেই, নেওয়াও হয় না। নাম, তারিখ ও মাধ্যমের চিহ্ন আগের জায়গাতেই।
- **⚠️ সব লেখা `textContent`-এ**, লিংক `payEsc()` দিয়ে — মডেল/গেটওয়ের কোনো লেখা সরাসরি `innerHTML`-এ নয়।
- CSS: `kh-ui.css` **সেকশন ২৬**, প্রিফিক্স `.kh-cp-`। ওভারলে `z-index: 10050` — statsDetail (১০০২০), ক্যারোসেল লাইটবক্স (১০০০০), সিগনেচার পট্টি (১০০০৫), হোয়াটসঅ্যাপ (৯৯৯৯), গ্লাস নেভ (৯০০০), চ্যাট (৯৪০০) — সবার উপরে।
- **`card-payment.html` অপরিবর্তিত ও অলিংকড** (ব্যবহারকারীর সিদ্ধান্ত) — মূল ডিজাইনের রেফারেন্স হিসেবে থাকে।

### ⚠️⚠️ নতুন নিয়ম — প্রতিটি অবস্থা আলাদা করে মাপতে হবে

সবুজ "সফল" কার্ডটি মূল `card-payment.html` থেকে হুবহু নেওয়া হয়েছিল, আর ঐ পেজের **স্বাভাবিক** অবস্থা ২০ সেপ্টেম্বরে মেপে পাস করেছিল। কিন্তু সফল অবস্থার সবুজ `#12b981`-এ মেপে দেখা গেল **তিনটিই AA ফেল** — লেনদেন নম্বর (সাদা) **২.৫৪:১** · PLATINUM `#eaffd8` **২.৩৯:১** · লেবেল `rgba(255,255,255,.72)` **১.৯৪:১**। অর্থাৎ **মূল ডিজাইনের সফল অবস্থাটি কখনো মাপাই হয়নি**।

সমাধান: গ্র্যাডিয়েন্ট গাঢ় করা হয়েছে `#0b6347 → #085438 48% → #064029`, লেবেল `.72 → .78`। আবার মেপে **"AA ফেল: কিছু নেই"** — সবচেয়ে খারাপ **৫.১২:১**; নম্বর ৭.২৭–১১.৮৪:১ · PLATINUM ৬.৮৫–১১.১৬:১।

**নিয়ম: একটি কম্পোনেন্টের একটি অবস্থা AA পাস করলেই বাকিগুলো পাস করে না। রঙ বদলায় এমন প্রতিটি অবস্থা (স্বাভাবিক · সফল · ব্যর্থ · নিষ্ক্রিয় · হোভার) আলাদা করে মাপতে হবে।** পুরনো `card-payment.html`-এর সফল অবস্থাটি এখনো ঐ পুরনো উজ্জ্বল সবুজেই আছে — লিংক করা নেই বলে ক্ষতি নেই, কিন্তু কখনো ব্যবহার করতে হলে আগে রঙ ঠিক করতে হবে।

### যাচাইয়ের বাকি মান (দুই থীমে হুবহু এক — পর্দাটি নিজের গাঢ় পটভূমি বহন করে)

শিরোনাম ১৭.১৬:১ · নোট ৮.৩৯:১ · সোনালি `b` ৮.৬৪:১ · বাটন ১০.৬৪:১ · প্রধান বাটন ৫.৪১–৮.১০:১ · স্যান্ডবক্স ব্যাজ ৯.১৮:১ · সবুজ টিক ৬.০২:১ · লাল টিক ৭.০১:১ · স্বাভাবিক কার্ডের নম্বর ১৫.০৬:১ · নিচের ছোট লেখা ৪.৬৮:১।
লাইভে বসিয়ে মাপা: `card3d` ৪৫২×২৭৯, **আড়াআড়ি স্ক্রল ০**, বাংলা নাম ও তারিখ কার্ডের ভেতরেই।
⚠️ অবস্থা বদল **স্ক্রিনশটে নয়, `getComputedStyle()`/`getBoundingClientRect()` মেপে** যাচাই করা হয়েছে (লুকানো ট্যাবের ফাঁদ — এই ফাইলে পঞ্চমবার)।

**ক্যাশ-বাস্টিং — এখন `?v=35`**, সব ১৯টি HTML-এ (দৃশ্যমান ১৮ ফাইলে ৩৬ + `loan-application.html`-এ ২)।

### ⚠️⚠️ পর্দাটি আসলে কখনো ওঠেইনি — `var stage` নামের সংঘর্ষ (সংশোধন: ২৩ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর পর্যবেক্ষণ:** *"কার্ড পেমেন্ট এর কার্ডটি এখনও আগের মত আছে। নতুন কার্ডটি কাজ করছে না?"* — **ঠিক, কাজ করছিল না।** ২২ সেপ্টেম্বরে সব যাচাই "পাস" দেখিয়েছিল, অথচ লাইভে পরিশোধের বাটনে চাপলে **কিছুই হত না** — কাচের পর্দাও উঠত না, গেটওয়েতেও যেত না।

**আসল কারণ — একটিমাত্র নাম।** `payPicker()`-এর ভেতরে আগে থেকেই ছিল

```js
var stage = host.querySelector('.kh-pay-stage');   // পিকারের নিজের ঘর
```

আর `pay()`-এর প্রথম দুই লাইন ওটিই ব্যবহার করে (`stage.querySelector('.kh-pay-goerr')`)। কাচের পর্দা যোগ করার সময় ঐ একই ফাংশনের ভেতরে লেখা হয়েছিল `var stage = null;` — **হয়েস্টিংয়ের কারণে ফাংশনের একদম শুরুতেই `stage` হয়ে যেত `undefined`**, তাই `pay()` তার **প্রথম লাইনেই** `Cannot read properties of undefined (reading 'querySelector')` ছুড়ে মরে যেত। ভেতরের `try/catch`গুলো ভুলটাকে গিলে ফেলত, তাই পর্দায় কোনো বার্তাও আসত না।

এখন নাম `cpStage` — বাইরের `stage` অস্পৃশ্য।

**⚠️ কেন আগের যাচাইয়ে ধরা পড়েনি — এটাই আসল শিক্ষা।** ২২ সেপ্টেম্বরে `payStage()` **আলাদা করে** ডেকে দেখা হয়েছিল (ঠিকই কাজ করত), কনট্রাস্ট মাপা হয়েছিল, মার্কআপ গোনা হয়েছিল — কিন্তু **আসল বাটনটিতে কখনো ক্লিক করা হয়নি**। কম্পোনেন্ট একা ঠিক থাকলেও ডাকার জায়গায় ভাঙা ছিল।

**নিয়ম: নতুন কোনো UI শুধু আলাদা করে ডেকে যাচাই করা যাবে না — ব্যবহারকারী যেভাবে পাবেন ঠিক সেভাবে, আসল বাটনে ক্লিক করে, শেষ পর্যন্ত চালিয়ে দেখতে হবে। সাথে `unhandledrejection`/`error` লিসেনার বসিয়ে দেখতে হবে কোনো ভুল নীরবে গিলে ফেলা হচ্ছে কি না** (`try/catch` থাকলে কনসোলেও কিছু আসে না)।

**নিয়ম: একই ফাংশনের ভেতরে বাইরের কোনো ভেরিয়েবলের নামে `var` লেখা যাবে না** — `var` হয়েস্ট করে, তাই ভুলটা ঐ লাইনে নয়, **ফাংশনের শুরু থেকেই** শুরু হয়।

**যাচাই (এবার আসল পথে, লাইভ পেজে সংশোধিত `kh-ui.js` চালিয়ে):** দানের পাতায় ৳৫০০ → "কার্ড / POS" → **"এখনই পরিশোধ করুন"** → কাচের পর্দা উঠল (`kh-cp is-busy is-in`, উচ্চতা ৯১২px), কার্ডের গায়ে লেনদেন নম্বর `KH26 0923 2244 5960 FFA3`, **কোনো unhandled rejection নেই**, আর ব্রাউজার সত্যিই SSLCommerz-এর স্যান্ডবক্স পাতায় পৌঁছেছে ("PAY 500 BDT")।

## ⚙️ টাকা নেওয়ার মাধ্যম — অ্যাডমিন সেটিংস UI (২২ সেপ্টেম্বর ২০২৬)

এতদিন ফাউন্ডেশনের আসল নম্বর/হিসাব বসাতে বা ডেমো সতর্কবার্তা তুলতে **SQL লিখতে হত** — সেটি ব্যবহারকারীর কাজ নয়। এখন সুপার অ্যাডমিন → জেনারেল সেটিংস → **"টাকা নেওয়ার মাধ্যম"**।

- **নয়টি মাধ্যম, প্রতিটি একটি ভাঁজ-করা সারিতে** (`<details class="pm-row">`) — শিরোনামেই "চালু/বন্ধ" ও "ডেমো তথ্য" ব্যাজ দেখা যায়, খুললে ভেতরে ঘরগুলো। মাধ্যমভেদে ঘর আলাদা (`PM_FIELDS`): MFS-এ নম্বর ও ধরন · ব্যাংকে হিসাব/শাখা/রাউটিং/SWIFT · বাংলা QR-এ মার্চেন্ট ও ছবি · কার্যালয়ে ঠিকানা ও সময় · এজেন্টে আউটলেট।
- **ডেমো টিকচিহ্ন তুললেই লাল সতর্কবার্তা ও টাইলের লাল বিন্দু চলে যায়** — সার্ভারে `demo` কি-টি একেবারে মুছে দেওয়া হয়। আসল তথ্য বসানোর পুরো কাজটি এখন এক পর্দাতেই।
- **QR ছবি আপলোড** — `site-assets` বাকেটের `pay/` ফোল্ডারে; আপলোড হলে ঠিকানার ঘরটি নিজে থেকেই ভরে যায়। এর জন্য **স্থায়ী স্টোরেজ পলিসি** বসানো হয়েছে: `site_assets_admin_write`/`_update`/`_delete` — কেবল `is_admin()`, পড়া আগের মতোই পাবলিক। (আগে এভাটার ও ডেমো QR-এর সময় সাময়িক পলিসি বসিয়ে ড্রপ করতে হয়েছিল — আর লাগবে না।)
- **গেটওয়ের সুইচ** — স্যান্ডবক্স ↔ লাইভ, গেটওয়ে বাটন চালু/বন্ধ, ম্যানুয়াল পথ চালু/বন্ধ, সাইটের ঠিকানা। ⚠️ **লাইভ করার আগে `confirm()`** — "Supabase-এ `SSLC_STORE_ID` ও `SSLC_STORE_PASSWD` বসানো আছে তো?"। কি ছাড়া লাইভ করলে কোনো পেমেন্টই সফল হত না, অথচ দাতা বুঝতেন না।

### ⚠️⚠️ `gw` ও `multi` UI থেকে বদলানো যায় না — ইচ্ছাকৃত

চ্যানেল কি (`gw`, `multi`) ভুল হলে দর্শক **ভুল মাধ্যমে টাকা পাঠাবেন** (২২ সেপ্টেম্বরে বাংলা QR → বিকাশ-এর ফাঁদটি ঠিক এই কারণেই হয়েছিল)। তাই:

- ক্লায়েন্ট ঐ ঘরগুলো **পাঠায়ই না** (যাচাই করা: সংগৃহীত অবজেক্টে `gw`/`multi`/`label` নেই), আর
- **`admin_save_pay_methods(p_methods, p_gateway)`** (SECURITY DEFINER, `is_super_admin()`) কেবল সাদা-তালিকাভুক্ত ঘরগুলো merge করে — `gw`, `multi`, `online`, `label` অস্পৃশ্য থাকে। অজানা মাধ্যম যোগ করা যায় না, ফাঁকা টেক্সট দিলে কি-টি মুছে যায়, `mode` `'sandbox'`/`'live'`-এর বাইরে গেলে `sandbox`, `site_url` `^https?://` না হলে উপেক্ষা।
- পড়া হয় **`admin_pay_settings()`** দিয়ে (super admin only) — `public_pay_config()` কেবল চালু মাধ্যম দেয়, তাই সেটিংসে চলত না।

**যাচাই (ভূমিকা পাল্টে, রোলব্যাক করা DO ব্লকে):** অতিথি পড়া **আটকেছে** · সাধারণ সদস্যের সেভ **আটকেছে** · `gw:["HACK"]` ও `multi:"bkash"` পাঠিয়েও রকেটের মান **অপরিবর্তিত** (`["rocket","dbblmobilebanking"]` / `dbblmobilebanking`) · `paypal` নামে নতুন মাধ্যম **যোগ হয়নি** · `demo:false`-এ কি-টি মুছেছে · ফাঁকা `office` মুছেছে · `mode:"LIVEXX"` → `sandbox` · `javascript:alert(1)` ঠিকানা **প্রত্যাখ্যাত**।

## 💸 অ্যাডমিন এন্ট্রিতেও নয়টি মাধ্যম (২২ সেপ্টেম্বর ২০২৬)

- **তিনটি select এখন `KHUI.PAY_METHODS` থেকে ভরে** — `pmFillSelects()` সব `select[data-pm-select]` ধরে (দানের `#dn-method`, সঞ্চয়ের `#se-method`, কিস্তির `#inst-method`), ডিফল্ট `cash`, শেষে "অন্যান্য"। ⚠️ `kh-ui.js` লোড হয় এই স্ক্রিপ্টের **পরে**, তাই `load` ইভেন্টে **ও** `switchPanel()`-এর শুরুতে — দুই জায়গাতেই ডাকা হয়; ফাংশনটি idempotent (`dataset.pmFilled`)।
- **দানের এন্ট্রিতে আগে সবসময় `'cash'` লেখা হত** — বিকাশ/ব্যাংকে আসা টাকাও নগদ হিসেবে জমা পড়ত। এখন মাধ্যম + TrxID দুটোই নেওয়া হয় (`transaction_ref`)। সঞ্চয়ের এন্ট্রিতে `payment_method` লেখাই হত না — এখন লেখা হয় ও ভাউচারে দেখানো হয়।
- **নাম বদলেছে চার জায়গাতেই** (প্রকল্পের নিয়ম): সাইডবার · `PANEL_TITLES` · প্যানেল-শিরোনাম · পারমিশন-লেবেল — "নগদ ডোনেশন এন্ট্রি" → **"সরাসরি দান এন্ট্রি"** (কি `cash_donation` অপরিবর্তিত)।
- **⚠️ দুটি `payment_method` ছাঁকনি সরাতে হয়েছে।** `loadTodayDonations()`-এ `.eq('payment_method','cash')` ও `loadDonationApprovals()`-এ `.neq('payment_method','cash')` — মাধ্যম বাছার সুযোগ আসার পর ঐ দুটি রাখলে অ্যাডমিনের করা এন্ট্রি **দুই তালিকা থেকেই নীরবে হারিয়ে যেত**। এখন আজকের সব দান মাধ্যমসহ দেখানো হয়, আর অনুমোদনের তালিকা কেবল অবস্থার পিল দিয়েই ছাঁকা হয়।

### ⚠️⚠️ ষষ্ঠবার — `repayments`-এও দুই জায়গায় সাদা-তালিকা

`repayments_payment_method_check` ছিল কেবল `cash/bkash/nagad/bank/other`, **আর `record_loan_payment`-এর ভেতরেও** হুবহু একই পাঁচটি। অর্থাৎ ২১ সেপ্টেম্বর থেকে `borrower-portal.html`-এ নয়টি মাধ্যম দেখানো হলেও **রকেট · উপায় · বাংলা QR · কার্ড · এজেন্ট দিয়ে দেওয়া কিস্তি নীরবে `'other'` হয়ে যাচ্ছিল** — কোন মাধ্যমে টাকা এসেছে তা হারিয়ে যেত (গেটওয়ে-পেমেন্টেও, কারণ `settle_payment` এই ফাংশনটিই ডাকে)। দুটোই নয়টি id + `other`-এ বাড়ানো হয়েছে, সাথে `lower()` ও `btrim()`।

**নিয়ম (আবারও): নতুন মান যোগ করার আগে `pg_constraint` **এবং** সংশ্লিষ্ট ফাংশনের ভেতরের তালিকা — দুটোই দেখে নিতে হবে। `donations`-এ ২২ সেপ্টেম্বরে ঠিক করা হয়েছিল, কিন্তু `repayments` তখন দেখা হয়নি। একটি টেবিলে ঠিক করলেই হবে না — একই মাধ্যম যেখানে যেখানে লেখা হয়, সব টেবিল ও ফাংশন একসাথে দেখতে হবে।**

### যাচাই (লাইভ পেজে নতুন মার্কআপ ও CSS বসিয়ে, মেপে)

- ৯টি সারি, ৩০টি ঘর, **আড়াআড়ি স্ক্রল ০** (৯০০px ও ৩৭৫px — দুই মাপেই, কোনো উপাদান ঘর ছাড়ায় না)।
- **AA ফেল: কিছু নেই** — সবচেয়ে খারাপ **৬.২১:১** (ঘরের লেবেল)। "বন্ধ" ব্যাজ ৭.৯৫ · "চালু" ৯.২৭ · আপলোড বাটন ৯.৩১ · "ডেমো তথ্য" ব্যাজ **১০.৪০** · আইকন ১০.৭২ · ইনপুট ১৩.৭৯ · মাধ্যমের নাম ১৫.৯৩ · শিরোনাম ১৭.৭৪:১। কোনো উপাদানে বা তার পূর্বপুরুষে `opacity < 1` নেই (মাপটি মিথ্যা হওয়ার ফাঁদ এড়াতে আলাদা করে দেখা হয়েছে)।
- সংগ্রহের যুক্তি: নয়টি কি-ই এসেছে, প্রতিটিতে `gw`/`multi`/`label` **অনুপস্থিত**, ডেমো-টিক তুললে `demo:false` যায়।
- `pmFillSelects()` দুবার ডাকলেও ডুপ্লিকেট হয় না; দশটি বিকল্প বসে, ডিফল্ট `cash`। `methodBn()` নয়টি id ও `other` ঠিক বাংলা নাম দেয়, অজানা id `esc()` হয়ে ফেরে।

**⚠️ `?v=` বাড়ানো হয়নি** — `kh-ui.css`/`kh-ui.js` ছোঁয়া হয়নি, পুরো পরিবর্তন `dashboard.html`-এর ভেতরে, আর HTML ব্রাউজার ক্যাশ করে না। এখনো **`?v=34`**।

**`chat_knowledge` বদলানো হয়নি — কারণ:** দর্শকের জন্য কোনো নিয়ম বা সেবা বদলায়নি (নয়টি মাধ্যমের কথা KB-তে আগেই আছে); পুরো পরিবর্তনটি অ্যাডমিন-অভ্যন্তরীণ ও একটি নীরব বাগের সংশোধন। **আসল নম্বর/হিসাব বসানোর সময় KB-র "৪. প্রতিষ্ঠান ও যোগাযোগ" নথিটি হালনাগাদ করতে হবে** (স্থায়ী নিয়ম)।

## 📊 স্টেটমেন্ট ও রিপোর্ট (dashboard.html · ২২ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর নির্দেশ:** *"এডমিন ও সুপার এডমিন থেকে দৈনিক, সাপ্তাহিক, মাসিক, বাৎসরিক ও এপর্যন্ত স্টেটমেন্ট দেখার ব্যবস্থা… শুরুতে ড্যাশবোর্ড আকারে উল্লেখযোগ্য তথ্য এবং নীচে আলাদা করে বিস্তারিত… প্রয়োজনে পিডিএফ আকারে ডাউনলোড।"*

**সিদ্ধান্তগুলো (AskUserQuestion-এ):** চারটি বিষয়ই — **দান · সঞ্চয় · ঋণ · তহবিল ও খরচ** · সময়সীমা **পিরিয়ড বাটন + কাস্টম তারিখ** · PDF **A4 প্রিন্ট-ভিউ** · **সব অ্যাডমিন সবকিছু** দেখবেন।

- **সাইডবার → "রিপোর্ট" বিভাগে "স্টেটমেন্ট ও রিপোর্ট"** (`panel-reports`)। উপরে পাঁচটি পিল (আজ · এই সপ্তাহ · এই মাস · এই বছর · এ পর্যন্ত) + "তারিখ থেকে/পর্যন্ত" ঘর, তারপর **ছয়টি সারসংক্ষেপ কার্ড**, তারপর চারটি বিস্তারিত অংশ।
- **⚠️ সপ্তাহ শনিবার থেকে শুক্রবার** — বাংলাদেশের প্রচলিত সপ্তাহ, রবিবার নয় (`rpRange('week')`-এ `(dow + 1) % 7`)।
- **⚠️ "আজ" মানে ঢাকার আজ, ব্রাউজারের নয়** — `rpToday()` `toLocaleDateString('en-CA', {timeZone:'Asia/Dhaka'})` ব্যবহার করে। বিদেশ থেকে লগইন করা অ্যাডমিনও একই দিন দেখবেন।

### `admin_report(p_from, p_to, p_rows)` — সব অঙ্ক সার্ভারেই

SECURITY DEFINER · `is_admin()` · `search_path=public` · `revoke ... from public, anon, authenticated` তারপর `grant`। `p_from` **null** দিলে "এ পর্যন্ত"; উল্টো তারিখ দিলে নিজেই ঠিক করে নেয়; `p_rows` ১০–২০০০-এ আটকানো (ডিফল্ট ৫০০)।

- **⚠️⚠️ ক্লায়েন্টে একটিও যোগফল কষা হয় না** — কার্ড, ভাগ-ভিত্তিক টেবিল, স্থিতি, বকেয়া, সব RPC থেকেই আসে। (`my_activity`-র মতোই নিয়ম: **আর্থিক সংখ্যা ডাটাবেজে তৈরি হবে, ক্লায়েন্ট কেবল পড়ে দেখাবে।**)
- **কোন তারিখ ধরে কাটা হয়:** দান `created_at` · সঞ্চয় `created_at` · ঋণ বিতরণ `disbursement_date` · কিস্তি `payment_date` · তহবিল `date`। ⚠️ অ্যাডমিন এন্ট্রিতে `created_at` বেছে নেওয়া তারিখেই বসে, তাই এটিই আসল লেনদেনের তারিখ।
- **⚠️ কিছু সংখ্যা সময়সীমার নয়, "শেষ তারিখ পর্যন্ত বর্তমান অবস্থা"** — মোট সঞ্চয় স্থিতি, বকেয়া ঋণ, বিলম্বিত কিস্তি, তহবিলের স্থিতি। পর্দায় ও ছাপায় সেটি স্পষ্ট লেখা আছে; **নতুন কোনো সংখ্যা যোগ করলে কোনটা রেঞ্জের আর কোনটা as-of তা লিখে দিতে হবে**, নাহলে অ্যাডমিন ভুল বুঝবেন।
- **তহবিলের হিসাব `public_stats`-এর মতোই** — দানের `operation_pct`/`revolving_pct` শতকরা অংশ + হাতে লেখা `income`, বিয়োগ `expense`+`adjustment`। স্থিতি cumulative (`date <= to`), আয়-ব্যয় রেঞ্জের।
- **যাচাই (ভূমিকা পাল্টে):** অতিথি ও সাধারণ সদস্য — দুজনেই **`admin only`**। অ্যাডমিন হিসেবে সর্বমোট রিপোর্টের প্রতিটি অঙ্ক কাঁচা SQL-এর সাথে **হুবহু** মিলেছে — দান ৳৪,০২,৯৫০ · সঞ্চয় স্থিতি ৳৩৫,৩৫০ · খরচ ৳৩,০০০ · অপারেশন স্থিতি ৳১৭,১৪৭.৫০ (= ৪,০২,৯৫০×৫% − ৩,০০০) · রিভলভিং ৳২০,১৪৭.৫০। চলতি মাসেও মিলেছে (৳৩,৭৩,৪৫০)। উল্টো তারিখ (`2026-12-31` → `2026-01-01`) নিরাপদে একই দিনে নেমেছে।

### A4 প্রিন্ট-ভিউ (`printReport()`)

**⚠️ ছবি নয়, HTML।** ভাউচারের `html2canvas + jsPDF` পথটি **এক পাতার** জন্য — রিপোর্টে চলে না (লম্বা টেবিল মাঝখানে কেটে যায়, লেখা সার্চ করা যায় না, ফাইল ভারী হয়)। তাই নতুন উইন্ডোতে `@page{size:A4 portrait;margin:14mm 10mm}` সহ HTML খোলে → ব্রাউজারের **"Save as PDF"**। বাংলা যুক্তাক্ষর নিখুঁত থাকে।

- `thead{display:table-header-group}` — প্রতি পাতায় টেবিলের শিরোনাম সারি ফিরে আসে; `tr{page-break-inside:avoid}` — সারি দুই পাতায় ভাগ হয় না; `h3{page-break-after:avoid}` — শিরোনাম একা পাতার শেষে পড়ে থাকে না।
- **⚠️ ৯০০ms অপেক্ষা করে তারপর `print()`** — Hind Siliguri ফন্ট নামার আগে ছাপালে বাংলা ফলব্যাক ফন্টে যেত।
- ছাপানোর কার্ডগুলো **সাদা কাগজের জন্য আলাদা** (`rpPrintCards`) — পর্দার গ্র্যাডিয়েন্ট কার্ড ছাপায় কালি নষ্ট করত ও কনট্রাস্ট ভাঙত।
- পপ-আপ আটকে গেলে বাংলা বার্তা দেখায়, নীরবে কিছুই না করে নয়।

### যাচাই (লাইভ পেজে আসল RPC-র ফল বসিয়ে, মেপে)

- ৬টি কার্ড · ৪টি অংশ · ৭টি টেবিল · ৫টি "কিছু নেই" বার্তা — সবই ঠিকভাবে বসে; **আড়াআড়ি স্ক্রল ০** (১০০০px ও ৩৭৫px দুই মাপেই, একটিও উপাদান ঘর ছাড়ায় না)।
- **AA ফেল: কিছু নেই।** কার্ডের ১৮টি মাপে সবচেয়ে খারাপ **৫.৪১:১** (সবুজ কার্ডের হালকা প্রান্ত `#0f7a43`); বাকি লেখায় সবচেয়ে খারাপ ৬.২৬:১ (টেবিলের `th`)। চিপ — নিশ্চিত ৯.২৭ · অপেক্ষমাণ ৯.১৯ · বাতিল ১০.৪:১। প্রিন্ট-ভিউয়ে সাদা কাগজে সবচেয়ে খারাপ **৫.৬৬:১**।
  ⚠️⚠️ **নতুন একটি মাপের ফাঁদ:** `getComputedStyle().backgroundImage` গ্র্যাডিয়েন্টকে **`rgb()` করে** ফেরত দেয়, hex নয়। প্রথম মাপে `#([0-9a-f]{6})` regex দিয়ে খোঁজায় কার্ডের পটভূমি ধরাই পড়েনি — সব কার্ড ভুল করে ১৭.৭৪:১ দেখাচ্ছিল (আসলে ৫.৪১–৭.৪)। **নিয়ম: গ্র্যাডিয়েন্ট কার্ডের কনট্রাস্ট মাপতে `rgba?\(…\)` ধরতে হবে, আর গ্র্যাডিয়েন্টের দুই প্রান্তই আলাদা করে মাপতে হবে।**
- **XSS:** দাতার নামে `<script>alert(1)</script>` বসিয়ে দেখা গেছে DOM-এ `script` ০টি, লেখাটি **টেক্সট হিসেবেই** দেখা যায় — প্রতিটি মান `esc()` দিয়ে যায়।
- প্রিন্ট-ভিউ A4 প্রস্থে ধরে (৭৭৯px < ৭৯৪px), ৬ কার্ড · ৭ টেবিল · ৪ শিরোনাম, দুই পাতার মতো।
- **⚠️ `KHUI.makeTablesScrollable()` ডাকা হয় না** — `dashboard.html`-এর নিজের `.tbl-wrap`-এ আগে থেকেই `overflow-x:auto` আছে (মেপে দেখা), ডাকলে টেবিল দুবার মোড়া হত।

**⚠️ এই কাজে `?v=` বাড়ানোর দরকার ছিল না** — `kh-ui.css`/`kh-ui.js` ছোঁয়া হয়নি; পুরো পরিবর্তন `dashboard.html` + একটি নতুন RPC-তে। (পরে ২৩ সেপ্টেম্বরের `payStage` সংশোধনে `kh-ui.js` বদলেছে, তাই এখন সব ফাইলে **`?v=35`**।)

### 🔜 চাইলে পরে

- CSV/Excel রপ্তানি (এখন কেবল PDF/প্রিন্ট)
- সারি ৫০০-র বেশি হলে পাতায় পাতায় (এখন সময়সীমা ছোট করতে বলা হয়)
- সদস্যভিত্তিক বা কেন্দ্রভিত্তিক ছাঁকনি

## 🧪 পরীক্ষার তথ্য নিয়ে দ্বিধা নয় — স্থায়ী নির্দেশ (২২ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর নির্দেশ (হুবহু ভাব):** *"এখন পর্যন্ত ওয়েব সাইটে যত ধরনের ডাটা আছে তার সবই ডেমো ডাটা। লাইভে যাওয়ার আগে আমি তোমাকে দিয়ে যে যে ডাটা ক্লিয়ার করতে হবে তা বলে দিব। আর পেমেন্ট গেটওয়ে বা ব্যাংক ও এমএফআই, এসএমএস সার্ভিস ইত্যাদির এপিআই বা ক্রেডেনশিয়াল শেয়ার করবো। তাই টেস্ট শেষে কোন কিছু ডিলেট করতে হবে না বা কোন কিছু এন্ট্রি করতে গিয়ে দিধা করার দরকার নেই।"*

- **সব বিদ্যমান তথ্যই ডেমো** — দান, সদস্য, হিসাব, লেনদেন, কমিটি, সবই। তাই পরীক্ষার সারি বানাতে বা রেখে দিতে দ্বিধা করার দরকার নেই, আর **পরীক্ষা শেষে মুছে ফেলার দরকারও নেই**।
- **লাইভে যাওয়ার আগে ব্যবহারকারী নিজে বলে দেবেন কোন কোন তথ্য মুছতে হবে** — তার আগে নিজে থেকে কিছু মুছে ফেলা যাবে না।
- **⚠️ তবু দুটি জিনিস আগের মতোই থাকবে:** ① সত্যিকারের ই-মেইল যেখানে যাবে (রশিদ/রিমাইন্ডার) সেখানে অকারণে বারবার পাঠানো হবে না; ② **পর্দায় দেখানো যেকোনো ডেমো আর্থিক তথ্যের পাশে লাল সতর্কবার্তা থাকতেই হবে** — কারণ দর্শক জানেন না কোনটা ডেমো।
- **ক্রেডেনশিয়াল ব্যবহারকারী দেবেন** (গেটওয়ে · ব্যাংক · MFS · SMS)। ⚠️⚠️ **সেগুলো কখনো চ্যাটে বা কোডে লেখা হবে না** — কেবল Supabase Dashboard → Project Settings → Edge Functions → Secrets-এ ব্যবহারকারী নিজে বসাবেন; আমি কেবল নামটি (`SSLC_STORE_ID` ইত্যাদি) বলে দেব।

## 🧾 দাতার তথ্যের ধাপ — গেটওয়েতে অটো-ফিল (২৩ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর চাওয়া (হুবহু):** *"কার্ড পেমেন্ট এর অপশনে গিয়ে প্রথমে ইনফরমেশন ইনপুট দিবে তারপর প্রসেস দিলে এসএসএল এর পেমেন্ট গেটওয়েতে যাবে। যাওয়ার সময় কার্ডে ইনপুটকৃত তথ্য এসএসএল এ অটো ফিল হওয়া প্রয়োজন।"*

### ⚠️⚠️ কার্ডের তথ্য অটো-ফিল করা যায় না — দুটি স্বাধীন কারণ

১. **SSLCommerz-এর সেশন API-তে কার্ডের কোনো প্যারামিটারই নেই।** `gwprocess/v4/api.php` নেয় `store_id`, `total_amount`, `tran_id`, `cus_*`, `product_*`, `multi_card_name` ইত্যাদি — নম্বর/Expiry/CVV পাঠানোর কোনো ঘর নেই। তাদের পাতাটি ভিন্ন ডোমেইনে, তাই ব্রাউজার থেকে JS দিয়ে ভরে দেওয়াও যায় না (same-origin policy)। **হোস্টেড গেটওয়ের পুরো উদ্দেশ্যই এটি** — কার্ডের তথ্য যেন মার্চেন্টের সিস্টেম কখনো না ছোঁয়।
২. **আইনগত।** নিজের পাতায় কার্ড নিলে PCI-DSS-এর **SAQ-D** স্তরে পড়তে হয় (বার্ষিক অডিট, পেনিট্রেশন টেস্ট), আর **CVV সংরক্ষণ বা প্রেরণ অনুমোদনের পরে সর্বাবস্থায় নিষিদ্ধ** — কোনো কমপ্লায়েন্স স্তরেই অনুমোদিত নয়। ফাঁস হলে দায় ফাউন্ডেশনের।

**নিয়ম: এই সিদ্ধান্তটি চূড়ান্ত। ভবিষ্যতে "কার্ডের ঘর ফিরিয়ে আনো" ধরনের অনুরোধ এলে আগে এই দুটি কারণ মনে করিয়ে দিতে হবে।**

### যা সত্যিই অটো-ফিল হয় — `cus_*` ঘরগুলো

**সিদ্ধান্ত (ব্যবহারকারী, AskUserQuestion-এ): "দাতার তথ্যের ধাপ যোগ করো।"**

- `KHUI.payPicker()`-এ নতুন ঘর **"আপনার তথ্য"** (`.kh-pay-cus`) — নাম · মোবাইল · ই-মেইল · ঠিকানা · শহর/জেলা · পোস্ট কোড। কেবল **অনলাইন মাধ্যম** বাছলে দেখা যায়; ম্যানুয়াল পথে (নিজে পাঠিয়ে TrxID) লুকানো।
- **অটো-ফিল দুই উৎস থেকে:** প্রথমে `opts.prefill` (পেজের নিজের ফর্ম), তারপর লগইন থাকলে `get_my_profile` (নাম, মোবাইল, ই-মেইল, `address`, `district`)। ⚠️ **খালি ঘরই কেবল ভরা হয়** — ব্যবহারকারীর লেখা কখনো মোছা হয় না (`cusFill(src, force)`)।
- **`opts.cusFields`** দিয়ে কোন ঘরগুলো দেখানো হবে তা ঠিক করা যায়। `donation.html`-এ `['address','city','postcode']` — নাম/মোবাইল/ই-মেইল ঐ পেজের উপরের ফর্মেই আছে, **দুবার দেখানো হয় না**।
- ব্রাউজার যাচাই করে (নাম ≥২ অক্ষর, মোবাইল `^01[3-9]\d{8}$`, ই-মেইল দিলে গঠন), কিন্তু **আসল সীমা সার্ভারে** — `start_payment` কেবল সাদা-তালিকার ঘরগুলো রাখে (`name · mobile · email · address · city · postcode`) ও দৈর্ঘ্য কেটে দেয়; বাকি সব ফেলে দেয়। যাচাই: `card_number`/`cvv` পাঠিয়ে দেখা গেছে সেশনের payload-এ ঐ দুটি **নেই**।
- `payment_session_for_gateway()` এখন `customer`-এ `address/city/postcode`ও দেয় (payload → প্রোফাইল → সাধারণ ফলব্যাক)।
- **⚠️ `pay-start`-এ `cus_add1/cus_city/cus_postcode` হার্ডকোড `"Faridpur"/"7800"` ছিল** — অর্থাৎ দাতা যেই হোন, গেটওয়েতে একই ঠিকানা যেত। এখন সেশনের আসল মান, সাথে `cut()` দিয়ে SSLCommerz-এর দৈর্ঘ্যসীমা মানা হয় (নাম/ই-মেইল ৫০, ফোন ২০, ঠিকানা ৫০)।
- **⚠️ মোবাইল ও পোস্ট কোডে বাংলা অঙ্ক নিজে থেকেই ইংরেজিতে বদলায়** — অনেকে বাংলা কী-বোর্ডে লেখেন, গেটওয়ে বাংলা অঙ্ক নেয় না।
- ঘরের নিচে স্থায়ী সবুজ নোট: *"কার্ডের নম্বর, মেয়াদ বা CVV কখনো এই পাতায় চাওয়া হয় না… কেউ এই সাইটে কার্ডের নম্বর চাইলে বুঝবেন সেটি আমাদের পাতা নয়।"* — এটি সাজসজ্জা নয়, **ফিশিং-প্রতিরোধ**।

### ⚠️⚠️ যে বাগটি ধরা পড়ল — `.kh-pay-f` ক্লাসের সংঘর্ষ

দাতার ঘরগুলোও `.kh-pay-f` ক্লাস ব্যবহার করে এবং DOM-এ রেফারেন্সের ঘরটির **আগে** বসে। ফলে পুরনো লাইনগুলো —

```js
var input = host.querySelector('.kh-pay-f input');   // ← রেফারেন্স ধরার কথা
```

— **ঠিকানার ঘরটি** ধরে ফেলত, আর তাতে TrxID-র সাফাই (`[^A-Za-z0-9-/]`) প্রয়োগ হয়ে **বাংলা ঠিকানা লিখলেই ঘরটি ফাঁকা হয়ে যেত**। লাইভে সত্যিকারের টাইপ করে ধরা পড়েছে (`addr: ""`)। এখন চারটি নির্বাচকই `.kh-pay-manual`-এর ভেতরে সীমাবদ্ধ।

**নিয়ম: শেয়ার্ড কিটে একই ক্লাস (`.kh-pay-f` ধরনের) দুই জায়গায় ব্যবহার করলে `host.querySelector()` কখনো ব্যবহার করা যাবে না — অভিভাবক ঘর ধরে সীমা টেনে দিতে হবে।** (২২ সেপ্টেম্বরের `var stage` সংঘর্ষের ঠিক পরের দিনই দ্বিতীয় নাম-সংঘর্ষ।)

### যাচাই (লাইভ পেজে নতুন কোড বসিয়ে, **আসল বাটনে ক্লিক করে**)

- ঘর দেখা/লুকানো: বিকাশ/কার্ড → দেখা যায় · "কার্যালয়ে নগদ" → লুকানো · আবার কার্ডে ফিরলে **লেখা তথ্য অটুট** (`paint()`-এ নতুন করে আঁকা হয় না)।
- বাংলা অঙ্ক `৭৮৬০` → `7860`; `78a60!#` → `7860`; বাংলা ঠিকানা ও "ফরিদপুর" **অটুট**; TrxID ঘরে `trx 9f2k-7x/1a০৫ঘ!@#` → `TRX9F2K-7X/1A` (দুটি ঘর আর একে অপরকে ছোঁয় না)।
- **AA ফেল: কিছু নেই** — সবচেয়ে খারাপ **৫.৫৬:১** (নোটের আইকন); শিরোনাম/লেবেল/ইনপুট ১৫.০৪ · উপশিরোনাম ৭.০৮ · নোট ৮.৫৯ · আইকন ৬.০৮ · ত্রুটি ৫.৮৯:১। **ডার্ক ও লাইট — হুবহু এক** (ঘরটি নিজের সাদা পটভূমি বহন করে), কোনো উপাদানে `opacity < 1` নেই।
- **আড়াআড়ি স্ক্রল ০**, একটিও উপাদান ঘর ছাড়ায় না।
- **শেষ-থেকে-শেষ:** ৳৫০০ → "কার্ড / POS" → **আসল "এখনই পরিশোধ করুন" বাটনে ক্লিক** → কাচের পর্দা (`kh-cp is-busy`), কার্ডে `KH26 0923 2306 29B1 CC4A`, `unhandledrejection` **০** → ব্রাউজার সত্যিই `sandbox.sslcommerz.com/EasyCheckOut/…`-এ পৌঁছেছে ("PAY 500 BDT")। ডাটাবেজে সেশন `launched`, payload-এ বাংলা নাম · মোবাইল · ই-মেইল · বাংলা ঠিকানা · "ফরিদপুর" · `7860` — সবই অটুট।
- গোপনীয়তা: `anon` `payment_session_for_gateway` ডাকলে `permission denied` (`create or replace`-এর পর আবার revoke করা হয়েছে — প্রকল্পের পুরনো শিক্ষা)।

**⚠️ সীমা জেনে রাখা:** SSLCommerz-এর EasyCheckOut পাতায় গ্রাহকের ঘরগুলো সাধারণত **দেখানোই হয় না** — মানগুলো সেশনে ও রশিদে বসে, আর যেসব চ্যানেল গ্রাহকের তথ্য চায় সেখানে পূরণ অবস্থায় আসে। অর্থাৎ "অটো-ফিল" মানে দাতাকে ঐ তথ্য **আর কোথাও টাইপ করতে হয় না**, পর্দায় ঘর দেখা যাবেই এমন নয়।

**ক্যাশ-বাস্টিং — এখন `?v=36`**, সব ১৯টি HTML-এ (দৃশ্যমান ১৮ ফাইলে ৩৬ + `loan-application.html`-এ ২; গুনে মিলিয়ে দেখা, `?v=35` অবশিষ্ট ০)।

## 🗣️ বটের সাথে মানুষের মতো কথোপকথন (২৫ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর চাওয়া:** *"আমি তার সাথে হিউম্যান এর মত কথপকোথন করতে চাই… আমি তাকে কর্জে হাসানার বিষয়ে কথা বলব সে আমাকে উত্তর দিবে।"*

**সিদ্ধান্ত (AskUserQuestion-এ):** সমস্যা একটিই — *"কথার ঢং শুকনো · মুখস্থ উত্তরের মতো"*। উত্তরের দৈর্ঘ্য **"ছোট ও আলাপের মতো"**। (বহু-পালা স্মৃতি, কণ্ঠে আলাপ ও পাল্টা প্রশ্ন — এগুলো আগে থেকেই ছিল, তাই ছোঁয়া হয়নি।)

### যা বদলেছে — সবই সার্ভারে

- **সিস্টেম প্রম্পটে নতুন "## তুমি কে" ও "## কীভাবে কথা বলবে" অংশ।** আগের নির্দেশ ছিল "২-৫ বাক্য, সর্বোচ্চ ১২০ শব্দ" — তথ্যবহুল, কিন্তু প্রতিবেদনের মতো। এখন: সালামের উত্তর · **বুলেট ও শিরোনাম নিষিদ্ধ** (উত্তর কণ্ঠেও পড়ে শোনানো হয়) · প্রতিবার একই ভঙ্গিতে শুরু না করা · নাম ধরে ডাকা · আগের কথার সূত্র ধরা · "সিস্টেম অনুযায়ী"/"উপরোক্ত" ধরনের যান্ত্রিক শব্দ বাদ · কষ্টের কথায় আগে সহানুভূতি · অপ্রাসঙ্গিক আড্ডায় ভদ্রভাবে সাড়া দিয়ে প্রসঙ্গে ফেরা · নিজেকে মানুষ বলে দাবি না করা।
- **`temperature` এখন `chat_rules.temperature`** (আগে হার্ডকোড `0.2`)। **⚠️ ১.০-এর কাছাকাছি কখনো নয়** — ভাষার স্বাধীনতা যত বাড়ে, তথ্য থেকে সরে যাওয়ার ঝুঁকিও তত বাড়ে।
- **`chat_rules.extra_rules`** — সিস্টেম প্রম্পটের "যা কখনো করবে না" অংশে হুবহু বসে। নতুন রক্ষাকবচ লাগলে **কেবল এই সেটিংসটি বদলালেই হয়, রিডিপ্লয় লাগে না**।

### ⚠️⚠️ temperature বাড়িয়ে দুটি আসল ড্রিফট ধরা পড়েছিল — এটাই এই কাজের সবচেয়ে বড় শিক্ষা

`0.65`-এ লাইভ কথোপকথন চালিয়ে দুটি ভুল পাওয়া গেছে, দুটিই **ভাষা সহজ হওয়ার সরাসরি পার্শ্বপ্রতিক্রিয়া**:

১. **শর্ত সংকুচিত করা।** গঠনতন্ত্রের ধারা ৭.৩-এ আছে *"মসজিদের নিয়মিত মুসল্লি বা **এলাকার** বাসিন্দা"*, কিন্তু বট বলেছিল *"**ফরিদপুর সদর** এলাকার বাসিন্দা"* — কার্যালয়ের ঠিকানা (KB নথি ৪: "খলিলপুর, ফরিদপুর সদর") মিশিয়ে ফেলেছিল। ফলে অন্য এলাকার কেউ অকারণে নিরুৎসাহিত হতেন।
২. **ভুল ধারা নম্বর।** জরিমানার প্রশ্নে বলেছিল *"ধারা ৭.১ ও **৭.৭**"* — অথচ ৭.৭ হলো *অনলাইন ঋণ আবেদন, বিতরণ ও কিস্তি পরিশোধ*। ৭.১ (সুদমুক্ত, কোনো জরিমানা নয়) ও ৭.৫ (মেয়াদ বাড়ানো) ঠিক ছিল।

**সমাধান:** `temperature` `0.65 → 0.55`, আর `extra_rules`-এ তিনটি নিয়ম —
• ধারা/অধ্যায়ের নম্বর **কেবল তখনই** বলবে যখন উপরের পাঠে ঠিক ঐ নম্বরটি দেখা যাচ্ছে; সন্দেহ হলে নম্বর বাদ দিয়ে নিয়মটি বলবে;
• কোনো শর্ত **নিজে থেকে সংকুচিত/নির্দিষ্ট করবে না** — "কার্যালয়ের ঠিকানা আর যোগ্যতার এলাকা এক জিনিস নয়";
• "আমি কি পাব?" প্রশ্নে শর্ত বলবে, কিন্তু **যোগ্যতার রায় দেবে না** — সিদ্ধান্ত ঋণ যাচাই কমিটির।

**নিয়ম: বটের ভাষা সহজ বা উষ্ণ করার যেকোনো পরিবর্তনের পর অন্তত একটি টানা কথোপকথন চালিয়ে প্রতিটি সংখ্যা, শর্ত ও ধারা নম্বর জ্ঞানভান্ডারের সাথে মিলিয়ে দেখতে হবে। ঢং বদলালে তথ্যও নড়ে — এটি অনুমান নয়, এবারই দুবার ধরা পড়েছে।**

### যাচাই (লাইভে, অতিথি হিসেবে, টানা কথোপকথনে)

| পরীক্ষা | ফল |
|---|---|
| "আসসালামু আলাইকুম, কেমন আছেন?" | সালামের উত্তর + স্বাভাবিকভাবে কথা এগিয়ে নেওয়া ✅ |
| "কর্জে হাসানা আসলে কী?" | আলাপের ভাষায়, শেষে পাল্টা প্রশ্ন ✅ |
| "আমার মুদি দোকান আছে, ঋণ নিতে চাই" | ৫,০০০–১৫,০০০ · ৬–১২ মাস · ১ গ্যারান্টর — **ধারা ৭.২-এর সাথে হুবহু মিলেছে** ✅ |
| যোগ্যতা | "এলাকার বাসিন্দা বা স্থানীয় মসজিদের নিয়মিত মুসল্লি", ১৮–৬৫ — **আর সংকুচিত করেনি** ✅ |
| "জরিমানা দিতে হবে?" | "কোনো জরিমানা নেই, আগে জানালে সময় বাড়ানো হয়" — **ভুল ধারা নম্বর আর বলেনি** ✅ |
| "সভাপতি ও কোষাধ্যক্ষ কে?" | শফিকুজ্জামান (মিন্টু মৌলভী); কোষাধ্যক্ষ "পদটি খালি" ✅ |
| "মিতালীর দান কত?" | অস্বীকার — গোপনীয়তার সীমা অটুট ✅ |
| "আবহাওয়া সুন্দর, তাই না?" | দুই বাক্যে সাড়া দিয়ে প্রসঙ্গে ফেরা ✅ |
| "আপনি কি একজন মানুষ?" | "না, আমি একটি সহায়িকা প্রোগ্রাম" ✅ |
| মার্কডাউন | বুলেট **০**, `**` **০**, শিরোনাম **০** — কণ্ঠে পড়ার জন্য নিখুঁত ✅ |

⚠️ পরীক্ষার মাঝে একবার উত্তর আসেনি। লগে কারণ স্পষ্ট: **`gemini 503 — This model is currently experiencing high demand`** — Google-এর সাময়িক সমস্যা, আমাদের কোডের নয়। (CLAUDE.md-র পুরনো নিয়মটি এখানেই কাজে লাগল: *"বট চুপ হয়ে গেলে সবার আগে `function_logs`-এ `kh-chat model error` খুঁজতে হবে"*।) **চাইলে পরে যোগ করা যায়:** 503/429-এ একবার নিজে থেকে আবার চেষ্টা করা — এখন দর্শক সরাসরি "দুঃখিত…" বার্তাটি দেখেন।

**⚠️ `?v=` বাড়ানো হয়নি ও `push-to-github.bat` লাগে না** — `kh-ui.css`/`kh-ui.js`/HTML কিছুই ছোঁয়া হয়নি; পুরো পরিবর্তন Edge Function ও `app_settings`-এ, অর্থাৎ **সাথে সাথেই লাইভ**।
**`chat_knowledge` বদলানো হয়নি — কারণ:** দর্শকের জন্য কোনো নিয়ম, হার বা সেবা বদলায়নি; কেবল বটের কথার ভঙ্গি বদলেছে (স্থায়ী নিয়মটি নিয়ম-পরিবর্তনের জন্য, ভঙ্গির জন্য নয়)।

## 🧕 Speak to Me — মুখোমুখি কথা বলার মানব ইন্টারফেস (সেশন ১ · ২৫ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর চাওয়া:** হ্যান্ডস-ফ্রির কাছে "Speak to Me" বাটন → আলাদা মানব ইন্টারফেস, মুখ নাড়িয়ে কথা বলবে, **ইসলামি পোশাকে নারী**, সফট-মধুর কণ্ঠ, আবেগ, হাতের ইশারা — "দেখে মনে হবে মানুষের সাথে কথা বলছি" (MTB-র MITA-র মতো)।

**সিদ্ধান্ত (AskUserQuestion-এ):** ⚠️⚠️ **"প্রতিষ্ঠানের খরচ করার মত ফান্ড নেই… সিস্টেম ইনবিল্ট এভাটার তৈরী করতে চাচ্ছি"** — অর্থাৎ HeyGen/Tavus/Simli-র মতো মিনিট-প্রতি-খরচের কোনো সেবা নয়। চেহারা **একজন সত্যিকারের নারীর (লিখিত সম্মতিসহ)**। কণ্ঠ Azure বাংলা নারীকণ্ঠ (পরের ধাপ; এখন ব্রাউজারের কণ্ঠ)। **কেবল লগইন করা সদস্য।** কাজ কয়েকটি সেশনে ভাগ করা যাবে।

### ⚙️ কৌশল — ভিডিও-ক্লিপের অবস্থা-যন্ত্র (সম্পূর্ণ ফ্রি)

- ঐ নারীর ছোট ক্লিপ: `idle · listen · think · talk-1/2/3 · talk-smile · talk-empathy · greet · explain · point · nod` + `poster.jpg`। প্রতিটি ক্লিপ **একই "মূল ভঙ্গি"তে শুরু ও শেষ** — তাই দুটি `<video>` ক্রসফেড (৩০০ms) করলে জোড়া চোখে পড়ে না। ক্লিপ থাকে `images/avatar/`-এ, তালিকা `images/avatar/manifest.json`-এ: `{"clips":{"idle":["idle.mp4"],…},"poster":"poster.jpg"}`।
- **manifest না থাকলে ছবি-মোড** — বর্তমান হাসানা এভাটারের ছবি ধীরে শ্বাস নেয়, কথার সময় দোলে, নিচে শব্দতরঙ্গ; সাথে লেখা থাকে "প্রাকদর্শন — সত্যিকারের ভিডিও এভাটার শীঘ্রই"। **এখন এই মোডই চলছে।**
- **কোন ক্লিপ চলবে ঠিক করে বটের নিজের উত্তর।** kh-chat `mode:"avatar"`-এ মডেল উত্তরের শুরুতে `[[mood=…;gesture=…]]` দেয়; সার্ভার `pullTags()` দিয়ে পার্স করে **সাদা-তালিকায় যাচাই** করে (`mood`: smile/neutral/empathy/serious · `gesture`: greet/explain/point/nod/none) ও লেখা থেকে মুছে দেয় — ক্লায়েন্ট কখনো কাঁচা ট্যাগ পায় না। ক্লায়েন্টও আবার সাদা-তালিকা মেলায়। ⚠️ **নতুন ক্লিপ/ইশারা যোগ করলে সার্ভারের `MOODS`/`GESTURES` ও ক্লায়েন্টের `STM_MOODS`/`STM_GESTS` — দুই জায়গাতেই যোগ করতে হবে।**
- ⚠️ ট্যাগ **দুই মোডেই** মোছা হয় (চ্যাট মোডে মডেল ভুল করে দিলেও "[[…]]" পর্দায়/কণ্ঠে যায় না), কিন্তু `mood`/`gesture` ফেরত আসে কেবল avatar মোডে।
- **অবস্থা:** খোলা → `greet` (একবার) → `talk_<mood>` → বাক্য শেষে ৪০০ms-এ নতুন বাক্য না এলে `idle` (তাই নীরবতায় মুখ নড়ে না) → বলা শেষে `listen` → প্রশ্ন পেলে `think` → উত্তর। না-থাকা ক্লিপের জায়গায় কাছাকাছিটি: `talk_x → talk`, ইশারা → কথা, `listen/think → idle`।
- **টানা আলাপ** — সবসময় হ্যান্ডস-ফ্রি; মাইক বাটনে থামানো/চালু; কথার মাঝে মাইক চাপলে তাঁর কথা থামিয়ে শোনা শুরু। টানা তিনবার নীরবতায় মাইক নিজে থেকে থামে (অকারণে খোলা থাকে না)।
- ⚠️⚠️ **ঠোঁট শব্দে-শব্দে মেলে না** — কথা বলার ক্লিপে মুখ স্বাভাবিকভাবে নড়ে মাত্র। শব্দ-নির্ভুল লিপ-সিঙ্ক (viseme) পরের কোনো সেশনের কাজ; ব্যবহারকারীকে জানানো হয়েছে।

### 🧱 কোড কোথায়

- `kh-ui.js` → `mountAssistant()`-এর ভেতরে (প্যানেলের কণ্ঠ/মাইক/ইতিহাস একসাথে ব্যবহারের জন্য): `openStm · closeStm · stmShow · stmSay · stmListen · stmAsk · stmGate · stmLoadClips`। প্যানেল ও এভাটার **একই `history`** ভাগ করে।
- `speak()`-এ নতুন `opts.force` (মিউট উপেক্ষা — মুখোমুখি আলাপে কণ্ঠই একমাত্র পথ) ও `opts.onChunk(on, text)` (প্রতিটি বাক্যের শুরু/শেষ — মুখ এটি দিয়েই নড়ে-থামে)। `startListen()` এখন `stm` খোলা থাকলে চলে না — **দুটি মাইক একসাথে নয়।**
- ⚠️ **প্রতিটি কলব্যাক `stmAlive(id)` দেখে** — বন্ধ করে সাথে সাথে খুললে পুরনো সেশনের মাইক/কণ্ঠ নতুন সেশনে কিছু করে না (যাচাই করা)।
- বাটন: `.kh-bot-stmrow` > `.kh-bot-stm` — ⚠️ মাথার সারিতেই বসানো যায়নি (৩৬৬px প্যানেলে নামের জন্য ~৩০px থাকত), তাই একই গ্র্যাডিয়েন্টে **দ্বিতীয় সারি, ডানে — হ্যান্ডস-ফ্রির ঠিক নিচে**। কেবল `SpeechRecognition` ও `speechSynthesis` দুটো থাকলেই দেখা যায়।
- CSS: `kh-ui.css` **সেকশন ২৭**, প্রিফিক্স `.kh-stm-`। পর্দা নিজের গাঢ় পটভূমি বহন করে (লগইন পাতার ক্যালিগ্রাফি + পর্দা) → **দুই থীমে হুবহু এক**। `z-index: 10060` (পেমেন্ট পর্দা ১০০৫০-এর উপরে)।
- **"AI সহায়িকা · হাসানা" চিহ্ন সবসময় দৃশ্যমান** — কেউ যেন মানুষ ভেবে ভুল না করেন; বটও জিজ্ঞেস করলে বলে সে প্রোগ্রাম।
- **লেখা (CC) ডিফল্টে বন্ধ** ("অডিওর বিনিময়ে অডিও" নিয়ম), `localStorage.kh_stm_cc`-এ মনে রাখে। কণ্ঠ না চললে উত্তর নিজে থেকেই লেখায় আসে।
- **লগইন গেট দুই স্তরে:** ক্লায়েন্ট সেশন না পেলে তালা-পর্দা + `user-login.html?next=…`; সার্ভার `mode:"avatar"` ও uid নেই → `need_login:true` (যাচাই করা)।
- `push-to-github.bat`: ⚠️ `xcopy`-তে `/E` ছিল না — `images\avatar\` **কখনো লাইভে যেত না**। যোগ করা হয়েছে।
- নির্দেশিকা: **`এভাটার_ভিডিও_রেকর্ডিং_নির্দেশিকা.md`** — সম্মতিপত্র, আলো/পোশাক/ফ্রেম, ১২টি ক্লিপের শট-তালিকা, Adobe Express/Canva দিয়ে ছাঁটাই-ক্রপ (৭২০×৯৬০, শব্দ বাদ, প্রতিটি < ১.৫ MB)।

### যাচাই (লাইভ পেজে নতুন কোড বসিয়ে, **আসল বাটনে ক্লিক করে**)

- লঞ্চার → প্যানেল → **"Speak to Me"** → পর্দা খুলেছে (লগইন অবস্থায়), ছবি-মোড, সালামের কণ্ঠ শুরু; `unhandledrejection` **০**।
- avatar মোডে সত্যিকারের প্রশ্ন ("মা অসুস্থ, জরুরি ঋণ?") → `mood: empathy`, `gesture: nod`, নাম ধরে সহানুভূতি, ট্যাগ ফাঁস **নেই**; অবস্থার ধারা `think → nod → talk(empathy)`।
- অতিথি: সার্ভার `need_login` ✅ · ক্লায়েন্ট তালা-পর্দা, নিয়ন্ত্রণ লুকানো, কণ্ঠ চলে না ✅ (সেশন কেবল সাময়িকভাবে ফাঁকা দেখানো হয়েছিল — **সাইন-আউট করা হয়নি**)।
- দ্রুত বন্ধ-খোলা: একটিই পর্দা, নতুন id; বন্ধের পর overlay ০, scroll-lock মুক্ত, কণ্ঠ বন্ধ ✅।
- **কনট্রাস্ট — AA ফেল কিছু নেই, দুই থীমে হুবহু এক:** সবচেয়ে খারাপ ৫.৪১ (মাইকের সবুজ) · লগইন বাটন ৫.৫৫ · পিলের আইকন ৫.৬৬ · শেষ-বাটন ৬.৪৭ · পিল ৯.১৯ · CC/বন্ধ ১১.৭১ · নোট ১১.৪৯ · চিহ্ন ১৩.৭৩ · অবস্থা ১৫.৯৮ · লেখা ১৯.৪৬:১।
- **৩৭৫×৮১২:** পিল হ্যান্ডস-ফ্রির নিচে ডানে; পর্দার সবকিছু ভিউপোর্টের ভেতরে (মঞ্চ ৩৫২px, মাইক/শেষ-বাটন y≈৬০০–৬৬৫); **আড়াআড়ি স্ক্রল ০**।
- ⚠️ **ভিডিও-ক্রসফেড এই পরিবেশে পরীক্ষা করা যায়নি** — স্বয়ংক্রিয় ব্রাউজারে `<video>` চলে না (২০ সেপ্টের পুরনো শিক্ষা)। আসল ক্লিপ এলে ব্যবহারকারীর নিজের ব্রাউজারে দেখে নিতে হবে।

### ⚠️⚠️ পরীক্ষায় ধরা পড়া বড় সমস্যা — Gemini ফ্রি টিয়ারে **দিনে মাত্র ২০টি অনুরোধ**

লগ: `429 … generate_content_free_tier_requests, limit: 20` — ৪ মিনিট পরেও আটকে ছিল, তাই এটি **দৈনিক** সীমা (রিসেট: মধ্যরাত প্যাসিফিক ≈ **বাংলাদেশ দুপুর ১টা**)। অর্থাৎ পুরো সাইটের চ্যাটবট দিনে ~২০টি প্রশ্নের উত্তর দিতে পারে; মুখোমুখি আলাপ এটি দ্রুত শেষ করবে। ২৫ সেপ্টেম্বরের পরীক্ষাতেই সীমা শেষ হয়েছিল।
- **সীমা মডেলভেদে আলাদা** — `gemini-3.5-flash-lite` দিয়ে পরীক্ষা করে উত্তর এসেছে, **কিন্তু ঋণের যোগ্যতা জিজ্ঞেস করলে সদস্যপদের শর্ত (ধারা ৩.২: "বাংলাদেশের নাগরিক", "ফৌজদারি মামলা") বলেছে** — ধারা ৭.৩ নয়। **তাই সাথে সাথে `gemini-3.6-flash`-এ ফেরানো হয়েছে।** **নিয়ম: মডেল বদলানোর আগে একটি আর্থিক-নিয়মের প্রশ্ন জ্ঞানভান্ডারের সাথে মিলিয়ে দেখতে হবে — সস্তা/হালকা মডেল ভুল অধ্যায় থেকে উত্তর দেয়।**
- সমাধানের পথ (ব্যবহারকারীর সিদ্ধান্ত বাকি): ① AI Studio-র প্রজেক্টে বিলিং চালু (Flash মডেলে খরচ খুবই কম, তবু খরচ); ② Google for Nonprofits / Cloud-এর অলাভজনক ক্রেডিটের জন্য আবেদন; ③ দ্বিতীয় একটি ভালো মডেলকে ফলব্যাক রাখা (429 হলে সেটি ডাকা) — প্রতিটির যাচাই লাগবে; ④ 429/503-এ একবার নিজে থেকে আবার চেষ্টা।
- ব্যবহারকারী নিজের সীমা দেখতে পারেন: **ai.dev/rate-limit** (AI Studio)।

### 🔜 পরের সেশনগুলো

1. **ক্লিপ এলে** — `manifest.json` লেখা, আসল ব্রাউজারে ক্রসফেড-সময় ও ক্লিপ-বাছাই মিলিয়ে দেখা, `poster.jpg`।
2. **Azure বাংলা নারীকণ্ঠ** (ফ্রি F0 টিয়ার, মাসে ৫ লাখ অক্ষর) — সব ডিভাইসে একই কণ্ঠ। কি থাকবে কেবল Supabase Secrets-এ; ব্রাউজার পাবে Edge Function-এর দেওয়া সাময়িক টোকেন। (Chrome-এ এখন বাংলা কণ্ঠ না-ও থাকতে পারে — পরীক্ষার Chrome-এ একটিও ছিল না।)
3. **শব্দ-নির্ভুল লিপ-সিঙ্ক** — Azure-এর viseme ঘটনা দিয়ে মুখের অংশে আলাদা স্তর; বড় কাজ, ধাপে ধাপে।
4. Gemini-র দৈনিক সীমার সমাধান (উপরে)।

**ক্যাশ-বাস্টিং — এখন `?v=37`**, সব ১৯টি HTML-এ (`?v=36` অবশিষ্ট ০; `loan-application.html` আলাদা করে বদলানো)।
**জ্ঞানভান্ডার:** "৩. ওয়েবসাইটের সেবা" নথিতে Speak to Me যোগ হয়েছে (মূল নথি, সবসময় প্রম্পটে যায়)। ⚠️ দৈনিক সীমা শেষ থাকায় লাইভ প্রশ্ন দিয়ে যাচাই করা যায়নি — সীমা রিসেট হলে একবার "Speak to Me কী?" জিজ্ঞেস করে দেখতে হবে।

### 🧕 সেশন ২ — AI প্রতিকৃতি, জীবন্ত করা ও হ্যান্ডস-ফ্রি বাদ (২৬ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারীর নির্দেশ:** *"Speak to Me বাটন থাকলে হ্যান্ডস-ফ্রি বাটনের প্রয়োজন নেই… আমি আপাতত কোন ছবি বা ভিডিও আপলোড করতে পারছি না। তুমি এআই জেনারেটেড একটি এভাটার তৈরী করে দিবে।"* পরে যোগ: *"হিজাবের নীচে চুল দেখা যাচ্ছে। যা দেখা যাবে না… অন্য কোন মানানসই কালার এর ইনার হিজাব এর নীচে দিয়ে দিবে।"*

- **হ্যান্ডস-ফ্রি টগল বাদ** — মার্কআপ, `hfB`, CSS `.kh-bot-hf` সব মুছে ফেলা; `handsFree` সবসময় `false`, পুরনো `localStorage.kh_bot_hf` মুছে দেওয়া হয়। প্যানেলের মাইক এখন কেবল **এক প্রশ্নের**; টানা কণ্ঠ-আলাপ কেবল Speak to Me-তে।
- **প্রতিকৃতি — Canva `generate-image`** (ব্যবহারকারীর প্রিমিয়াম অ্যাকাউন্ট): বেগুনি হিজাব, হাসিমুখ বাংলাদেশি নারী, ১০৮৮×১৪৫৬ (৩:৪ — মঞ্চের অনুপাতেই)। v2-তে **হালকা মভ-গোলাপি ইনার হিজাবে কপালের চুল পুরো ঢাকা**। ⚠️ **নারী-প্রতিকৃতিতে কখনো চুল দেখা যাবে না** — নতুন ছবি বানালে প্রম্পটে ইনার হিজাব/আন্ডারস্কার্ফ স্পষ্ট করে লিখতে হবে।
  • ⚠️ **পূর্ণ মাপে নামানোর কৌশল:** `get-assets`-এর thumbnail মাত্র ১৪৯×২০০ ও signed (মাপ বদলালে 403)। তাই হোল্ডার ডিজাইন `DAHWQki9CwE`-এ ছবিটি `insert_fill`/`update_fill` + `crop_media {0,0,794,1062.5}` (⚠️ `update_fill` নিজে থেকে জুম করে দেয়, তাই crop আবশ্যক) → **ব্যবহারকারীর অনুমতি নিয়ে commit** → `export-design` png width 1088 → ব্রাউজারে উপরের ১৪৫৬ সারি কেটে JPEG। ⚠️ edit-transaction কয়েক মিনিটে মেয়াদোত্তীর্ণ হয় — অনুমতির পর সাথে সাথে commit।
  • থাকে `site-assets/bot/stm-avatar-v2.jpg` (৭২০px, ৫৯ KB) ও `@2x.jpg` (১০৮৮px, ১০৫ KB)। আপলোডে সাময়িক anon পলিসি বসিয়ে **ড্রপ করা হয়েছে** (এখন কেবল `site_assets_admin_*` ও `public_read`)। পুরনো `stm-avatar*.jpg` (v1, চুল দেখা যায়) বাকেটে পড়ে আছে কিন্তু কোথাও লিংক নেই।
  • ⚠️ **ছবি বদলালে নতুন ফাইলনাম** — `cacheControl` এক বছর, একই নামে বসালে পুরনোটাই দেখাবে।
- **জীবন্ত করা — `stmFace(host)` (`kh-ui.js`, `stmAlive`-এর ঠিক আগে)**, খরচ শূন্য, সব ক্যানভাসে:
  • **চোয়াল/ঠোঁট:** মুখরেখার (y ৬৯১) নিচের সারিগুলো ২px-এর ফালিতে নিচে সরে (থুতনি পর্যন্ত পূর্ণ, তারপর ৮৮৪-এ শূন্যে মেলায়), উপরের ঠোঁট ২২% ওঠে; পাশে ২০%-এর ঢালে মিলিয়ে দেওয়া; ফাঁকে গাঢ় মুখের ভেতর + ফিকে দাঁত + জিভের আভাস। খোলার মাত্রা তিনটি সাইন-ছন্দে (৪.৪/৬.৯/২.৩ Hz) + মাঝে বিরতি; কণ্ঠ `onboundary` দিলে প্রতিটি শব্দে বাড়তি খোলা (`speak()`-এ নতুন `opts.onWord`)।
  • **পলক:** ২.২–৬ সেকেন্ডে, ১৮০ms, ১৮% সম্ভাবনায় জোড়া পলক। ⚠️ শুধু চামড়া টেনে নামালে বন্ধ চোখ ভূতুড়ে-ঝাপসা দেখাত — তাই আগে মাপা রঙের ভরাট ঢাল, উপরে ৩৫% বুনট, নিচে পাপড়ির রেখা।
  • **মাথা:** CSS transform (নিজের `scale(1.06)`, কিনারা ঢাকা) — শ্বাস, দোলা, শোনায় কাত, ভাবনায় উল্টো কাত, `empathy`-তে ২.২° কাত; ইশারা: `nod` দুবার মাথা নাড়া, `greet` কাত+নাড়া, `explain` দোলা, `point` পাশে সরা। `prefers-reduced-motion`-এ মাথা স্থির, ঠোঁট চলে (তথ্য বহন করে)।
  • ⚠️⚠️ **`FACE`-এর সংখ্যাগুলো এই ছবির মাপ** (চোখ ৪৪৯/৬৩৩, ৫০৬; মুখ ৫৪১, ৬৯১, অর্ধপ্রস্থ ৯০; থুতনি ৮২৪)। ছবি বদলালে গ্রিড এঁকে নতুন করে মাপতে হবে (v1→v2-তে মাপ মিলেছে, মেপে দেখা)।
  • `getImageData` নেই → অন্য ডোমেইনের ছবিতে canvas tainted হলেও কিছু ভাঙে না। নিষ্ক্রিয় অবস্থায় (মুখ বন্ধ, পলক নেই) ক্যানভাস আবার আঁকা হয় না — ব্যাটারি বাঁচে; লুকানো ট্যাবে থামে।
  • ছবি না এলে `.is-fallback` — পুরনো ছোট এভাটার (`.kh-av`) থাকে। ভিডিও ক্লিপের manifest থাকলে `stmFace` চলেই না (ক্লিপ অগ্রাধিকার পায়)।
  • পর্দার নোট এখন: **"AI দিয়ে তৈরি প্রতিকৃতি — কোনো সত্যিকারের ব্যক্তির ছবি নয়।"**
- ⚠️ **সীমা:** হাতের ইশারা নেই (একটি স্থির ছবি থেকে হাত বানানো যায় না) ও ঠোঁট শব্দে-শব্দে মেলে না। সত্যিকারের ভিডিও-ক্লিপ লাগলে একই মুখ দিয়ে Veo/Gemini-তে বানিয়ে `images/avatar/manifest.json`-এ বসালেই চলবে।
- **যাচাই:** লাইভ পেজে আসল ছবিতে খোলা মুখ ও বন্ধ চোখ বড় করে দেখা; jsdom-এ পুরো পথ — Speak to Me → ছবি-মোড → `stm-avatar-v2.jpg` লোড → কথার সময় ~১৫০ drawImage/ফ্রেম, কথা শেষে থামে → মাথার transform বদলায় (empathy কাত) → বলা শেষে মাইক শুরু → বন্ধ করলে ক্যানভাস মুছে যায়; হ্যান্ডস-ফ্রি বাটন নেই।
- **জ্ঞানভান্ডার (নথি ৩০)** হালনাগাদ — হ্যান্ডস-ফ্রি বাদ ও AI প্রতিকৃতির কথা। **ক্যাশ-বাস্টিং — এখন `?v=38`** (৩৮টি রেফারেন্স, `?v=37` অবশিষ্ট ০)।

### 🎙️ সেশন ৩ — সার্ভারের নারীকণ্ঠ + অডিও-চালিত ঠোঁট (২৬ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারী:** *"কথা বলার সময় ঠোঁটের অংশটি একদমই মিলছে না। আর যেহেতু এ্যাভাটার ফিমেল তাই ভয়েস সবসময় ফিমেল হতে হবে।"*

- **আসল কারণ:** ব্রাউজারের `speechSynthesis` অডিওর প্রবাহ দেয় না — তাই ঠোঁট চলত কৃত্রিম সাইন-ছন্দে, শব্দের সাথে কোনো যোগ ছিল না। আর কণ্ঠ ডিভাইসভেদে আলাদা (কোথাও বাংলা নারীকণ্ঠ নেই)।
- **সমাধান — Edge Function `kh-tts`** (`verify_jwt:false`, কেবল লগইন করা সদস্য, কোটা `chat_quota`-তে `t:<uid>`, `chat_rules.daily_tts` ডিফল্ট ৮০): Gemini **Interactions API** (`POST /v1beta/interactions`, মডেল `chat_rules.tts_model` = `gemini-3.8-flash-tts`, কণ্ঠ `chat_rules.tts_voice` = **`Achernar` — নরম নারীকণ্ঠ**), `response_format {type:audio, mime_type:audio/l16, sample_rate:24000}` → base64 PCM ফেরত। **বাংলা সমর্থিত** (ডকের ভাষা-তালিকায় "Bangla" ✔️)। ⚠️ বিদ্যমান `GEMINI_API_KEY`-ই চলে, নতুন কি লাগেনি।
  • ⚠️ ভয়েস লাইব্রেরিতে `bn-BD`/`bn-IN` + female ছাঁকলে তালিকা **ফাঁকা** আসে — প্রিবিল্ট কণ্ঠগুলো বহুভাষিক, তাই এটাই স্বাভাবিক।
  • ⚠️⚠️ **`tts_voice`-এ কখনো পুরুষকণ্ঠ বসানো যাবে না** (ব্যবহারকারীর শর্ত)। নারী প্রিবিল্ট: Achernar · Kore · Leda · Aoede · Callirrhoe · Autonoe · Despina · Erinome · Laomedeia · Gacrux · Pulcherrima · Vindemiatrix · Sulafat · Zephyr।
  • সিস্টেম-পরীক্ষা: pg_net থেকে হেডার `x-voucher-secret` = `app_settings.voucher_cron_secret` দিলে অডিওর বদলে কেবল `{ok, bytes}` ফেরে (কি ডাটাবেজের বাইরে যায় না)। যাচাই: ৭.৮ সেকেন্ডের বাংলা সালাম → ৩৭৬ KB PCM।
- **ক্লায়েন্ট (`kh-ui.js`):** `stmSay()` → `stmTtsFetch()` (PCM → `AudioBuffer`, WAV হেডার থাকলে বাদ) → `stmPlay()` — `AnalyserNode`-এ প্রতি ফ্রেমের RMS, নিজে-মানানো শিখর (`peak*0.997`) দিয়ে ০–১ → `stmFace().level(v)`। শব্দ থামলেই মুখ বন্ধ। ব্যর্থ হলে (`quota`/`rate`/`no_key` → ঐ সেশনে আর চেষ্টা নয়) `stmBrowserSay()`।
  • ⚠️ **`AudioContext` খোলা হয় `openStm()`-এর ক্লিকের ভেতরেই** (`stmAudio()`, প্রথম `await`-এর আগে) — পরে খুললে ব্রাউজার শব্দ আটকে দেয়। বন্ধে `ac.close()`, মাইক চাপলে `stmStopAudio()`।
- **⚠️⚠️ ব্রাউজারের কণ্ঠেও পুরুষকণ্ঠ বাদ** — `pickVoice()` এখন কেবল **বাংলা** কণ্ঠ নেয় ও পুরুষ-নামের কণ্ঠ বাদ দেয়; কিছু না পেলে `null` → উত্তর লেখায় আসে। (আগের "বাংলা পুরুষকণ্ঠ ফলব্যাক" নিয়ম বাতিল।) এটি চ্যাট প্যানেলেও প্রযোজ্য।
- **যাচাই (jsdom, নকল AudioContext):** সার্ভার-পথে `speechSynthesis` ০ বার, অডিও বাজে, শেষে মাইক শুরু; `kh-tts` `quota` দিলে ব্রাউজার-কণ্ঠে নামে। ⚠️ আসল শব্দ ও ঠোঁটের মিল স্বয়ংক্রিয় ব্রাউজারে শোনা যায় না — ব্যবহারকারীকে নিজের ব্রাউজারে দেখতে হবে।
- ⚠️ **দেরি:** পুরো উত্তরের অডিও একবারে বানানো হয় (কোটা বাঁচাতে এক উত্তর = এক কল), তাই কথা শুরুর আগে কয়েক সেকেন্ড "কণ্ঠ তৈরি হচ্ছে…"।
- **কোটা নিয়ে সত্য:** সীমা **প্রজেক্টভিত্তিক** (API কি নয়), RPD রিসেট মধ্যরাত প্যাসিফিক। Google Workspace-এর Gemini সাবস্ক্রিপশন API-র সীমা বাড়ায় না; বাড়াতে AI Studio-তে ঐ প্রজেক্টে **বিলিং যুক্ত করতে হয় (Tier 1, সাথে সাথে)**। কি বদলাতে হয় না।
- **ক্যাশ-বাস্টিং — এখন `?v=39`** (৩৮টি রেফারেন্স, `?v=38` অবশিষ্ট ০)। জ্ঞানভান্ডার নথি ৩০ হালনাগাদ।

### ⚡ সেশন ৪ — দ্রুততা, দাঁত, অযাচিত লেখা বাদ (২৬ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারী:** *"সময় বেশী লাগছে। কন্ঠ তৈরী হচ্ছে এই ধরনের অযাচিত লেখা বাদ দিতে হবে… শুধু হাসানা নামটাই ঠিক আছে… দুই ঠোটের মাঝখানে কালো দাগ আসছে… স্বাভাবিক কথা বলার সময় যেমন দাত দেখা যায় সেভাবে।"* (নারীকণ্ঠ এখন ঠিক আছে — নিশ্চিত করেছেন।)

- **⚠️ পর্দায় কোনো অবস্থা-লেখা নেই** — "সংযোগ হচ্ছে/কণ্ঠ তৈরি হচ্ছে/বলছি/ভাবছি/শুনছি" সব বাদ (শোনার সময় সবুজ আভাই যথেষ্ট)। কেবল কাজের বার্তা থাকে: মাইকের অনুমতি, "থামানো আছে", "কিছু শুনতে পাইনি"। **নতুন কোনো অবস্থা-লেখা যোগ করা যাবে না।**
- **চিহ্ন কেবল "হাসানা"** — "AI সহায়িকা · হাসানা" ও "AI দিয়ে তৈরি প্রতিকৃতি…" নোট বাদ (ব্যবহারকারীর সিদ্ধান্ত: "এটা সবাই বুঝতে পারে")। সততা রক্ষা হয় বটের নিয়মে — জিজ্ঞেস করলে বলে সে প্রোগ্রাম। (সেশন ১-এর "চিহ্নটি সবসময় দৃশ্যমান" নিয়ম এতে বাতিল।)
- **সালাম (`STM_GREET`):** "আসসালামু আলাইকুম! আমি হাসানা। কর্জে হাসানা এবং আপনার হিসাব সম্পর্কে কী জানতে চান, বলুন।" — অডিও **আগে থেকে বানানো** `site-assets/bot/greet-achernar-v1.pcm` (৭.৯ সে, ৩৭৮ KB, ~১ সেকেন্ডে নামে; আগে ~৭ সেকেন্ড লাগত)। বানানো হয় `kh-tts`-এর সিস্টেম-পথে `{store:"bot/….pcm"}` দিয়ে (কেবল `bot/*.pcm`)। ⚠️ **সালামের লেখা বা কণ্ঠ বদলালে নতুন নামে ফাইল বানাতে হবে ও `STM_GREET_URL` বদলাতে হবে** — ফাইলটি এক বছরের cache-এ।
- **⚡ টুকরো করে একসাথে চাওয়া (`stmSplit`):** মাপা হয়েছে Gemini TTS-এ সময় ≈ অডিওর দৈর্ঘ্য (৯.৫ সে কথা ≈ ৭ সে; `flash-lite-tts`-ও একই, তাই মডেল বদলানো হয়নি)। তাই উত্তরের প্রথম টুকরো ≤৯০ অক্ষর, বাকিগুলো ≤২২০, সর্বোচ্চ ৪ — **সব একসাথে** অনুরোধ, প্রথমটি আসতেই বাজা শুরু, বাকিগুলো পরপর। কোনো টুকরো ব্যর্থ হলে বাকিটুকু ব্রাউজারের নারীকণ্ঠে। কোটা বেশি খরচ হয় বলে `daily_tts` ৮০ → **২০০**।
- **👄 দাঁত, কালো ফাঁক নয়:** মুখের ভেতর এখন উষ্ণ লালচে-বাদামি (`#5a2429 → #94504c`, কখনো কালো নয়), ফাঁকের উপরের ~৬৬% জুড়ে **উপরের দাঁত** (হাতির দাঁতের রঙ `#ebe1d6 → #b8a597` — ⚠️ ঝকঝকে সাদা দিলে সাদা পাত বসানো মনে হত), দাঁতের হালকা ভাঁজ, বেশি খুললে নিচের দাঁতের কিনারা, কোণে ছায়া, `blur(0.6px)`। সর্বোচ্চ খোলা ১৭ → **১২px** (স্বাভাবিক কথায় মুখ বেশি খোলে না)। যাচাই: লাইভ পেজে আসল ছবিতে তিন মাত্রায় (০.৩৫/০.৬৫/১.০) বড় করে দেখা।
- **ক্যাশ-বাস্টিং — এখন `?v=40`** (৩৮টি রেফারেন্স, `?v=39` অবশিষ্ট ০)। জ্ঞানভান্ডার নথি ৩০ হালনাগাদ ("কেবল 'হাসানা' নামটি থাকে")।

### 🦷 সেশন ৫ — আসল দাঁত, "আপু" সম্বোধন, এক কণ্ঠ, আরও দ্রুত (২৬ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারী:** *"শুধু একটি সাদা দাগ আসছে… ফিমেলদের ভাই না বলে আপু… বার বার ভয়েস চেঞ্জ হয়ে যাচ্ছে… উত্তর দিতে বেশ সময় নিচ্ছে।"*

- **👄 দাঁত:** আগে মুখ খুলত ছবির মাপে মাত্র ১২px (পর্দায় ~৪px) আর দাঁত ছিল একটি উপবৃত্ত → "সাদা দাগ"। এখন খোলা `(2.5 + open*21.5)*k` (সর্বোচ্চ ≈ ৭ মিমি; মাপ: ঠোঁট ১৮০px ≈ ৫০ মিমি), আর **আলাদা আলাদা উপরের দাঁত** — মধ্য (২৯) · পাশের (২৪) · শ্বদন্ত (১৯) · প্রিমোলার (১৫), ভেতরে গেলে ছোট ও ছায়ায় (`shade()`), ডগায় হালকা হাসির বাঁক, পেছনে ছায়াময় সারি (ফাঁক গাঢ় গর্ত নয়), উপরের ঠোঁটের ছায়া, বেশি খুললে জিভ ও নিচের দাঁতের একটানা নরম কিনারা। ⚠️ আলাদা গোল গোল নিচের দাঁত পুঁতির মালার মতো দেখাত, আর চওড়া ফাঁক বেড়ার খুঁটির মতো — দুটোই লাইভ ছবিতে দেখে ঠিক করা।
- **👩 সম্বোধন:** নতুন কলাম `user_profiles.gender` (`female`/`male`/null) + RPC `my_salutation()` (নিজের নাম ও লিঙ্গ, authenticated only) ও `set_my_gender(p)`। kh-chat সিস্টেম প্রম্পটে "সম্বোধন" অংশ: নারী → "আপু", **কখনো "ভাই" নয়**; পুরুষ → "ভাই"; অজানা → নাম স্পষ্টভাবে নারী/পুরুষ হলে সেভাবে, সন্দেহ হলে কোনোটাই নয়; অতিথিকে কখনো ভাই/আপু নয়। ডেমো ডেটায় Mitali = female, বাকি তিনজন male বসানো। ⚠️ **বাকি কাজ:** প্রোফাইল ফর্মে (my-profile, সাইন-আপ) লিঙ্গের ঘর এখনো নেই — `set_my_gender` তৈরি আছে, UI যোগ করতে হবে।
- **🎙️ এক কণ্ঠ:** উত্তর এখন সর্বোচ্চ **২ টুকরো** (ছোট প্রথম বাক্য ≤৭০ অক্ষর + বাকিটা এক টানে) — টুকরো যত বেশি, সুর তত বদলাত। কোনো টুকরো ব্যর্থ হলে একবার আবার চাওয়া হয়; তবু না এলে **মাঝপথে ব্রাউজারের কণ্ঠে যাওয়া হয় না** (ওটাই "আরেকজন কথা বলছে" মনে হত) — বাকিটা লেখায় আসে (`stmCaption(…, force)`)। একদমই শুরু না হলে তবেই পুরো উত্তর ব্রাউজারের নারীকণ্ঠে।
  ⚠️⚠️ **TTS-এ `temperature` দেওয়া যাবে না** — ০.২ দিয়ে পরীক্ষায় ৭ সেকেন্ডের সালাম ৪০ সেকেন্ডের অডিও হয়ে গিয়েছিল (১.৯ MB, আবোল-তাবোল দীর্ঘ)। kh-tts কোডে `tts_temperature` ডিফল্ট ০.২, তাই `app_settings.chat_rules.tts_temperature = "off"` বসানো আছে — **এই কি-টি মুছলে কণ্ঠ ভেঙে যাবে।** (ভুলে তৈরি `bot/greet-achernar-v2/v3.pcm` বাকেটে আছে, ব্যবহৃত নয়; সালাম আগের মতো v1।)
- **⚡ দ্রুততা:** মাপা — চ্যাটের মডেল-সময় ছিল **১৩.৭ সেকেন্ড** (Gemini-র ডিফল্ট "গভীর ভাবনা"); এখন `thinkingConfig.thinkingLevel = "low"` (`chat_rules.chat_thinking` / `avatar_thinking`, `"default"` দিলে পুরনো আচরণ) → **২.৫–৫.৮ সেকেন্ড**, আর যোগ্যতা (ধারা ৭.৩: মুসল্লি বা এলাকার বাসিন্দা, ১৮–৬৫) ও জরিমানা নেই — দুটোই নির্ভুল থেকেছে। সাথে: সব ডাটা (জ্ঞানভান্ডার, হিসাব, প্রোফাইল) **একসাথে** আনা (আগে একটার পর একটা), এভাটারে কেবল ১–২ বাক্য, `avatar_kb_budget` ৭০০০, `avatar_max_tokens` ১২০০, ৫০৩-এ একবার আবার চেষ্টা (kh-chat ও kh-tts দুটোতেই)। kh-chat এখন উত্তরে `ms` ও `thinking` ফেরত দেয়।
- **ক্যাশ-বাস্টিং — এখন `?v=41`** (৩৮টি রেফারেন্স, `?v=40` অবশিষ্ট ০)।

### 🌐 সেশন ৬ — দ্বিভাষিক এভাটার ও লিঙ্গের ঘর (২৬ সেপ্টেম্বর ২০২৬)

- **ভাষার বাটন** (`.kh-stm-cc.kh-stm-lang`, Speak to Me-র উপরে "লেখা"-র পাশে): বাংলা ↔ English, `localStorage.kh_stm_lang`। ⚠️ Web Speech একবারে **একটিই** ভাষা চেনে — স্বয়ংক্রিয় শনাক্ত সম্ভব নয়, তাই বাটন। `listenLang()` → `bn-BD`/`en-US`, **চ্যাট প্যানেলের মাইকও** এটিই মানে। শোনার মাঝে বদলালে নতুন ভাষায় আবার শুরু হয়।
- **ইংরেজি সালাম** `STM_GREET_EN` — আগে-বানানো `bot/greet-en-achernar-v1.pcm` (একই Achernar কণ্ঠ, ৭.২ সে)। সার্ভারের কণ্ঠ (Gemini TTS) দুই ভাষাতেই একই নারীকণ্ঠ।
- **ব্রাউজার-ফলব্যাক কণ্ঠ:** `textLang()` লেখায় বাংলা হরফ দেখে ভাষা ঠিক করে; `pickVoice(lang)` ইংরেজিতে কেবল নারী-চিহ্নিত কণ্ঠ নেয় (লিঙ্গ-অজানা কণ্ঠে −৮০), en-IN/en-GB অগ্রাধিকার।
- **সার্ভার:** kh-chat-এ কোড বদলায়নি — `chat_rules.extra_rules`-এ ভাষার নিয়ম: প্রশ্নের ভাষায় উত্তর; ইংরেজিতে ইংরেজি অঙ্ক ও "Tk", "bhai/apu/brother/sister" নয়, নাম বা "you"। ক্লায়েন্ট `lang` পাঠায় (এখন সার্ভার ব্যবহার করে না, ভবিষ্যতের জন্য)।
- **⚠️ ধারা নম্বর — নতুন কড়া নিয়ম:** thinking "low" করার পর ইংরেজি উত্তরে আবার ভুল "Article 7.7" এসেছিল (২৫ সেপ্টের সেই পুরনো ড্রিফট)। তাই `extra_rules`-এর প্রথম নিয়ম এখন: **ধারা/Article নম্বর নিজে থেকে কখনো বলবে না**, কেবল ব্যবহারকারী চাইলে ও পাঠে দেখা গেলে। যাচাই: জরিমানা (ইং ও বাং) ও যোগ্যতা — তিনটি উত্তরই নির্ভুল, কোনো নম্বর নেই।
- **লিঙ্গের ঘর — `my-profile.html`:** "ব্যক্তিগত তথ্য"-এ দুটি পিল (নারী/পুরুষ, `.gen-seg`, রেডিও), সংরক্ষণে `update_my_profile`-এর পরে আলাদা `set_my_gender` (পুরনো RPC-র স্বাক্ষর অপরিবর্তিত)। চালু পিল নিজের গ্র্যাডিয়েন্ট `#c2255c → #8b1fb0` বহন করে (সাদা ≥৫.৩:১), বন্ধ পিল ফর্মের ঘরের রঙে — দুই থীমেই।
- জ্ঞানভান্ডার নথি ৩০-এ দুই ভাষা ও লিঙ্গের কথা যোগ হয়েছে। **ক্যাশ-বাস্টিং — এখন `?v=42`** (৩৮টি রেফারেন্স)।

### 🎙️ সেশন ৭ — এক উত্তর = এক কণ্ঠ-অনুরোধ (২৬ সেপ্টেম্বর ২০২৬)

- **ব্যবহারকারীর পর্যবেক্ষণ:** দুই টুকরোতেও কথার মাঝপথে নারীকণ্ঠ পুরুষকণ্ঠ হয়ে গেছে। Gemini TTS-এর প্রতিটি অনুরোধ আলাদা করে কণ্ঠ "বানায়" — একই `Achernar` নাম দিলেও টুকরোভেদে সুর সরে যায়।
- **সমাধান:** `stmSplit()` এখন সবসময় `[clean]` ফেরত দেয় — **পুরো উত্তর একটিই অনুরোধে**। দাম: প্রথম শব্দ শোনার আগে কয়েক সেকেন্ড বেশি অপেক্ষা (উত্তর এমনিতেই ১–২ বাক্যের)। **নিয়ম: কণ্ঠ ভাঙার ঝুঁকিতে আর টুকরো করা যাবে না।**
- `chat_rules.tts_style` এখন স্পষ্ট করে "Female speaker … Keep exactly the same female voice from start to end" — কোড বদলায়নি, সেটিংসে।
- ⚠️ **সীমা (ব্যবহারকারীকে জানানো):** একটি স্থির ছবিকে ক্যানভাসে নাড়িয়ে MTB-র MITA-র মতো "সম্পূর্ণ হিউমানাইড" হবে না — সেটি ভিডিও-ভিত্তিক ডিজিটাল হিউম্যান (প্রশিক্ষিত মডেল/রেকর্ডেড মানুষ)। পথ তিনটি: ① একই নারীর AI-ভিডিও ক্লিপ (Veo/Canva) → বিদ্যমান `manifest.json` ক্লিপ-ব্যবস্থা (ফ্রি, মানুষের মতো দেখায়, ঠোঁট শব্দে মেলে না); ② পেইড রিয়েল-টাইম সেবা (HeyGen Interactive/D-ID/Simli/Tavus); ③ নিজস্ব GPU সার্ভারে MuseTalk/Wav2Lip। সিদ্ধান্ত ব্যবহারকারীর।
- **ক্যাশ-বাস্টিং — এখন `?v=43`** (৩৮টি রেফারেন্স)।

### 🎬 সেশন ৮ — আরবি ঘরানার নতুন এভাটার + সত্যিকারের ভিডিও ক্লিপ (২৬ সেপ্টেম্বর ২০২৬)

**ব্যবহারকারী:** নির্দিষ্ট কারো মতো নয় — সুন্দর আরবি এভাটার, প্রফেশনাল কল-সেন্টার কর্মী + মুসলিম ভাব। Chrome-এ Workspace অ্যাকাউন্টে লগইন করে দিয়েছেন; Vids-এর ছবি-অধিকারের শর্তে "Agree" চাপার অনুমতি দিয়েছেন।

- **প্রতিকৃতি (v3):** ব্যক্তিগত Gemini-র "Create image" দিয়ে — হেডসেট, নেভি ব্লেজার, বেগুনি হিজাব + ল্যাভেন্ডার ইনার (চুল একটুও নেই), কোনো লেখা নেই (প্রথম সংস্করণে নামফলক ছিল, বাদ দেওয়ানো হয়েছে)। `site-assets/bot/stm-avatar-v3.jpg` / `@2x.jpg` (৭৬৫×১০২৪)।
- **ভিডিও:** ⚠️ ব্যক্তিগত ও Workspace — দুই Gemini-তেই ভিডিওর অপশন নেই। **Google Vids (Workspace) → AI Video → Animate** (Omni, ৭২০p, পোর্ট্রেট, ১০ সে) দিয়ে v3 ছবি থেকে চারটি ক্লিপ বানানো। মুখ প্রতিটিতে একই থেকেছে।
- **প্রক্রিয়া পুরোটাই ব্রাউজারে:** Vids পাতায় ক্লিপের URL `fetch(…,{credentials:'include'})` → blob → `<video>` + canvas-এ **৭২০×৯৬০ ক্রপ (y ১২০ থেকে)** → `MediaRecorder` (`video/mp4;codecs=avc1`, ১.৪ Mbps, **শব্দ ছাড়া**) → Supabase-এ সরাসরি আপলোড (সাময়িক anon পলিসি `bot/avatar/%`, কাজ শেষে **ড্রপ করা হয়েছে**)। ক্রপে নিচের কোণের Gemini ✦ চিহ্নটিও বাদ পড়ে; অদৃশ্য SynthID থেকে যায়।
  ⚠️ ছবি ফাইল-ইনপুটে দেওয়া হয় `DataTransfer` দিয়ে (Supabase থেকে fetch করে `File`) — নেটিভ ফাইল-পিকার খোলা যায় না।
- **ক্লিপ (`site-assets/bot/avatar/`):** `idle-1` (৫.৩ সে, মুখ বন্ধ) · `listen-1` (৬.৬) · `talk-1` (৯.৮, টানা কথা) · `talk-empathy-1` (৭.৮) · `greet-1` (৪.৪, হাসিমুখে কথা — `talk_smile`-এ) · `poster.jpg` · `manifest.json` (cache ৬০ সে)। নতুন ক্লিপ যোগ করলে কেবল ফাইল + manifest বদলালেই চলে।
- **কোড:** `STM_BASE`-এর ডিফল্ট এখন ঐ Supabase ফোল্ডার (`window.KH_AVATAR_BASE` দিয়ে বদলানো যায়)। ⚠️ **ক্লিপ-মোডে কণ্ঠ আসার আগে `think` (→ idle) দেখানো হয়, অডিও শুরু হলে তবেই কথা-বলার ক্লিপ** — নাহলে কয়েক সেকেন্ড নিঃশব্দে ঠোঁট নড়ত। অডিও শুরুর শর্ত `!/^talk/.test(stm.state)` (আগে কেবল `idle` ছিল)।
- **চ্যাট বাটনের ছোট এভাটারও নতুন মুখে:** `bot/avatar/hasana-avatar.jpg` / `@2x` (v3 থেকে বর্গ ক্রপ), `kh-ui.css`-এর পাঁচটি URL বদলানো।
- ⚠️ **বাকি:** ছবি-মোডের ফলব্যাক (manifest না এলে) এখনো পুরনো v2 ছবি + v2-এর `FACE` মাপ — চেহারা আলাদা। v3-এ বদলাতে হলে FACE নতুন করে মাপতে হবে। ঠোঁট শব্দে-শব্দে মেলে না (ক্লিপের নিজের নড়াচড়া)। হাতের ইশারা/`think` ক্লিপ নেই।
- **যাচাই:** পাঁচটি ক্লিপ Supabase থেকে লোড হয় (সবই ৭২০×৯৬০); ফ্রেম ধরে দেখা — চুল দেখা যায় না, লেখা নেই, মুখ একই। ⚠️ আসল পর্দায় ক্রসফেড স্বয়ংক্রিয় ব্রাউজারে দেখা হয়নি — ব্যবহারকারীকে দেখতে হবে।
- **ক্যাশ-বাস্টিং — এখন `?v=44`**।

## ⏱ হিরোর ফ্লিপ ঘড়ি (index.html · ২৫ সেপ্টেম্বর ২০২৬)

- `digital watch.mp4` থেকে হুবহু, **ছোট আকারে** (ডেস্কটপে ২৯১×১৪৮px, ≤৪৮০px-এ ২৫৭px), হিরোর বোতামগুলোর নিচে। সিদ্ধান্ত: **কেবল হোম পেজের হিরোতে**, লেখা **ভিডিওর মতো ইংরেজি** (LIVE · HOURS · MINUTES · SECONDS · "Friday, September 25, 2026")।
- ভিডিওর কোড-প্যানেল থেকে হুবহু: `perspective:260px`, `font-size: calc(var(--h) * .72)`, `text-shadow: 0 0 18px var(--amber-glow)`, পাতা `rotateX(-180deg)` `.42s cubic-bezier(.6,.04,.4,1)`। রঙ পিক্সেল মেপে: অ্যাম্বার `#ffa52f`, ফ্রেম `#121212`। ফন্ট Oswald 700 + JetBrains Mono (index.html-এর Google Fonts লিংকে যোগ করা)।
- **ঢাকার সময়, ২৪ ঘণ্টা** (`Intl … timeZone:'Asia/Dhaka', hourCycle:'h23'`) — দর্শক বিদেশে থাকলেও।
- সব CSS/JS `index.html`-এর ভেতরে, প্রিফিক্স `.khfc-` — `kh-ui` ছোঁয়া হয়নি, তাই `?v=` বাড়েনি।
- ⚠️ **ভিডিওর লেবেল ও তারিখ খুব ফিকে (~২:১)** — AA মানতে উজ্জ্বল করা হয়েছে: LIVE ৬.৬৬ · লেবেল ৫.৫৭ · তারিখ ৭.৪৩ · অঙ্ক ৭.০৮:১। ঘড়ি নিজের গাঢ় পটভূমি বহন করে, **দুই থীমে হুবহু এক** (মেপে দেখা); সেকশন ৯-এর ঢালাও নিয়ম (০,২,২) এড়াতে নির্বাচক `html .khfc.khfc …` (০,৩,১) + `!important`।
- ⚠️ ট্যাব লুকানো বা reduced-motion হলে অঙ্ক উল্টানো ছাড়াই সরাসরি বসে।
- **AM/PM বাটন (২৬ সেপ্টেম্বর ২০২৬, ব্যবহারকারীর অনুরোধ)** — LIVE-এর ঠিক পাশে `#khfcAp`। চাপলে ২৪ ↔ ১২ ঘণ্টা; ১২ ঘণ্টায় বাটনেই "AM"/"PM" দেখায় (অ্যাম্বার, `aria-pressed="true"`), ২৪ ঘণ্টায় "24H" (ধূসর)। পছন্দ `localStorage.kh_clock_12`-এ মনে থাকে; ডিফল্ট ২৪ ঘণ্টা। কনট্রাস্ট: 24H ৯.৯১ · AM/PM ৮.১:১। যাচাই (jsdom): ক্লিকে অঙ্ক ও লেখা বদলায়, আবার ক্লিকে ফেরে।
- **🟠 ডার্ক থীমে ফ্রেম কমলা (২৬ সেপ্টেম্বর ২০২৬, ব্যবহারকারীর সিদ্ধান্ত)** — `linear-gradient(145deg,#f39a3a,#f08c28 45%,#e95420)`, ফ্রেমের লেখা গাঢ় কালি `#2a1206` (৭.৯৯ / ৭.১৫ / ৪.৮৪:১ — তিন প্রান্তেই পাস)। অঙ্কের ঘর ও AM/PM বাটন আগের মতো গাঢ়। **লাইট থীমে আগের গাঢ় ফ্রেমই থাকে** ("হোয়াইট থীমের জন্য এই কালারটাই ঠিক আছে")। নির্বাচক `html:not([data-theme="light"]) .khfc.khfc …`।
- যাচাই (লাইভ পেজে বসিয়ে): উল্টানোর মাঝপথের ছবিতে উপরে নতুন ও নিচে পুরনো অঙ্ক দেখা গেছে (ভিডিওর মতো), ৩৭৫px-এ আড়াআড়ি স্ক্রল ০।

## 🧰 ব্যবহারকারীর প্রিমিয়াম অ্যাকাউন্ট — কাজে লাগাতে হবে (ব্যবহারকারীর নির্দেশ: ২৫ সেপ্টেম্বর ২০২৬)

> *"আমার ক্যানভা, গুগল ওয়ার্কস্পেস, Adobe Express এর প্রিমিয়াম একাউন্ট আছে। এটি মেমোরিতে নিয়ে নাও। এখন ও ভবিষ্যতেও আমার টোকেন সেভ করার জন্য এদের সহযোগীতা নিতে পারি।"*

- **Canva (প্রিমিয়াম)** — **কানেক্টর যুক্ত আছে** (`generate-image`, `generate-design`, `export-design`, `remove-background` ইত্যাদি)। ছবি, পোস্টার, ব্যানার, সোশ্যাল পোস্ট, প্রেজেন্টেশন, এভাটারের প্রতিকৃতি — **এসব আমি নিজে SVG/ক্যানভাসে আঁকব না, আগে Canva দিয়ে চেষ্টা করব।** হাতে আঁকা ও বারবার ঠিক করায় টোকেন বেশি খরচ হয়।
- **Adobe Express (প্রিমিয়াম)** — কানেক্টর এখনো নেই; ছবি/ভিডিও সম্পাদনা, Firefly দিয়ে ছবি তৈরি, ব্যাকগ্রাউন্ড মোছা — ব্যবহারকারী নিজে করে ফাইল দিতে পারেন। কাজের আগে জানিয়ে দিতে হবে কোন কাজটি ওখানে সহজ।
- **Google Workspace (প্রিমিয়াম)** — Docs/Sheets/Slides/Drive। ⚠️ Workspace-এর Gemini আর AI Studio-র API কি **আলাদা জিনিস** — Workspace থাকলেই চ্যাটবটের জন্য API কি পাওয়া যায় না (উপরের "অর্গানাইজেশনাল অ্যাকাউন্টে কি নাও পাওয়া যেতে পারে" দেখুন)।
- **⚠️ সীমা জেনে রাখা:** এই তিনটির কোনোটিই **রিয়েল-টাইম, মুখ-নাড়ানো কথোপকথনের এভাটার** চালাতে পারে না — ওরা আগে থেকে তৈরি ছবি/ভিডিও বানায়। লাইভ এভাটারের জন্য আলাদা সেবা লাগে (নিচের "🧕 Speak to Me" অংশ দেখুন)।
- **নিয়ম:** ডিজাইন/মিডিয়া-ধরনের কোনো কাজ এলে আগে ভাবতে হবে — এটি কি Canva বা Adobe Express দিয়ে কম খরচে করা যায়? গেলে সেটিই প্রস্তাব করতে হবে।

## 🚀 ডিপ্লয়

`push-to-github.bat` সব ফাইল GitHub রিপোতে কপি করে কমিট ও পুশ করে → Vercel-এ অটো ডিপ্লয়। নতুন ফাইল বানালে স্ক্রিপ্টে তার কপি লাইন যোগ করতে হবে।

## ✅ যাচাই

- প্রতিটি পরিবর্তনের পর: HTML/CSS/JS সিনট্যাক্স, দুই থীমে কনট্রাস্ট, ও (সম্ভব হলে) jsdom/happy-dom দিয়ে আচরণ পরীক্ষা।
- ব্রাউজারে চোখে দেখা যায় না — তাই কোড ও কনট্রাস্ট মেপে যাচাই করতে হবে, এবং ব্যবহারকারীকে লাইভে দেখতে বলতে হবে।
